# Worklog — 2026-06-30-001 (Phase 1: delivery foundation, parallel w/ Window 1 fingerprint)

Flight recorder — terse, append-as-you-go. Scope: installer DELIVERY plumbing only
(PL-8 frozen-exe serving, baked token+pre-auth-key+tailnet target, uses-check, TTL 1–2h).
NOT touched: TOFU / fingerprint / enrollment-review card / hw_monitor enroll logic (Phase 2).

## Fork resolutions (from owner, this session)
- F1: serve zip = generic prebuilt exe + per-request nemesis_install.conf. Consume-and-delete
  = installer-side requirement (documented; consumption is F4 follow-up).
- F2: code is tailnet-AWARE (conf nemesis_ip = tailnet addr; links prefer tailnet). nginx
  :80→tailnet-only enforcement = PUNCHLIST/infra note, NOT built.
- F3: delivery-side only — add uses-check to download validation; TTL 24h→1–2h. hw_monitor
  enroll validation untouched (Phase 2).
- F4: bake single-use pre-auth key (UI→endpoint→conf). Consumption + spent-key error path =
  documented follow-up.

## Pre-build audit (read-only, Rule 1) — DONE
- generate endpoint dashboard.py:1456 (auth-gated, NOT in _AUTH_EXEMPT — preserve).
- token: token_hex(16), 24h TTL, max_uses=1, auto_approve=1.
- _valid_installer_token:1400 checks revoked+expired only (no uses-check).
- serves legacy .ps1 (1502) → calls install_windows.ps1 (needs Python) = PL-8.
- /exe (1515) + /zip (1530) redirect to GitHub release (generic, no baked conf).
- conf contract (installer_gui _read_baked_config): [nemesis] nemesis_ip/nemesis_port/
  device_name/enrollment_token/support_contact.
- NO generic exe on the box (dist/ absent; built on Windows/CI) → zip assembly needs a
  configured local exe path; hard-fail if absent.
- enrollment_tokens DDL: database.py:188.

## Build steps (DONE)
- database.py: enrollment_tokens +preauth_key (canonical CREATE + ADR-0001 guarded ALTER).
- dashboard.py _valid_installer_token: +`uses < max_uses` (used token now 410s at download).
- dashboard.py: retired _PS1_INSTALLER_TEMPLATE + _render_windows_installer_ps1 +
  _nemesis_server_host → new _nemesis_tailnet_host() + _render_install_conf() (matches
  installer_gui [nemesis] reader; adds preauth_key).
- generate endpoint: TTL 24h→2h; accept+store preauth_key; tailnet-preferring base
  (NEMESIS_PUBLIC_URL|NEMESIS_TAILNET_ADDR|request host); response drops ps1_url, adds
  preauth_key_baked. Auth-gating preserved (NOT in _AUTH_EXEMPT).
- /install/windows/<token> (.ps1): retired → 410 with pointer to frozen bundle.
- /install/windows/<token>/zip: assemble in-memory zip = generic exe (NEMESIS_AGENT_EXE) +
  per-request nemesis_install.conf. Hard-fail 503 if exe unstaged; 410 if token spent/expired.
- /exe route: stale ".ps1 meantime" msg → point to /zip; inherits uses-check.
- UI form: ~2h blurb + Tailscale pre-auth-key input (optional) + helper note.
- agent-enroll.js: submit preauth_key; drop .ps1 link; show preauth_key_baked note.
- PUNCHLIST: PL-8 marked RESOLVED; new [INSTALLER-DELIVERY Phase 1] block (nginx-tailnet
  infra item, NEMESIS_AGENT_EXE staging ops-dep, consume-and-delete + spent-key follow-ups,
  auto_approve-unchanged Phase-2 note).

## Verification (real output)
- py_compile dashboard.py + database.py: OK.
- Isolated test (scratchpad/test_delivery.py): migration adds preauth_key on fresh AND
  old-schema DB (guarded ALTER); uses-check {valid:T, used:F, expired:F, revoked:F}; conf
  parses as [nemesis] with token+preauth_key+tailnet ip+port; zip = exe + conf, conf carries
  key. ALL CHECKS PASSED.
- Rule-8 scan of the diff: NO MATCHES — clean (placeholders only).
- NOT done here: live HTTP verify (410/503/auth-redirect/real download) needs a dashboard
  restart = deploy; deferred/coordinated with Window 1 (commit-first per Rule 5).

## Deploy + live verify (2026-06-30, commit 2e27a60 on origin)
- Rule-6 backup: alerts-PRE-PREAUTH-MIGRATION.db (integrity ok; enrollment_tokens 4 /
  agent_devices 3 / alerts 8 match source).
- `sudo systemctl restart dashboard` → active; /api/health 200.
- Migration applied LIVE: enrollment_tokens now has `preauth_key` column.
- Live HTTP (Flask :5000), real output:
  - POST /api/agent/installer/generate → **302 → /login** (auth-gated, preserved).
  - GET /install/windows/<tok> (.ps1) → **410** (legacy retired).
  - GET /install/windows/{invalid,expired,used}/zip → **410** (uses-check + expiry hard-fail).
  - GET /install/windows/<valid>/zip, exe NOT staged → **503** (NEMESIS_AGENT_EXE gate).
  - Happy-path 200 (zip = exe + conf): proven by isolated test; live 200 awaits staged exe
    (ops dep NEMESIS_AGENT_EXE — documented, not on box).
- Cleanup: 3 ztest_ tokens deleted; enrollment_tokens back to 4; no residue. Service healthy.

## Scope guard (confirmed untouched — Phase 2)
- No agent_devices column reads added; hw_monitor enroll logic untouched; no TOFU/fingerprint/
  review-card code. auto_approve default left =1 (manual-default flip is Phase 2).
