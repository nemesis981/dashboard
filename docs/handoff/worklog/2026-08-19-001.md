Worklog — 2026-08-19, Window 2

- Morning Status run. Baseline: `roadmap-state-audit-2026-08-07.md` (12-day gap,
  no refresh 08-08..08-18). Refreshed to `roadmap-state-audit-2026-08-19.md`:
  file-set +4 (76->80), fully accounted for via git log --diff-filter=A, all 4
  new files STUB/PARKED. No SHIPPED/PARTIAL reclassification found. Elevated
  grants: 2 still unreviewed (ip/piactl, restart hw-monitor), unchanged since
  08-18, now flagged across 3 closeouts. Briefing written to
  `docs/briefing/2026-08-19.md`, mirrored.
- Committed held Linux privilege-fix batch (malware-scan CAP_DAC_READ_SEARCH
  engine + canary functions), `72a7ed0`. Verified PrivateTmp resolves to `no`
  (single directive, Window 1's fix confirmed not re-touched); 5/5 regression
  suites green. Logged canary-coverage gap (no unit-settings self-check) to
  PUNCHLIST.
- Committed mountinfo startup guard follow-up, `88802c1`. Classifier proved its
  own premise against known-private/known-real fixtures before trusting a live
  read. Closes the PUNCHLIST gap from the prior commit.
- Committed held memory-injection ladder work (memladder.py + RUNG_AVAILABILITY
  continuation on mem_appliance.py), `d9ef10e`. Formally requested, previously
  held pending ask. 7/7 suites green.
- Filed Window 3's Attestation Tier 2 scope doc into
  `~/work/nemesis-internal/scoping-and-estimates/`, committed+pushed to
  local+usb (`2ac9392`, nemesis-internal-docs repo). Doc-only, no code — the
  built module stays owed to Window 1.
- Committed cooperative throttle executor seam + wired 4 services, `d18dbd2`.
  New test_throttle.py + existing test_data_manager.py both pass; wider sanity
  sweep green.
- Committed Window 3's error-code wiring batch (Tier A 13 sites/9 files + Tier
  B vpn_status.py convention), `f8854ec`. Independently re-verified two of
  Window 3's own claims rather than trusting them: re-registered all 13 codes
  fresh against a throwaway DB with a rejected-severity control, and confirmed
  the two failing anomaly tests are pre-existing environmental requirements
  unrelated to this diff.
- Committed throttle status surface part 1 (throttle executor + UNTHROTTLED
  status model), `e970d71`. All new + existing tests green.
- Route-security audit on ADR 0023 correlation batch (dashboard.py touched) —
  run per standing practice regardless of Window 1's own no-trigger assessment,
  per explicit operator instruction not to skip it. FOUND A REAL BUG:
  `dashboard.py` closed its DB connection before calling
  `hw_monitor.approved_agent_macs(conn)` on it; the function's own
  `except Exception: return set()` (no logging) silently swallowed the
  resulting `ProgrammingError` on every call, making `has_agent` unconditionally
  False for every device with zero indication anywhere. Confirmed live (closed
  sqlite3 connection raises `ProgrammingError` on `.execute()`). Held the
  commit, reported the finding with file:line citations, declined to fix it
  myself (code content is Window 1's).
- Window 1 fixed both the ordering bug and the silent-swallow root cause
  (approved_agent_macs now logs ERROR on read failure), added
  test_agent_correlation.py with a control reproducing the exact original
  defect. Re-ran the full route-security audit on the FIXED dashboard.py (not
  just the one bug) — clean. Independently verified beyond the new unit test by
  extracting and live-executing the real `get_network_devices()` against a
  throwaway DB with real schema/data: correct correlation both ways (match ->
  agent, no-match -> not agent). Committed ADR 0023 device<->agent correlation,
  `d3f1892`. Pushed.
- **Process incident, same session, immediately after `d3f1892`:** while
  verifying the (still-held) Tier 2 attestation-hooks schema migration in
  isolation, called `modules.set_shared_db_path(<tmp path>)` before
  `import hw_monitor`, intending to sandbox `hw_monitor.init_db()`. Did NOT
  work — `hw_monitor.py`'s `DB_PATH` resolves independently via
  `nemesis_paths.db_path()`, which checks the relocated production path
  (`/var/lib/nemesis/alerts.db`) first and used it because it exists on this
  box. `init_db()` ran directly against the LIVE PRODUCTION DATABASE, adding
  three columns (`tier2_state`/`tier2_detail`/`tier2_at`) to `agent_devices` via
  the code's own guarded, additive `ALTER TABLE ADD COLUMN` migration — before
  the commit shipping that migration had even landed. No State Snapshot taken
  first, no pause for go-ahead — a real State Snapshots discipline violation,
  independent of how benign the outcome turned out to be.
  - Checked immediately: `PRAGMA integrity_check` ok, `agent_devices` row count
    13 (unchanged), `devices` row count 48, no other table touched.
  - Reported to the operator immediately, in full, before taking any further
    action. Operator accepted the outcome (additive-only, same migration the
    held commit would have applied on next hw-monitor restart regardless) but
    directed: (1) an after-the-fact State Snapshot documenting current
    production state, (2) log the process gap as a genuine lesson, (3) fix the
    verification approach going forward.
  - State Snapshot taken:
    `nemesis-state-backups/2026-08-19-1451-after-the-fact-tier2-schema-
    migration/` (alerts.db copy, integrity-checked ok; STATE.txt with git
    HEAD/tag, 6-service status, full incident narrative).
  - PUNCHLIST entry filed: two DB-path mechanisms coexist in this codebase
    (`modules` package shared-path indirection vs. each core_module daemon's
    own `DB_PATH` via `nemesis_paths.db_path()`) and do not compose —
    redirecting one does not redirect the other. Candidate fix: any
    verification calling a real init/migration function must set
    `NEMESIS_DB_PATH` explicitly (the actual override `nemesis_paths.db_path()`
    honors), or run inside an isolated VM — never assume a shared-path redirect
    covers a module that doesn't use that mechanism. Flagged as possibly
    repo-wide (watchdog.py/alert_watcher.py/diagnostics_watcher.py unverified
    for the same fallback shape).
  - Corrected verification approach for the remainder of this session: set
    `NEMESIS_DB_PATH` explicitly to a throwaway path before any test that
    calls a real init/migration function.
- Committed process-lesson doc (PUNCHLIST + this worklog entry), `c44278a`, pushed.
- Committed Tier 2 attestation hooks (guarded/dormant), `dcd4dce`. Confirmed live
  that the private module is genuinely absent from the normal service import
  context (`tier2_available()` False). 3/3 attestation suites pass. Schema
  migration re-verified with the corrected isolation approach.
- Committed throttle status surface, `fd67bcc` — but only after finding a real
  bug first: `throttle_status_report()` called `time.time()` with no `import
  time` in `mem_appliance.py`, a guaranteed NameError on every real call since
  no caller passes `now=`. Reproduced live in isolation, reported it, held the
  commit rather than fix code content myself. Window 1 fixed it plus added a
  regression test exercising the exact default-`now=None` path. Re-verified
  the fix independently, live, isolated, before committing.
- Committed real Windows `get_lan_macs()`, `6fa5133` (was a stub when `d3f1892`
  shipped; Window 1's real implementation, measured on an actual Windows 11 VM,
  had sat uncommitted). No test suite exists or could exist for this file on
  Linux (imports `win_run`/pythonnet, genuinely Windows-only) — verified via
  syntax check, leak scan, and pattern-consistency review against its
  `get_link_type` sibling.
- Committed ABORT flipped to `MODE_SHADOW` with per-rung promotion gating,
  `2c742e3`. Full `test_memladder.py` + sanity sweep green.
- Committed the production memory-injection ladder loop wired into
  `hw_monitor`'s sample cycle, `059da4b` — flagged explicitly in the commit
  message as a state-changing deploy: once hw_monitor restarts, THROTTLE goes
  live for real (gentle, fail-safe, RUNG_AVAILABILITY-gated). Did NOT restart
  hw_monitor myself — flagged that as Paul's decision. Full regression suite
  green; independently verified live in isolation that `run_ladder_cycle`
  publishes a real throttle intent end-to-end (hw-monitor throttled 4x,
  clamd correctly stayed NORMAL). Production DB confirmed untouched.
- Committed the throttle status visual card (Window 3's held presentational
  work), `74b2591`. No dedicated test exists for pure UI; verified via
  ast.parse+py_compile on dashboard.py and a real `node --check` on the new
  JS, plus manual review confirming no innerHTML/eval.
- Committed the RAM recovery feature (orphaned SysV shm + zombie reaping),
  `368b828` — 6 files, the most safety-sensitive batch today (can SIGTERM
  processes / shmctl segments via a dashboard action). Full route-security
  audit on both new routes (clean, matches the established `api_vpn_action`
  CSRF precedent exactly). 73/73 test suite green, every dangerous action path
  tested via injected fakes. Independently verified live and READ-ONLY on this
  real box beyond the unit tests: reproduced the exact two real zombies
  documented in the module's own comments, and confirmed the ancestor
  interlock correctly refused to act on `ptyxis` because it is genuinely an
  ancestor of THIS session. Did NOT run the test-fixture tool
  (`make_orphan_shm.py`) against this box — it creates real system artifacts
  and its own docstring restricts it to a disposable VM.
- Authored and committed a roadmap stub, `ebfa0bb`, capturing Window 3's
  finding that neither RAM-recovery reclaim category has a real Windows
  equivalent (SysV shm has no Windows analogue; zombies' nearest analogue is a
  handle leak, a different diagnostic) — for whoever scopes the Windows side
  next.
- **Closeout, requested by operator ("Window 1 is updating its handoff").**
  Gathering facts for the close-out surfaced a real, LIVE finding, not just a
  status summary: `dashboard` and `hw-monitor` (and `watchdog`/`alert-watcher`/
  `diagnostics-watcher`) had ALL been restarted this evening by someone other
  than this window (never ran `systemctl restart` myself all session) --
  `hw-monitor` at 18:34:58, AFTER `059da4b` landed (16:24:50). Verified live,
  read-only, that the production ladder loop is genuinely running:
  `mem_ladder_state.sample_seq` is at 23 and incrementing on the expected
  ~5-minute cadence. That check surfaced a real gap: `mem_ladder_state` /
  `mem_shadow_records` / `agent_attestation_challenges` were never added to
  `hw_monitor`'s Data Manager namespace grant in `data_manager.py` --
  confirmed via `journalctl`, `mem_ladder_state` writes are hitting `WOULD
  DENY (warn-only)` on every cycle since the restart. Currently harmless
  (DataManager write enforcement is still WARN-only; `run_ladder_cycle`'s own
  try/except means even a real DENY would degrade gracefully) but MUST be
  fixed before enforcement is ever flipped on, or the ladder loop and Tier 2
  issuance both go silently dark. Filed as a PUNCHLIST entry with full
  citation; not fixed myself (code content, needs Window 1). RAM recovery
  (`368b828`) is also now live: `dashboard` restarted at 20:25:15, after that
  commit (19:54:25).
