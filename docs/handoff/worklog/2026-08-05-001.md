# Worklog — 2026-08-05 (Window 2)

> Raw chronological log. Reconstructed from `git log` at closeout — no live worklog was
> appended during the session (same discipline gap as 2026-08-04, not yet resumed; see
> HANDOFF.md §6). Curated narrative: `docs/handoff/supplements/2026-08-05-001.md`.

- **08:28:35** `41ba66f` — dual-stack `_detect_connection_type()`, step 4/5 of the
  observation-layer foundation (Window 1's work, committed by Window 2). 14/14 new checks
  + mutation control, full agent suite 621 = 607 baseline + 14, no regressions. Verified
  scope: exactly 3 intended files staged.
- **08:42:50** `4eb6be2` — PUNCHLIST closure for d1c23d7 against `41ba66f`. Isolated from a
  concurrent unrelated PUNCHLIST addition in the same file.
- **08:43:58** `d636f2e` — `scan_tasks` added to `hw_monitor`'s namespace grant. Root cause:
  drift (table created after the grant list was derived), not transcription. Controls
  confirmed the guard remained real (ungranted table still refused, granted sibling still
  passed). Deploy (hw-monitor restart) explicitly held for snapshot + go-ahead.
- **08:44:46** `a835d53` — new PUNCHLIST entry: `test_quarantine.py` RED 8 days (routes
  hardened to POST 2026-07-28, test still issues GET). Collateral discovery during the
  `scan_tasks` audit, confirmed unrelated.
- Push coordination check: 3 commits pending (`4eb6be2`, `d636f2e`, `a835d53`). Operator
  chose normal push of all three (over a targeted partial-ref push). Pushed, HEAD confirmed
  matching origin.
- **08:54:19** `dd32ccb` — chat-widget duplicate-`id` collision fixed (render-once/
  relocate-everywhere) + chat effort=medium for the chat path (model-gated allowlist).
  Six files, staged in dependency order (ai_engine first). Independently re-rendered
  `get_chat_js()` and ran `node --check` before staging.
- **Mid-session correction**: an earlier splice-isolation step (staging one PUNCHLIST entry
  apart from concurrent WIP) had used a stale pre-edit snapshot for the working-tree
  restore, silently dropping the just-added entry from disk (commit itself was unaffected).
  Caught via a targeted grep, working tree repaired before any further action.
- **09:30:47** `a5c7137` — the ACTUAL root cause of "chat still broken" after `dd32ccb`:
  three literal newlines in a JS string literal produced a silent `SyntaxError`, killing
  the whole chat IIFE. Verified via isolated `get_chat_js()` re-render + `node --check`.
- **09:40:27** `940b1a6` — `sqlite3.Row` missing on `_anchor_load_alert()`'s connections.
- **10:00:21** `746d522` — copyable chat answers + fenced code blocks. Push held per
  operator instruction (Window 1's degraded-ingest WIP on disk, no restart until resolved).
- **10:16:03** `fa52626` — PUNCHLIST backlog entry: unpin chat into a resizable popup
  (feature request). Repeated the splice-isolation bug from earlier and self-caught it
  again the same way — rebuilt the restore target from the current working file, not a
  stale copy.
- **10:24:41** `254c9a9` — degraded-journal → `audit_log` ingest (`alert_manager/
  degraded_ingest.py`, new + `dashboard.py` poller wiring). 25/25 checks independently
  re-run. Already deployed and verified in production by Window 1 before this commit —
  this landed the repo to match already-running code.
- Push coordination: `746d522` + `fa52626` pushed together (operator: "no deploy
  coordination concern here — that hold was specific to Window 1's restart timing").
  Then `254c9a9` pushed alone.
- **10:33:51** `51942a8` — chat system prompt: two standards (facts-about-this-network vs.
  general security knowledge), read-only investigation permitted at L0_OBSERVE, hard
  "ONLY EVER SUGGEST COMMANDS THAT READ STATE" rule, dynamic diagnostics-check enumeration
  (verified both the populated and the graceful-degradation-to-`""` paths live).
- **11:00:05** `5386879` — same off-by-one class as the earlier `analyze_alert` display
  fix, found independently: two more indices in `dashboard.py`, plus
  `_anchor_load_incident()`'s device-list formatter crashing on dict-shaped
  `devices_json` and silently reporting "unreadable" for 153/153 incidents.
- **11:02:23** `71c5352` — PUNCHLIST entries for both. **Rule-8 catch**: the draft entry
  quoted a real device name + LAN IP as its worked example; sanitized to placeholders
  before commit.
- **11:18:13** `2129331` — Enter-to-submit on chat input (Shift+Enter still newlines),
  gated on the Ask button's own disabled state, isComposing-checked for IME.
- **11:22:00** `b478818` — PUNCHLIST entry for the same, filed and closed together.
- **12:08:15** `9521346` — the `analyze_alert` GATE's off-by-one (not just the display
  reads) corrected, after operator cost approval (~$0.08–0.16 one-time, measured).
  `community_queue`'s skipped-count now surfaces in the UI (backend had returned it since
  `d7851df`, UI dropped it).
- **12:09:34** `7db17a5` — closed 4 PUNCHLIST entries found already-fixed during an
  AI-item survey (one had been fixed a full day earlier and was about to be re-queued as
  "highest-value next work"); filed a PUNCHLIST-drift meta-entry on the habit change.
- **12:29:53** `6358b5d` — per-row Analyze link on `/firewall-db`, deep-linking to the
  main page's existing `viewAlert()` rather than a second renderer. Independently
  confirmed the deep-link handler already existed and already called `viewAlert()`.
- **12:31:19** `6424fe9` — PUNCHLIST entry: `/api/analyze/<rule_id>` is a GET route that
  spends money (pre-existing shape, now consequential since the gate works). Independently
  confirmed no `methods=` on the route decorator and absence from `_AUTH_EXEMPT`.
- **12:57:11** `8f227a4` — empty deep-linked alert body fix + `test_analyze_alert_body.py`
  (new, 17/17). Root cause: the deep-link entry point has ALWAYS passed `raw=""`; once the
  gate worked, this billed a call that answered "no data" and poisoned `alerts.explanation`
  + `risk_level` (measured live: CRITICAL→LOW on rule 1000002, seen 108 times).
- **13:48:46** `f8c116f` — fenced-JSON parse fix + `COALESCE` write-back (17→29 checks,
  29/29). Root cause: a correct MEDIUM reply wrapped in a ` ```json ` fence broke
  `json.loads()`, hardcoded fallback wrote UNKNOWN over the stored CRITICAL. Independently
  re-verified the regex against a realistic multi-line fenced reply beyond the unit
  fixtures.
- **14:39:16** `31a9bbf` — local-time offset-stamp fix (was UTC) + ADR 0019 Phase 2
  enforcement-visibility panel, landed together deliberately (panel structurally depends
  on the fix). Self-caught bug during the panel's own build: a future-dated heartbeat
  (exactly what the pre-fix UTC stamp produced) read as HEALTHY with no lower age bound.
  27/27 + new 21/21 checks, both independently re-run; independently re-rendered the panel
  HTML against a synthetic DB.
- Push coordination: all 3 pending commits confirmed and pushed together.
- **Verification gap surfaced**: operator referenced a commit hash (`c5f7a1c`) as "already
  committed" for a pseudonymization PUNCHLIST entry that did not, in fact, exist anywhere
  in this repo. Traced: `c5f7a1c` is a commit in the PRIVATE mirror
  (`~/work/nemesis-internal`) — Window 1's handoff document, not a public PUNCHLIST entry.
- **15:09:07** `dde55fc` — the pseudonymization decision (sourced from the private
  handoff, re-verified against live code first) and the Layer D declared-but-absent
  finding, both filed to the public PUNCHLIST for the first time this session.
- **15:09:23** `cb00d4a` — `/diagnostics` disclosure fix: redaction scope explicitly does
  not cover network addresses; AI analysis sends them externally (interim, pre-
  pseudonymization-build disclosure).
- **15:54:00** `f743b9a` — pseudonymization BUILT same day: new module
  `alert_manager/nemesis_pseudonymize.py`, wired into `analyze_alert()`. 51/51 tests
  independently re-run, plus this window's own adversarial probes beyond the suite
  (comma-lists, CIDR suffixes, IPv4-mapped-IPv6+port, invalid octet) — all clean.
- **15:54:20** `f30126c` — PUNCHLIST: closed the pseudonymization entry against the
  shipped build, filed a NEW separate finding (`enrich_ip()` still sends the real source
  IP to AbuseIPDB/ipinfo.io, unaffected by pseudonymization — independently confirmed
  against the code), and landed the long-parked `firewall-db` backlog idea after explicit
  operator confirmation to include it this time.
- Push coordination: both commits confirmed and pushed together. `local == origin/main`
  at `f30126c` — last push of the session.
- **Closeout** (this pass): confirmed production restarted at 18:57:04 (not by Window 2),
  picked up all 26 commits (service runs directly against `/opt/nemesis`, no separate
  deploy copy), zero errors in the journal since restart. Live-verified: local-time
  heartbeat stamp, zero `WOULD DENY` lines, alert 1000002's repaired MEDIUM state,
  `test_quarantine.py`'s consistent 38-pass/14-fail (pre-existing, unrelated). Two
  untracked roadmap stubs and one uncommitted QUIC/nftables PUNCHLIST addition noted as
  other windows' work, left untouched.
