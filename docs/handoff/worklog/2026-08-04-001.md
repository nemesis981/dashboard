# Worklog — 2026-08-04 (Window 2)

> Raw chronological log, reconstructed from `git log` at closeout — a live append-as-you-go
> worklog was not maintained during this session (gap, noted for future sessions). Commit
> hashes/timestamps are authoritative; annotations are terse and factual, not curated. See
> `supplements/2026-08-04-001.md` for the curated narrative.

08:53 `8d0fe78` docs(audits): roadmap-state audit refresh -- 08-04 baseline
07:04 `77abe9c` docs(roadmap): correct two stale headers flagged by the 08-04 audit
07:21 `9cb444b` docs(punchlist): install-retry disown gap -- reviewed, already fixed (edc6133)
07:21 `353ce11` docs(punchlist): uninstall leaves agent process + state behind
07:21 `35af42b` docs(punchlist): revoked device has no way to know it was revoked
07:22 `8ae6b60` docs(punchlist): provenance should be recorded incrementally at install time
07:23 `37a7e8c` docs(punchlist): archive files written with inconsistent ownership
07:24 `86ba3a1` docs(punchlist): IPv6 keytest false-DEGRADED -- moved in from docs/audits/
07:35 `67326d0` feat(agent,server): retire loopback scan/notify push, add task-poll cadence hint (ADR 0004 Stage 1 steps 5-6)
07:38 `8fcbebf` docs(punchlist): scan_queue row strands at 'executing' on a failed enqueue
07:39 `cd624d9` docs(punchlist): correct the 468/468 test-suite baseline to 465/498
07:57 `1db1cf2` feat(server): redeliver stuck dispatched tasks past their envelope (ADR 0004 Stage 1)
08:15 `04c4914` fix(auth): idle-lock overlay shows the same health detail unlock.html does
09:34 `3548e40` fix(ai): cost display was understating spend up to ~13x
10:00 `e2067c0` feat(auth,server): mandatory scan on trust-boundary reinstatement + enrollment integrity fixes
10:00 `707ecdb` test(quarantine): fix blocked_cache signature drift -- alert_watcher no longer takes it
10:02 `746ae44` docs(punchlist): scan_conditions seed only backfills empty tables
10:02 `0795d98` docs(punchlist): SECURITY -- agent-side integrity attestation gap
10:12 `29a9ca5` fix(auth): no-store on authenticated pages -- back-forward cache was replaying a typed password
10:26 `69ade29` test(quarantine): label synthetic alerts for Rule 11 cleanup
10:27 `82d4def` docs(punchlist): parse_alert stores Classification block as rule_name
10:36 `40cef9d` docs(adr-0009): retire Fork B Pieces 1+4, update Status line not just piece notes
10:37 `d7851df` feat(ai): monthly cost average + rate-age display, community_queue job_id dedup
10:49 `9945893` docs(punchlist): _hour_of_week() misnamed since 06-20, weekday/weekend baseline deferred
10:58 `4488584` feat(ai): per-model pricing rates, hard prerequisite for model selection
11:18 `845ac8b` feat(ai): monthly dollar spend cap, subscription-price comparison
12:09 `ac824d4` feat(ai): pricing change-detection -- detect and notify, never auto-write
12:13 `53bf7ed` fix(alerts): parse_alert() stored the Classification block as rule_name
12:16 `927f8b3` docs(punchlist): parse_alert fix landed -- flag historical-row backfill for the operator
12:55 `91b4003` feat(auth): wire dm_operation_log.actor -- attribution was unwired since ADR 0006 shipped
13:07 `6df1b6f` fix(scan-conditions): backfill missing default condition types, not just empty tables
13:25 `ba499bb` feat(ai): settings UI for the monthly spend cap
13:35 `b283ef3` feat(ai): pricing-drift scheduler, notification, and dashboard banner
14:56 `9f0c217` feat(ai): graduated-authority foundations -- ceiling tables + effective_ceiling()
14:57 `9c47099` docs(roadmap): revise interactive-ai-clarification's explanation-only constraint (D1)
14:57 `fe4b66e` docs(punchlist): malware quarantine has no restore/undo path
15:10 `a0642cb` feat(ai): contextual chat backend -- anchored follow-up questions
15:28 `1246a31` feat(ai): wire contextual chat to four real surfaces, fix two bugs found before shipping
15:38 `a3dcf54` feat(install): bound /tmp and give ClamAV explicit disk-backed scan staging
15:50 `90cbd9f` feat(integrity): server-side cross-check of agent-reported scan activity — includes stored-XSS fix in get_dashboard_card() (device_id/signal/detail were unescaped %s interpolation), found in review before commit, patched with html.escape(quote=True), new test_integrity_watch.py added
16:02 `141c676` docs(roadmap): QUIC/HTTP-3 gap and resolution, Piece J status correction
16:03 `7f370f0` docs(roadmap): four new dependencies for Tier 3's local-trigger list
16:03 `ce22539` docs(roadmap): split memory-injection status, add prerequisites and open ownership question
16:05 `ddbfc90` docs(roadmap): activate the observation-layer foundation (parked -> active)
16:06 `32dcc7d` docs(roadmap): new scope -- UDP policy (default-deny, Game Mode, hosting, visibility) [new file]
16:06 `6334757` docs(punchlist): agent's connection reporting excludes all UDP
16:07 `fa4a977` docs(punchlist): agent's process listing is a top-10-by-CPU sample, not enumeration
16:07 `d1c23d7` docs(punchlist): agent's local/remote connection classification is IPv4-only
16:11 `94a37df` docs(enrollment): document why the TOFU fingerprint match must stay log-only (24-line comment, 0 deletions)
--- operator resolves Tier 3 vs memory-injection ownership directly (Window 3's ADR 0009 §3 reasoning); recorded in both docs, no Rule 10 review needed (closes an already-public question) ---
16:24 `7b45bfd` feat(hw_monitor): Tier 1 agent attestation schema + heartbeat integration — isolated from unrelated concurrent Window 1 WIP (enqueue_manifest/ensure_manifest_queued/attest_manifest dispatch branch) that had crept into hw_monitor.py; migration verified against real live schema in throwaway DB; 25-suite regression re-run, zero failures
16:26 `12d58fe` feat(nemesis_agent): Tier 1 client-side attestation scaffold — 16/16 checks, 3/3 mutants caught, independently re-verified
16:27 `9004bb4` feat(alert_manager): Tier 1 agent attestation, server half — 21/21 checks, round-trip interop verified (server signs via build_manifest_envelope, agent's own real tasks.verify_task accepts; device-binding negative rejects)
16:38 `4133dd9` feat(agent,hw_monitor): Tier 1 attestation dispatcher wiring + self-sustaining queue — restored the previously-isolated-out manifest queue pieces (now with full rationale comments), 22/22 e2e checks
16:47 `f35ad51` feat(nemesis_agent): event-triggered check-in (request_early_beat) — deliberately callerless (confirmed zero call sites), 12/12 checks, storm-tested at 50 concurrent requests
16:52 `71f653d` docs(roadmap): resolve Tier 3 vs memory-injection ownership question (P1 of Window 3's priority batch)
16:54 `110239f` fix(ai_engine): bump _ACTIVE_MODEL claude-sonnet-4-6 -> claude-sonnet-5 (isolated via hand-extracted single hunk for a clean one-line revert path)
16:55 `88c63b6` feat(ai_engine): shared contextual chat widget, model tiers, real spend summing — Rule-8 fix applied during review (a comment naming the product owner directly was reworded to "the product requirement"), full test_chat.py re-run after fix
17:04 `ce91f1e` feat(malware_detection,dashboard): wire Layer C AI verdict, adopt shared chat widget — new test_layer_c.py, reachability-through-real-scan_file() proven, not just the function in isolation
17:36 `d151dc3` fix(ai_engine): stop indexing content[0] for the model's answer -- select by block type — model-drift regression (claude-opus-5 defaults thinking ON, unlike 4.7/4.8), confirmed the ONLY messages.create() call site and ONLY .content[0] indexing site in the whole tree via grep
--- Window 3 status-check flagged an apparent commit-9004bb4 discrepancy; re-verified independently (fetch + ls-tree + remote -v), confirmed correct on Window 2's side, Window 3's read was a stale local clone ---
--- process note from operator: run `git show --stat` against a split commit's expected file list BEFORE pushing, not after (saved as standing feedback memory) ---
17:50 `524646b` feat(database): core settings table + agent_remote_observe_every_n — verified dynamically against throwaway DB (default fallback, roundtrip, clamping of 0/out-of-range/garbage to DEFAULT not nearest bound, boundary values)
17:52 `3fe91c3` feat(agent,hw_monitor): observation layer -- process enumeration + UDP attribution — landed as ONE commit (agent.py + hw_monitor.py + test_observation.py + docs/OPERATION.md) rather than split by file, since the test drives both halves together; full suite passed including real host measurement (598 processes, 399 exe()-denied; 33 UDP sockets, ~25 attributable)
17:53 `1633d29` feat(dashboard): Settings UI for the remote-observation cadence — route-level security audit applied (POST-only, absent from _AUTH_EXEMPT, parameterized SQL); confirmed scope was NOT mixed with other windows' work as initially speculated
17:54 `5330220` feat(anomaly_detection,community_queue): adopt shared chat widget, add cross-engine context — completes the four-surface chat widget rollout (alert modal + malware findings in ce91f1e, these two here)
--- all four pushed; HEAD confirmed == origin/main == 5330220 ---
18:0x (untracked by Window 2) -- production restarted, snapshots taken first: 1647-pre-attestation-migration, 1716-pre-dashboard-ai-restart, 1756-pre-ai-fix-restart, 1806-pre-integrity-watch-enable. Live schema confirmed post-restart: attestation_state column present, settings table present. dashboard.service ActiveEnterTimestamp 18:06:51, after all commits above -- production is caught up to 5330220.
