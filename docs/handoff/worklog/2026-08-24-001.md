# Worklog — 2026-08-24-001 (Window 2)

> ⚠ Reconstructed at closeout, not appended live through the session — a real process gap
> against Rule 9's "append the moment each discrete step completes" discipline. Chronology
> below is accurate to the conversation record but was written in one pass at the end of the
> day, not as it happened. Flagging this rather than presenting it as a true live log.

- Morning Status run. Baseline `roadmap-state-audit-2026-08-22.md` (10/12/59, 81 total).
  File-set +2 (two new PARKED stubs, 08-23 gap-scan, accounted for). Shipping drift found:
  `dashboard-roles-access-control.md` PARKED→SHIPPED (RBAC foundation shipped 08-22 evening,
  after that morning's baseline was cut) — flagged for closeout refresh. Elevated grants
  re-checked live against 08-22 HANDOFF §9 baseline: no drift. Briefing written to
  `docs/briefing/2026-08-24.md`, mirrored.
- Window 1 (nemesis-9d) asked which window this session is + sequencing read on ADR 0026
  §6 step 2 (D3 admin key-pair / inner-envelope with tasks.py). Confirmed Window 2,
  confirmed their sequencing read via independent grep (`inner_envelope`: 0 hits).
- Operator: correct ADR 0026 §6 step 2's stale `keyprotect` clause (private key lives on
  phone, appliance never holds it — satisfied-by-not-applying, not outstanding). Fixed,
  committed `950e0d3`, pushed.
- Operator: new standing practice — "limitation is a last resort, not the first landing
  spot" — saved to memory.
- Window 3 (nemesis-7d) flagged the same `attempts`-counts-passes-not-tries ADR wording
  issue independently; already fixed by the time their message landed. Fixed both D4 prose
  and the `user_capability_unlocks` schema comment. Committed `31c76e1`, pushed.
- Operator: add a fourth standing CLAUDE.md practice — "does a new branch/default actually
  have a test that exercises it, not just one that could" — per Window 3's cross-window
  observation (capabilities._conn(), role.js missing sub_admin, unwired unlock gate, a
  same-day test_capabilities.py near-miss). Written, committed `af4183f`.
- Window 3's RBAC training-UI batch (8 paths per their handoff, actually 10 — miscounted
  in their prose, caught by diffing against `git status` before staging). Verified live from
  `/tmp` against fresh `origin/main` worktrees at each step. Split into 3 dependency-ordered
  commits: `d46a651` (role.js RANK map fix + test_roles reconciliation), `08fbef1`
  (capabilities._conn() bug fix — called a nonexistent function, zero test coverage on the
  default path), `e00e158` (the training UI itself, roles.py capability-gate fix). Pushed
  after listing + confirming.
- Two documents from Window 3 landed: ADR 0028 (email security gateway) to the public repo
  (`ac778e5`), build spec to the private repo (`a9303ff`, pushed local+usb). Rule 10
  disclosure already resolved in-document by the operator.
- Window 1's four queued handoffs (commit 1 default-deny dispatch, commit 2b admin-approval,
  meminject sweep, Stage 5 approve_enrollment): worked through in dependency order using
  isolated `git worktree add --detach` builds. Caught a real stale-claim bug before it
  shipped — commit-1's handoff said `tasks.py` was "+147, all mine, clean" but the live file
  was actually +507 lines merging commit-1 AND commit-2b's admin-approval code together;
  copying it whole would have crashed `test_task_envelope.py`'s self-test
  (`ModuleNotFoundError: admin_approval`). Flagged to Window 1, who split it into two proper
  patches and fixed the root handoff. Independently re-verified the fix (sha256 chain-check,
  all 7 suites re-run) before trusting it a second time.
- Committed all four in a chain: `c93e08c` (Stage 5), `e6b6ccb` (commit 1), `dc06ef8`
  (commit 2b), `029b8e4` (meminject). **Process failure**: built each in a detached-HEAD
  worktree and removed the worktree after committing without ever creating a branch ref —
  all four became unreachable dangling objects, invisible to `git log`/`push`, one `git gc`
  away from being destroyed. Caught by Window 1's independent check, not my own process.
  Fixed with `git branch stage5-2b-meminject 029b8e4`, re-verified reachable, pushed. Saved
  as a standing process memory (branch before `git worktree remove`, every time).
- Brought `/opt/nemesis`'s shared checkout up to date to `029b8e4`. Not a clean
  fast-forward as assumed: 2 tracked files (`agent.py`/`agent_errors.py`, Window 1's held
  GUI-findings buffer) and 1 untracked file (Window 3's quiz wording revision) would have
  been silently overwritten by a plain pull. Used `git stash push -- <11 files>` → `pull
  --ff-only` → `stash pop` (clean auto-merge, verified no conflict markers, py_compile
  clean, both bodies of code confirmed present by grep), plus a manual backup/restore for
  the untracked quiz revision (confirmed digest-safe: id/prompt/options/correct unchanged
  from origin, so no earned unlocks invalidated). `PUNCHLIST.md` and an unrelated ADR 0028
  edit were never touched throughout.
- Stage 0 step 1 (session realms, X-Forwarded-Proto handling, nemesis-fwd interface-scoped
  deny/allow op): reviewed and verified independently (session_realm.py read in full,
  dashboard.py's realm gate read line by line, zero new `@app.route` lines confirmed by
  grep). One contradiction surfaced and resolved: the operator's task description said
  Window 1 flagged `NEMESIS_DOOR_SECRET` as needing to be added to `ENV_WRITE_ALLOWED_KEYS`;
  Window 1's own message said the opposite (deliberately excluded, real security reasoning —
  a compromised dashboard must not be able to mint its own door secret). Verified the live
  code matched Window 1's stated intent (0 occurrences), did not add the key, flagged the
  contradiction rather than guessing. Window 1 later traced it to an offhand early remark
  superseded by their own later analysis but never retracted. Committed `462d664`, pushed.
- Follow-up batch: a live production gap surfaced by Window 1 — `scripts/nemesis-cert-renew`
  + its systemd timer were installed and running on the real box (timer active, ~8h to next
  fire) but existed in no repository. Verified installed binary was byte-identical to the
  untracked working-tree source before committing. Split into 4 logical commits: `f03c7e2`
  (cert-renew, the urgent one), `aa84d12` (core/rp_identity.py, WebAuthn RP identity, unwired
  as expected), `604ff19` (local_port_watch.py, visibility-only, unwired as expected),
  `a5826f0` (a small self-caught test fix — `test_capability_stage5.py` had asserted a
  transient review-status string as an invariant). All live-verified from `/tmp`, pushed.
- Window 3's quiz revision to `approve_enrollment.json` flagged as newly vulnerable to being
  clobbered after the pull completed. Re-verified it was the reviewed version (not the
  earlier placeholder) and digest-safe, committed alone (`33e3cfd`), pushed immediately —
  no window for it to be lost.
- Closeout: roadmap baseline refreshed (`docs/audits/roadmap-state-audit-2026-08-24.md`,
  tally 11/12/60, 83 total — the `dashboard-roles-access-control.md` reclassification from
  this morning's audit formalized). HANDOFF.md, this worklog, and the closeout supplement
  written and mirrored to `~/work/nemesis-internal/handoff/`.
