## 2026-08-28-001 (Window 2) — live worklog

*Added at closeout, not rewritten history: `d95527b` (cited below as live/current) was later
renamed to `8cec3eb` by the same day's tailnet-leak history rewrite (see worklog-003 and
`~/work/nemesis-internal/handoff/2026-08-28-window2-handoff.md` §1). This entry is left as
written — it was accurate at the time — this note exists so a reader doesn't chase a hash that
no longer exists on `origin/main`.*

- Morning Status run. LOC 166,921. Services all active. Tree at session start: `M CLAUDE.md`
  (uncommitted, unattributed `--passwordfile` VBoxManage rule), `?? alert_manager/hw_map.json`
  (undecided track-or-gitignore, carried forward). Roadmap baseline
  `docs/audits/roadmap-state-audit-2026-08-24.md` (11/12/60/83) diffed against live state:
  +1 file (`child-safety-monitoring.md`, added `8bbdd31`, correctly PARKED, no
  reclassification), no shipping drift found on spot-checked candidates
  (`connection-type-awareness.md` already SHIPPED, `server-side-session-store.md` and
  `connection-health-subsystem.md` confirmed still correctly not-shipped). Resolved tally:
  11 SHIPPED / 12 PARTIAL / 61 STUB-PARKED / 84 total. Elevated grants: `sudo -n -l` read
  live, ~65 NOPASSWD entries all Nemesis-install-scoped, nothing new; groups unchanged;
  polkit rules unreadable this session (permission denied, as before). Gateway-VM `nmap`
  grant (flagged 2026-08-26) still open, unresolved, now flagged a third session. Briefing
  written to `docs/briefing/2026-08-28.md` and mirrored to
  `~/work/nemesis-internal/briefing/2026-08-28.md`.

- Operator gave a new standing rule to add to CLAUDE.md: **Fleet Archive Policy**
  (non-active/template VMs move to the Seagate USB via `VBoxManage movevm`, disposable NVMe
  clones on demand, accepted HDD-seek-time and mixed-use-with-backups risks, plus a tracking
  requirement since `nemesis-fleet-review` only sees registered VMs). Attributed
  operator-directed, 2026-08-28.

  Before editing: CLAUDE.md already carried an **unattributed, uncommitted** diff (the
  `--passwordfile` rule, flagged in this morning's briefing and the prior two closeouts,
  still unclaimed). To avoid silently laundering that diff into this new attributed commit,
  saved it to a patch file (`git diff CLAUDE.md > .../pending-passwordfile.patch`), reset
  CLAUDE.md to HEAD, made the Fleet Archive Policy edit on the clean baseline, committed it
  alone, then reapplied the saved patch and byte-for-byte diff-verified the working tree
  matches its pre-edit state exactly. The passwordfile diff is untouched, still uncommitted,
  still unattributed — not resolved by this session, deliberately.

  Committed `d95527b` (public repo, CLAUDE.md only, 29 insertions) — **held, not pushed**,
  pending operator confirmation (standing push-confirmation rule, no exceptions).

- Created the manifest the policy calls for: `vm-fleet/archive-manifest.md` — placed in the
  **private mirror** (`~/work/nemesis-internal/vm-fleet/`), not the public
  `docs/vm-fleet/...` path the operator's message used as an example, matching the
  established pattern that every other fleet-tracking doc (`VM-FLEET-LOG.md`,
  `fleet-inventory-*.md`) lives there, not in the public repo. Flagged this deviation to the
  operator explicitly rather than silently picking a location.

  Staged and committed by exact path only — `~/work/nemesis-internal` had substantial
  unrelated uncommitted work from other windows at the time (`.gitignore`,
  `handoff/HANDOFF.md`, two scoping docs, staged-host-defence files, plus untracked
  `PUNCHLIST.md`/`VPS/`/several audits and briefings/`appliance-self-scan-scope-...md`) —
  none of it was touched or swept in; `git status --short` confirmed still present,
  unstaged, after the commit. Committed `7eb5b43` (1 file, 61 insertions) — verified both
  `local` and `usb` remotes agree at the same prior tip (`554caca`) before committing, so the
  one new commit has no risk of carrying anyone else's work along as an ancestor. **Held, not
  pushed** — same standing rule, plus flagged to the operator that Window 2 committing to a
  private-module repo is meant to be a *backup* role when Window 1 is occupied, and this
  session has no actual confirmation Window 1 is occupied right now (only circumstantial
  evidence from the other uncommitted files). Asked the operator to weigh in on both open
  questions (who should land this commit, and whether the manifest's public-vs-private
  placement was actually wanted the way the operator's example path implied) before pushing
  either commit.

- **Operator announced a reboot mid-turn**, before answering any of the above. Nothing
  pending is destructive or half-finished: both commits exist locally only, tree is
  otherwise as described above, no uncommitted CLAUDE.md content beyond the pre-existing
  unattributed passwordfile diff (verified byte-identical to its pre-session state). Nothing
  else to protect against a session interruption right now — logging this so a fresh session
  (or the same one resuming) has the state without re-deriving it.

**Open, waiting on the operator when the session resumes:**
1. Push `d95527b` (public repo) — awaiting go-ahead.
2. Push `7eb5b43` (private repo, both `local`+`usb`) — awaiting go-ahead AND a decision on
   whether Window 2-as-backup was the right call here vs. handing to Window 1.
3. Confirm (or correct) the private-mirror placement of `archive-manifest.md` vs. the
   public `docs/vm-fleet/...` path used in the operator's original message.
4. CLAUDE.md's unattributed `--passwordfile` diff — still needs its author to claim it
   (third session flagged, unresolved).
