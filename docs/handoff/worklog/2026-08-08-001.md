# Worklog — 2026-08-08-001 (Window 2)

Raw chronological log, flight-recorder style. Curated version (if warranted) goes in the
matching supplement; this file is the unfiltered record.

- Morning Status run: 79,308 LOC, HEAD=`a643de1`=origin/main, tree clean, all 6 core services
  active. Roadmap-vs-state audit baseline `roadmap-state-audit-2026-08-07.md` (8/11/57, 76
  total) re-checked live against the 3 commits landed since — no drift. Briefing written to
  `docs/briefing/2026-08-08.md`, mirrored to `~/work/nemesis-internal/briefing/`.
- Reviewed Window 1's 6 held files for Track C step 5 (seen-set core): `conn_seen.py` +
  `test_conn_seen.py` (new), `database.py` + `data_manager.py` + `hw_monitor.py` +
  `test_conn_ingest.py` (modified). Confirmed the Data Manager grant (`data_manager.py`) and
  DDL (`database.py`) land in the same commit as the hw_monitor ingest hook — no window of
  writing to ungranted tables. `py_compile` clean; live test runs: `test_conn_seen.py` 67/67,
  `test_conn_ingest.py` 46/46 (was 26/26 pre-change). Rule-8 leak scan on the full diff: clean
  (RFC 5737/3849 test-net addresses only).
- Committed `4a82785` (the 6 held files) and `faaf9a2` (roadmap doc correction — Window 1
  confirmed the seen-set does NOT reuse the reputation cache; that cache is agent-side/IP-only
  and out of scope for this table's per-device destination membership). Pushed both to
  `origin/main` after operator confirmation; `HEAD`==`origin/main`==`faaf9a2` verified.
- New request: formalize a standing CLAUDE.md rule that Morning Status must surface all
  current elevated access grants every session (not a one-off note), check whether HANDOFF's
  existing "Standing elevated grants" section already covers the pattern, and record today's
  grants (Suricata sudo grant, DHCP module's `pihole` group membership, and a claimed new
  `pihole` group membership for the operator tied to the cardinality tool).
  - Live-checked before writing anything: `sudo -n -l`, `getent group pihole`/`nemesis-db`/
    `nemesis-fw`, `groups`/`id` for the operator and `nemesis-dash`, `ls /etc/polkit-1/rules.d/`
    (permission denied — noted, not silently skipped).
  - Suricata sudo grant: CONFIRMED live (3 scoped NOPASSWD entries for
    `/etc/suricata/rules/local.rules` + `systemctl reload/restart suricata`).
  - DHCP module `pihole` group membership: re-confirmed NOT present on this production box
    (`getent group pihole` empty, `nemesis-dash` groups unchanged) — matches the 2026-08-07
    finding that this grant exists only on the gateway test zone.
  - Operator `pihole` group membership for the cardinality tool: **could not confirm — appears
    false.** `getent group pihole` shows the operator is not a member; `id <user>` agrees.
    Cross-checked `~/work/nemesis-internal/handoff/2026-08-08-window1-handoff.md`, which
    states explicitly that the operator is NOT in the `pihole` group and ran the cardinality
    tool via general (password-gated) `sudo`, not a group grant. Flagged in HANDOFF.md's
    elevated-grants section as an unconfirmed/contradicted claim rather than recorded as fact.
  - Added CLAUDE.md Morning Status item 7 ("Elevated access grants — surfaced live every
    session"), renumbered the old item 7 (write briefing to disk) to item 8, added an
    "Elevated grants:" line to the status-block format, and fixed the one cross-reference to
    "Morning Status §7" in Rule 12 (now §8).
  - Updated HANDOFF.md §6 with today's live-reverified state for the two real grants and the
    flagged/unconfirmed third claim. Rule-8 leak-scanned the diff before staging (initial draft
    used the operator's real username directly — caught and fixed to `<user>`/"the operator"
    before commit).
