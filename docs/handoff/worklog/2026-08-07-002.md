# Worklog — 2026-08-07-002 (Window 2)

Flight recorder. Minimal prose, chronological, append-as-you-go. Second session today —
picks up after the `2026-08-07-001` checkpoint closeout (`8c99a85`).

- Session start: Window 2 identity confirmed by operator. Model self-check: Sonnet 5,
  matches expected — no flag needed.
- Ran Morning Status a second time this date (a fresh session, per the routine). Baseline
  resolved to `docs/audits/roadmap-state-audit-2026-08-06.md` (8 SHIPPED/10 PARTIAL/58
  PARKED, 76 total). File-set drift: none (76=76, `--diff-filter=A/-D` confirmed empty
  both ways since 08-06). Shipping drift found: `track-c-metadata-tier-build-plan.md`
  PARKED→PARTIAL — header still says "no code written yet" but `ccf02aa`
  (consent gate, Track C Requirement 0, 43/43) and `e14e5a4` (connection-event schema,
  Track C step 2, 52/52) both shipped with commit subjects explicitly naming Track C.
  Wrote refreshed baseline `docs/audits/roadmap-state-audit-2026-08-07.md` (new tally
  8/11/57, 76 total) and briefing `docs/briefing/2026-08-07.md` (overwrote the 08:37
  run from session 001). Both written to disk first, committed later this session (see
  below) — briefing is gitignored/local-only per Rule 12, never committed.
- User asked: "is the diff current and healthy?" — full audit of everything Window 1 had
  left as "ready to commit" (per session 001's worklog tail): 10 Track C schema-v2 files,
  2 Rule 13 continuation files, 1 new tooling script, plus `install_pihole_pwd.sh`
  (mode-only diff, flagged suspicious). Verified fresh (`git fetch`, local==origin
  confirmed before starting). `py_compile` on all 11 Python files + both shell scripts:
  clean. Ran the 3 live test suites: `test_conn_events.py` 64/64, `test_conn_collector.py`
  54/54, `test_conn_ingest.py` 28/28. Read every diff file-by-file (data_manager.py grant,
  database.py DDL, hw_monitor.py ingest/consent-gate/reaper, dashboard.py hookup,
  nemesis_fw_watch.py/dns_enforce.py Rule 13 proven-revert additions,
  nemesis-pihole-password.sh). Rule 8 leak scan: clean (RFC 5737/1918 test fixtures only).
  Flagged `install_pihole_pwd.sh`'s mode-only diff (644→755) as likely a stray `chmod`,
  not a deliberate edit — held pending Window 1 confirmation rather than bundled.
- User: "Proceed — stage by exact path, excluding install_pihole_pwd.sh... Commit and
  give me the push-coordination list." Mid-audit, `modules/dhcp/module.py` appeared
  freshly modified in the working tree (not present at session start) — flagged as a
  live concurrent edit from another window, left untouched, correctly excluded from
  staging (path-based `git add`, not `-A`). Three commits, by import-dependency /
  logical-unit grouping:
  - `180514a` — Track C schema v2 (10 files): server-side ingest, consent-gate
    enforcement (fails closed), `resolved_name`/provenance schema fields, retention
    reaper, `conn_collector.py` + `etw_probe.py` (new).
  - `b43e487` — Rule 13 continuation: `nemesis_fw_watch.py` auto-restore readback,
    `dns_enforce.py` set/restore/kill-switch readback verification.
  - `7400e9c` — new `scripts/nemesis-pihole-password.sh`, replacing the dead
    `install_pihole_pwd.sh` (left in place, unmodified).
  Push-coordination list shown; user confirmed "push those 3". Pushed
  `8c99a85..7400e9c`, verified `local==origin` at `7400e9c` via fresh fetch.
- User, two-part request: (1) commit the 4 held DHCP files together (module writes to
  tables the grant authorizes, so they must land as one unit), push. (2) file PUNCHLIST
  entries for tonight's DHCP deployment follow-ups, surface in tomorrow's briefing.
  - Window 1 confirmed mid-turn: the `install_pihole_pwd.sh` chmod was a stray, reverted.
    Confirmed clean via `git status --short` on that path.
  - Verified fresh, reviewed `data_manager.py`'s new exact-name grant
    (`dhcp_health_samples`, `dhcp_lease_events`) against `module.py`'s actual table
    writes (matched, confirmed via grep), `py_compile` clean, ran both test suites live:
    `test_dhcp_module.py` 159/159 (new §18a/18b/18c behaviourally proves the grant, not
    just source-text), `test_dhcp_mode_switch.py` 78/78 (regression check, unaffected by
    this change). Rule 8 scan clean (127.0.0.1/1.2.3.4/10.0.0.x test fixtures only).
    Noted in-review: module calls `nemesis_errors.record_error_best_effort()` for every
    E-DHCP code but the `dhcp` namespace has no grant for `error_codes`/
    `error_occurrences` — flagged for the PUNCHLIST entry below, not fixed here.
  - Committed `f5deda0` (steady-state health observability: crash-loop detection,
    port67 verification via `/proc`, lease-event log). User's instruction included
    "push" explicitly — pushed `7400e9c..f5deda0` directly, verified sync.
  - Filed 4 PUNCHLIST entries under a new dated section (polkit-rule-as-stopgap vs.
    `nemesis-fwd` pattern; Pi-hole group-membership over-grant exposing the web-password
    hash; the `error_codes`/`error_occurrences` grant gap, framed as an ADR-0006
    question not DHCP-specific; three host-level deployment fixes — polkit rule, systemd
    drop-in, group membership — missing from `install.sh`, meaning a fresh install hits
    the same wall tonight worked through by hand). Edited `HANDOFF.md` §3: item 5 marked
    RESOLVED (`f5deda0` superseded it), new item 7 points at the PUNCHLIST section,
    explicitly flagged for tomorrow's (08-08) Morning Status per operator instruction.
    Rule 8 scan clean. Committed `d2b4df8`, mirrored `HANDOFF.md` to
    `~/work/nemesis-internal/handoff/`. Push list shown (1 commit); user confirmed
    "push it". Pushed `f5deda0..d2b4df8`, verified sync. Gave full commit/push list per
    user's follow-up request.
- User: audit the two long-uncommitted "not mine" files (`docs/roadmap/
  venue-guest-network.md`, `docs/audits/roadmap-state-audit-2026-08-07.md`) — what are
  they, finished or stale, public or private per Rule 10, commit if worth keeping,
  recommend (not decide) if genuinely orphaned.
  - `roadmap-state-audit-2026-08-07.md`: mine, from earlier this session, finished,
    Rule-8-clean, same location/format as every prior baseline (`git log -- docs/
    audits/` confirms the pattern). Straightforward keep.
  - `venue-guest-network.md`: traced via `git log`, `git diff`, and every worklog/
    supplement mention since 07-26. The uncommitted diff is a single complete 27-line
    addition — an operator decision dated 2026-08-03 (Android agent deferred until near
    v2.0-final core agent; Apple/Mac/iOS work strictly sequenced after Android's QR-
    enrollment flow validates end-to-end, not in parallel) — sitting untouched since at
    least 08-03 despite six separate session references flagging it as "not mine, don't
    touch" without anyone reviewing it. Confirmed via `git show HEAD` that the file's
    pre-existing "FLAGGED TENSION" caveat (app-as-credential vs. ADR 0015's QR-only
    enrollment) is untouched by this diff and was already public since 07-26 — no new
    Rule 10 exposure. The new content itself is a sequencing/business decision, not a
    novel mechanism or new honest-limitation caveat — clean for public.
  - Recommended keeping and committing both; user's instruction already authorized
    committing on a "worth keeping" finding. Committed `85e4e1e` (audit doc),
    `75b8b14` (venue-guest-network addition). Push list shown; user confirmed "push
    them". Pushed `d2b4df8..75b8b14`, verified sync.
- User: commit the final two Track C step 4 files (`conn_collector.py`,
  `test_conn_collector.py` — Window 1 had been actively iterating on these
  concurrently through the session, flagged each time they showed fresh diffs).
  Verified fresh, `py_compile` clean, ran `test_conn_collector.py` live: 88/88 (was
  54/54 — new coverage for flow identity/port-reuse, corrected TCP/UDP classification,
  idle expiry, close-grace deferral, UDP direction-swap fix). Read the full diff:
  `EtwSource` now classifies by MEASURED event ID (probe VM + LAN rig runs, 2026-08-07)
  rather than field shape — the prior field-shape heuristic was ACTIVELY WRONG (TcpIpSend
  carries startime/endtime, so every send was misclassified as a close, `bytes_sent`
  never accumulated, first send popped the accumulator and orphaned all later data; the
  file also retracts an earlier false conclusion that ETW delivers events out of order).
  `connid` confirmed useless (measured: literal `"0"` on all 211 probe events) — identity
  now derived from 5-tuple + open timestamp. `pywintrace` import confirmed lazy (inside
  `EtwSource.start()`), so the module stays importable/testable on Linux. Rule 8 scan
  clean (RFC 5737/1918 fixtures only); no leftover debug artifacts (the `print()` hits
  are the test script's own section-header output style, matching `test_conn_events.py`'s
  established convention). Committed `9a94244`. Push list shown (1 commit, tree fully
  clean); user confirmed. Pushed `75b8b14..9a94244`, verified sync.
- Working tree fully clean as of `9a94244` — first point all session this touched at
  once. No files left uncommitted from either session today.
- User: Window 1 updating its own context handoff (private repo,
  `~/work/nemesis-internal/handoff/`, its own git-write privilege — not this repo, not
  mine to wait on or touch) and will commit it there. Operator asked Window 2 to start
  the full nightly closeout.
