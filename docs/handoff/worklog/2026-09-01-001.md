# Worklog — 2026-09-01 (Window 2)

> Reconstructed at closeout from commit history + session record, not kept fully live
> during the session — same gap noted in the 2026-08-31 worklog. Raw chronology, minimal
> prose. Curated account: `supplements/2026-09-01-001.md`. Session ran under an
> operator-issued storm/power-loss warning from mid-morning on — commit cadence is
> unusually fine-grained as a result (see HANDOFF.md).

- 09:xx — Morning Status run. 197,556 LOC. Services: all 6 active. Roadmap baseline
  (`roadmap-state-audit-2026-08-31.md`, 08:28 same-day snapshot) found to already predate
  ~70 commits of that same day's later shipping — flagged as drift, not corrected in
  place (read-only per Rule 1). `a8e0221` found already pushed despite HANDOFF listing it
  as pending. Briefing written: `docs/briefing/2026-09-01.md`.
- 09:xx — Elevated grants re-checked live: production box clean, unchanged from 08-31.
  Committed `e86e366`.
- ~10:18 — Push confirmed and executed: `eb938b3` (Window 1's tailscale
  packaging-independence test suite, 28 checks) + `e86e366`, both named explicitly per
  shared-tree attribution discipline. Verified `local == origin`.
- ~10:2x — Operator flagged storm/hurricane warning. Working discipline tightened:
  commit locally after every stable unit, handoff updated after every commit, no long
  atomic state-changing operations.
- ~10:3x — Temporary Tailscale-audit sudo grant (added earlier for Window 1's
  investigation) confirmed revoked, independently verified via live `sudo -n -l`.
  Committed and pushed as `d1ebcf9`.
- ~11:0x — Status re-check requested; found actually clean (claim of uncommitted work did
  not match live state) — reported the discrepancy rather than acting on the unverified
  claim.
- ~12:0x–12:22 — Full Tailscale/PIA/snap DNS saga read and consolidated from ~14 private
  documents (spanning 2026-08-01 through same-day) into
  `~/work/nemesis-internal/known-limitations/tailscale-magicdns-pia-saga-FULL-2026-09-01.md`
  (`c355449`, private repo). At the time of writing, Option B (the PIA-reconnect fix) was
  described as "scoped, not yet built" — this was already stale within hours (see below).
- ~13:xx–14:xx — Two deliverables from operator: (1) user-facing doc explaining
  vpn-dns-guard's existing self-heal behavior on VPN connect/disconnect, deliberately
  scoped away from the still-unresolved MagicDNS-specific case — `docs/operation/
  VPN_CONNECT_DNS_DELAY.md` (`ab73831`); (2) PUNCHLIST follow-up logged for verifying the
  MagicDNS guard's VPN-agnostic design against a simulated (non-PIA) killswitch (`750c806`).
- ~14:xx — Discovered, on fetch before pushing those two commits, that Window 1 had
  shipped 8 real commits (`ff3d6c4`→`b05ec54`) building and live-testing Option B that
  same afternoon — contradicting the "not yet built" framing just written. Corrected both
  the PUNCHLIST entry (`750c806`, amended in place before first push) and the saga doc
  (`bc6d328`, private repo) with explicit UPDATE sections rather than silent edits.
- ~14:xx — Public repo's 10 (later 11) MagicDNS-guard commits reviewed and pushed
  (`ff3d6c4`...`b05ec54`, then `faf7666`), together with the two Window-2 doc commits.
- ~15:xx — Private-repo (`~/work/nemesis-internal`) push requested; found the intended 2
  commits sit 52+17 commits deep in a single linear, entirely-unpushed history (76 commits
  at first count, growing to 82 by end of day) — a normal push cannot publish 2 commits
  without publishing the whole backlog. Held the push, did NOT rewrite history.
- ~16:xx — Full categorized review of the 82-commit private backlog run: bulk 08-01→08-31
  doc-backlog catch-up (109 files, grepped for secrets — none found), Piece F/G TLS work
  (Window 3, coherent progression, matches current file state), Tailscale/PIA/MagicDNS
  thread (already read in depth), legal/LICENSE + VPS/ gitignore (both already
  operator-decided 08-31). No commits found warranting exclusion or a stricter-than-
  local+usb sensitivity tier. Reported categorized findings; did not push pending
  explicit go-ahead.
- 18:xx — End-of-day closeout requested. Re-verified current state: public repo now 11
  unpushed (Window 1's final push-set, `faf7666` added), private repo 82 unpushed.
  Window 1's `4d99b2a` explicitly flagged 2 of those 11 as "not mine" — confirmed both
  (`ab73831`, `750c806`) as this window's own, authored earlier the same session.
  Read Window 1's and Window 3's FINAL/NIGHT closeout docs for authoritative current
  state: `accept-dns=True` is now the live state (changed from the morning's "keep
  false" recommendation, made safe by the guard being proven — 6 PIA live tests + 1
  Proton generalization test, 5 root causes found and fixed, 190 checks passing). Proton
  VPN installed on the daily driver (Rule-13-compliant: revert written before connect,
  offline-capable). Residual backup-loss risk closed by `b05ec54`'s self-repair op.
  Public repo pushed (11 commits, gate-checked, verified `local == origin`).
  Private-repo push attempted, blocked by the permission classifier (scale of the
  action) — held for explicit operator action.
- 18:xx — Closeout docs written: this worklog, `supplements/2026-09-01-001.md`,
  `HANDOFF.md` overwritten.
