Worklog — 2026-08-17, Window 2

RECONSTRUCTED AT CLOSEOUT, not written live — the live-worklog habit lapsed again
today (same process gap noted in 2026-08-08's worklog). No evidence anything was
lost; reconstructed from git log + conversation record. Re-establish live
append-as-you-go next session rather than repeating this note a third time.

- Read-only audit: production nginx config vs install.sh template. Deployed
  config on this box has rate-limit stanzas + hardcoded server_name; install.sh
  ships the pre-hardening baseline. New finding beyond the known ADR 0021 gap:
  install.sh's nginx block has no idempotency guard, so a re-run would silently
  regress the deployed hardening. Wrote docs/audits/nginx-config-drift-audit-
  2026-08-16.md (public, sanitized) + full-detail private copy. STILL
  UNCOMMITTED at closeout — flagged in HANDOFF §3.
- Surveyed and committed Window 1's Gap 1-3 firewall/loopback/source-guard batch
  as 4 sequenced commits (ca00be9, 9df9b9e, 21b3541, 8981f52), hunk-splitting
  dashboard.py/hw_monitor.py away from an unrelated 08-08 clamscan-log WIP that
  was NOT ready to commit. Caught and sanitized a Rule 8 leak in the tailnet-
  removal test fixtures (a real household device name + a captured real tailnet
  IP) before committing — fixed with placeholders, re-verified clean. Pushed
  after explicit confirmation; held on request pending live E2E test against
  the real Tailscale API, which Window 1 then ran and found+fixed a real bug
  (positional vs by-name row access) before the hold was lifted.
- Surveyed and committed the node-locked licensing engine (e25a92c) + PUNCHLIST
  correction (96cfe9b) as 2 commits per Window 1's suggested split. Independently
  re-verified Window 1's Rule 10 read (ran the 65-assertion test suite myself,
  scanned the diff for pricing/threshold language) rather than trusting it.
- Committed the dashboard licensing integration (schema-first: 458079f, then
  1b52d7c) — 4 routes, budget strip, backup codes UI. Verified auth posture,
  route-security checklist, no raw-apostrophe f-string risk.
- Committed cap enforcement (c1cd5ff) + its connectivity-specific test suite
  (0a359de) as 2 commits, after determining by actual test dependency (not
  guessing) that the "pasted-key fix" commit had to include the full seam-1 +
  seam-2 + stamping + revoke-clearing infrastructure, since
  test_source_level_wiring hard-requires all of it. Found and sanitized a
  second, more serious Rule 10 issue before committing: cap_guard.py and
  net_reachability.py's public docstrings spelled out exactly how to defeat the
  cap enforcement (root + defeat-the-probe). Replaced with a neutral pointer;
  full text moved to the private mirror
  (known-limitations/cap-guard-bypass-disclosure-2026-08-17.md).
- Audited the repo for a LICENSE file per direct request — found none exists,
  ever (no git history of one either), no README, no license-grant language
  anywhere. Drafted LICENSE (source-available, personal-free/commercial-paid),
  README.md, and ADR 0022 explaining the choice, all explicitly marked DRAFT.
  Committed and pushed (326c0e8) once Paul confirmed a reviewed-later draft
  beats no license at all. Copied the draft to the private mirror
  (~/work/nemesis-internal/legal/) for reference, uncommitted there.
- Committed the hw_monitor fingerprint-loader fix (2c2ff14) after re-verifying
  a claimed commit citation (9ffac56, the relocation commit) against real git
  history, and running the new subprocess-based regression test for real
  output (13/13). Rewrote the PUNCHLIST entry in place rather than appending a
  correction on top of an incomplete original.
- Committed the 3-category orphan-display fix (49efa1f), hand-splitting a
  tangled dashboard.py hunk (known_not_entitled addition vs. an unrelated
  codes_level default change on adjacent lines) via a hand-crafted patch rather
  than a plain hunk slice. Ran the full licensing suite (71/71) before
  committing.
- Verified and committed the real production Ed25519 issuer public key
  (8662b14) on its own, per explicit request given how much depends on it being
  exactly right. Independently re-derived the public key from both the local
  private-key file and its offline USB backup copy, and ran a real
  already-issued license through the product's own verifier against the
  now-embedded key (verdict: valid). Private key material never printed,
  logged, or copied anywhere during verification. Flagged the USB backup copy's
  loose file permissions (0755, should be 0600) as a minor, non-blocking note.
- Committed the backup-codes none_issued/exhausted fix (6597853), manually
  verifying all three states against a temp DB with real output since no
  dedicated regression test existed yet for this one (flagged, not
  self-authored, since writing new test code is outside Window 2's role).
- End-of-session survey for closeout: found a new, unrelated install.sh WIP
  hunk (HTTP/2 stack deps for the private L3 Tier 2 gate) had appeared mid-
  session from Window 1 — not touched, noted as open WIP.
