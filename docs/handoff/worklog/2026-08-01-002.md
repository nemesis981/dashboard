# Worklog — 2026-08-01 (Window 1, build)

Raw chronological log, authored by Window 1, committed by Window 2. Rule-8/Rule-10
redactions applied before commit — see the private mirror for full detail on the items noted
below; nothing else in this file was altered from what Window 1 provided.

---

### C5 harness (last_activity security property)
- Created a real account via the normal path (`manage.py create-user harnesstest`,
  Rule 11 label in display_name), not a synthetic fixture.
- PASS: 6 GETs to `/account/unlock` over 61s left `last_activity` byte-identical
  (checked at every checkpoint); control GET to `/` moved it by 60.21s,
  matching wall clock. Production coalesces writes at 30s, so 61s spans two
  windows — the lock-screen auto-refresh cannot extend a locked session.
- Account deleted entirely (users row + recovery_codes). `login_events` id 83 and
  `audit_log` id 127 KEPT — deleting audit history to tidy a test would falsify the
  trail. Rule 11 audit_log exception applies; ids recorded here per CLAUDE.md.
- Incidental proof of the item-2 fix (`331589a`): `audit_log` id 127 shows
  `cli_create_user` with actor `cli:<user>` — `_actor()` resolves `SUDO_USER` correctly.
  First confirmation against a real invocation.

### Never-block guard — SHIPPED (`bc64988`)
- New `scripts/nemesis-fw-neverblock`; `nemesis-fw-render` fails closed (exit 3,
  `$OUT` untouched) when the guard cannot run. Guard self-tests four canaries every
  run (127.0.0.1, ::1 must refuse; 203.0.113.1, 2001:db8::1 must allow).
- VM-verified 6/6 (Ubuntu 26.04): protected address refused+visible, ordinary
  unaffected, /32 and /24 both matched against a bare entry, guard-unavailable
  refusal leaving output byte-identical, `NEMESIS_NEVER_BLOCK` honored end-to-end,
  canary sabotage killing the guard.
- Production render check: 0 refused, 0 verdicts — clean branch, no baseline
  interaction.

### ADR 0019 Increment 4
- Commit A (`df24480`): verdict emission, gated OFF by default. VM checks 1,2,3,7,8.
- Commit B: default flipped to `:-1` — MADE, VERIFIED IN-FILE, **UNCOMMITTED as of
  this worklog entry** (Window 2 held it the same session pending resolution of
  check 7 — see "State at close" below).
- DEFECT FOUND AND AVOIDED: appending `drop` after `limit rate 10/minute` would
  rate-limit the DROP. `limit` is a MATCH — "a rule using this statement will match
  until this limit is reached" (nft(8)). Would have dropped ~10 packets/min and
  passed the rest: fail-open under exactly the load enforcement exists for. Fixed
  with two rules per address; live kernel confirms identical counts between them.
- Log prefix renamed drop -> blocked: a prefix containing a verdict word is
  indistinguishable from a verdict to anything counting verdicts from rule text.

### Checks 4-6 (independent generators, isolated test network)
- **Conntrack gap reproduced and confirmed closed by Increment 4.** Full mechanism
  detail, exact priority value, and measured round-trip/packet counts are in the
  private mirror (`known-limitations/block-path-conntrack-bypass-2026-08-01.md`,
  "Update 2026-08-01 (VM)" section) rather than repeated here.
- Attribution was isolated deliberately: ufw's own deny rule was removed (ACCEPTs
  kept) so the derived table was the only possible source of the block, confirmed
  by direct packet capture.
- Windows notes: `Start-Process` does not survive SSH (OpenSSH kills the session
  tree) — use `Win32_Process.Create`. Nested quoting through cmd.exe fails silently;
  use `-File`. ufw default-deny had to be opened for the generator or no drop could
  ever be attributed.

### Lockout failsafe timing — investigated, NOT changed
- `AccuracySec=1min` hypothesis REFUTED three ways (staggered arming phases showed
  no matching pattern; repeated trials landed near the established baseline; a test
  build with a tighter accuracy setting showed no material difference) — that fix
  was deliberately NOT committed.
- Real cause and exact measured figures are in the private mirror
  (`ADR-0019-deterministic-enforcement-point-FULL.md`, "Lockout-failsafe timing"
  section and its 2026-08-01 update) rather than repeated here — summary only:
  querying live `nft` state while the failsafe is armed measurably delays the
  revert; switching the test harness to a non-`nft` poll removed the delay.
- Harness fixed to stop being part of what it was measuring.

### Production cutover — steps 1-6, 8 PASSED
- Snapshot `2026-08-01-2027-pre-increment4-cutover` (+ live nft ruleset captured as
  root during the deploy, closing the UNKNOWN logged in STATE.txt).
- Real drop authority proven live against probe `203.0.113.9`; watcher rebaselined
  on both the controlled apply and the unattended re-render; no false tamper alert;
  no collateral regressions.
- **STEP 7 UNPROVEN — NOT passed, NOT failed.** Reported FAIL was the test
  instrument: `thash()` hashed the RAW `nft` dump including live packet/byte
  counters, which move on every inbound packet. `rerender()` DOES check the render
  exit code (`nemesis_fw_watch.py:220-222`) and returns before `nft -f`;
  `AUTORESTORE` defaults off. Confirmed on production: raw hashes taken 6s apart
  differ (as expected, since counters moved); counter-stripped hashes of the same
  two reads match.
- **COVERAGE GAP FOUND:** VM check 8 tested `render` directly, never `rerender()`'s
  call site. The watcher was not even installed on the VM. The unattended path — no
  revert window, fires on every ufw change — has NO fail-closed coverage yet.

### State at close (as Window 1 left it)
- `HEAD df24480 == origin/main`. Uncommitted: `scripts/nemesis-fw-render` (the flip).
- Production enforcing-capable and clean: 0 drop rules (nothing real blocked),
  probes removed, guard restored, all services active.
- **Window 2 commit for Commit B HELD until check 7 has a correctly-measured
  result.**

### Open items for the next session
1. Re-measure `rerender()` fail-closed with a counter-stripped hash (Fix A), and
   have the instrument prove itself: two hashes with no change must match, a known
   change must differ. THEN commit Commit B.
2. Install `nemesis-fw-watch` on the VM and add the `rerender()` fail-closed path to
   the permanent VM plan — close the coverage gap for good.
3. Private mirror only — the two lockout-failsafe timing findings (honest baseline
   latency against the nominal window; an under-load degradation, cause since
   identified — see the private mirror doc). Not public: timing behavior of the
   failsafe.
4. Tracked risk, deliberate: two apply paths have no revert window — the boot unit,
   and `rerender()`. Both fail-closed on guard failure (`rerender()`'s fail-closed
   behavior itself pending item 1's re-measurement). A revert window on an
   unattended path would auto-withdraw enforcement with nothing able to confirm it,
   so the asymmetry is by design.
5. Instrument defects this session: 12+, all one shape. Now covered by the standing
   rule in CLAUDE.md (`68124dd`).

---

### Addendum (Window 2, same date) — Commit B status update

A later handoff message characterized item 1 above ("re-measure `rerender()`
fail-closed... THEN commit Commit B") as "not part of this commit and not blocking
it," and Commit B (`scripts/nemesis-fw-render`'s default flip) was committed locally
as `e522c05` on that basis. Comparing that characterization against this worklog's
own explicit gating language, the discrepancy was flagged before pushing. Operator
confirmed: this worklog's language is the accurate one — check 7 has not been
re-measured with the corrected instrument, and that re-measurement is deferred to
the next session per item 1 above. **`e522c05` is held: committed locally, not
pushed, and stays that way until item 1 is done and passes.**
