# Worklog — 2026-07-28 (Window 2)

Raw chronological log, flight-recorder style. Curated version: `supplements/2026-07-28-001.md`.

- 08:02 Morning migration-status audit run. Found critical gap: `dashboard.service`'s
  hardening-exception (deliberately unhardened, per 07-27 HANDOFF) was not codified in
  `gen_units.py` — a regeneration risked silently hardening it and breaking `sudo -n ufw`.
  Committed as `bc36219` (audit doc) then fixed in `bd23f2b`.
- 09:54 CLAUDE.md's stale `~/dashboard` paths (Morning Status commands, Key paths section)
  fixed post-relocation. `ebd3bf6`.
- Read-only audit of dashboard's sensitive-action surfaces for three-layer-verified-pattern
  applicability (process identity + admin-account check + fresh-credential check). Findings
  written privately: `~/work/nemesis-internal/known-limitations/dashboard-sensitive-surfaces-audit-2026-07-28.md`.
  Tiering (Tier 1/2/3) confirmed by operator, no implementation yet.
- 10:33 CSRF fix: GET-as-write on `/api/quarantine/<id>/confirm`, `/api/quarantine/<id>/lift`,
  `/api/action/<rule_id>/<action>` → POST-only, matching JS updated. Urgent (Window 1 blocked
  on it for the ufw-helper rewire). Window 2 edited `dashboard.py` directly, user-authorized.
  `e7360d5` → amended to `8c8bce9` (wrong cross-reference in commit message, caught and fixed
  same session).
- 13:49 `nemesis_fwd.py` build reviewed and committed: three-layer verification (SO_PEERCRED,
  users-table lookup, bcrypt credential check), `PEER_POLICY` per-peer op allowlists. Held for
  a Rule-8 hit (`DASH_USER` defaulted to the real install username), Window 1 fixed to
  no-default/fail-loud, re-verified.
  `3cf0e4d`.
- 14:00 `scripts/deploy_nemesis_fwd.sh` committed after a permissions fix (664→755). `7d4999a`.
- Audit for upcoming core_module work: dashboard's OS-identity coupling (sudoers, groups) +
  core-but-not-pluggable process landscape. Found a live fd-leak in already-committed
  `nemesis_fwd.py` (`with _db() as conn:` never closes the connection — `sqlite3.Connection`'s
  context manager only handles the transaction). Documented privately, not fixed this session:
  `~/work/nemesis-internal/known-limitations/core-module-identity-and-db-audit-2026-07-28.md`.
- 15:08 ADR 0018 authored (attacker-resistant backup medium protection + manifest-based
  recovery), cross-referenced from ROADMAP.md. `045cedc`.
- 16:21/16:24 Roadmap stub for three future Windows-agent requirements (paused
  memory-injection module: agent rework, small GUI, Windows-Update-triggered re-checks), then
  amended same-day to add a dashboard fleet-update detail. `2c2d580`, `04a3b88`.
- 17:11 Data Manager v1.1 retrofit reviewed: explicit table lists, 3-state enforcement modes,
  column grants, `GuardedCursor`. Held for a confirmed `executescript()` NameError
  (`sql` vs `stmt` copy/paste) and missing test coverage; both fixed by Window 1, re-verified
  by reverting/re-testing/restoring. Landed alongside `nemesis_fwd.py`'s tiered account
  lockout fix (same commit batch). `c10a9d3`, `401b58d`.
- 17:58 core_module layout move, Commit A: six daemons (alert_watcher, hw_monitor,
  device_scanner, watchdog, malware_canary, diagnostics_watcher) additively copied into
  `core_module/<name>/` (old files untouched). Found and fixed (user-authorized, direct edit)
  a hardcoded real username in `core_module/device_scanner/manifest.json` → `__INSTALL_USER__`.
  `9ffac56`. **Rule-8 note (found at closeout, not fixed):** that commit's own message quotes
  the literal offending value rather than a placeholder — the real username is now in public
  git history a second time, in the commit log itself. See Open Items.
- 18:20 `migrate_core_module.sh --verify` fix: three-state classification (migrated+healthy /
  pending / failed) instead of two states lumped under FAIL. Found and held a
  `set -euo pipefail` + grep-no-match silent-abort bug in `classify_service()`; Window 1 fixed
  with `|| true` (both there and defensively in `verify_healthy()`); re-verified via a
  reproduction harness (mocked systemctl/journalctl, all 4 state combinations). `eb5a35b`.
- 18:55 Dashboard's unit gains `core_module/hw_monitor` on PYTHONPATH (zero behavior change —
  `import hw_monitor` still resolves to the `alert_manager` copy, found first). Prerequisite
  for later removing that copy. Deploy performed by operator (no interactive sudo in either
  window); independently re-verified healthy after. `04c9e11`.
- 19:05 Commit B, hw-monitor: removed `alert_manager/hw_monitor.py` +
  `alert_manager/hw-monitor.service` together (not just the .py) — confirmed the actual
  installed hw-monitor unit had already cut over to `core_module/hw_monitor/` separately;
  the stale alert_manager unit, if left, would point at a now-missing file and break fresh
  installs harder than a silent skip. `85e2baa`.
- 19:06 `install.sh`'s `deploy_services()` fixed to glob `core_module/*/` first, ahead of
  `alert_manager/` and `core/` — closes the fresh-install gap flagged earlier in the day
  (found during pre-action verification before attempting Commit B on the other five).
  `dd95de1`.
- 19:20 Commit B attempt for the remaining five (alert_watcher, device_scanner, watchdog,
  malware_canary, diagnostics_watcher) — found two real consumers of the OLD path that
  contradicted the "confirmed not imported anywhere" handoff claim: `test_quarantine.py`'s
  `sys.path.insert` only covers `alert_manager/` then `import alert_watcher`; and
  `deploy_nemesis_fwd.sh`'s `check_sources()` hardcodes `alert_manager/alert_watcher.py`.
  Held `alert_watcher.py` + its unit pending Window 1 fixing both. Proceeded with the four
  unaffected files.
- 19:37 Commit B landed for device_scanner, watchdog, malware_canary, diagnostics_watcher.
  All four confirmed live from `core_module/` beforehand (0 restarts), byte-identical,
  `gen_units.py --check` PASS after removal, fresh-install search order re-derived clean.
  `fbce915`.
- End of day: closeout requested ahead of Window 1's ~4hr pen-test run tonight against an
  isolated Linux host + Windows 11 agent (separate test lab, not production). Roadmap
  file-set drift found (+1, the memory-injection-prereqs stub) — baseline refreshed to
  `docs/audits/roadmap-state-audit-2026-07-28.md`. All 9 production services confirmed
  active/running/0-restarts at closeout. `alert_watcher` Commit B remains open, held on
  Window 1.
