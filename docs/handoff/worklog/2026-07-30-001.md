# Worklog — 2026-07-30-001 (Window 2)

Raw chronological log, reconstructed from git history plus direct check-ins with Window 1's
and Window 3's live sessions (read their session transcripts directly — not their own summaries
alone) and live-system verification. Times are commit/log timestamps where known.

- Morning: Window 2 read-only audit of the host-defense deploy (steps 0–7) and the
  overnight/live tailnet-block verification Window 1 had in progress. See prior session
  turns; not re-logged here.
- ~11:05 `alert-watcher` blocks `<test-vm-tailnet-ip>` (test VM) again via `fw_client.block_ip` —
  audit_log confirms.
- `19d9b5c`-equivalent work in progress: Window 1 builds ADR 0019 Increments 1–3
  (`nemesis-fw-apply` lockout-failsafe tool, `nemesis-fw-render` observe-only derived
  ruleset). Runs `selftest` at least twice, watches the revert actually fire both times.
  Measures a real bug: back-to-back `apply` runs without `systemctl reset-failed` reuse the
  transient timer's `OnActiveSec` state and fire early (33s against a stated 60s window).
  Fixes it, re-measures clean: armed `15:36:27`, fired `15:37:27` — exactly 60s (61s
  measured, 1s polling granularity).
- 13:49 Window 3, with the operator at the terminal, runs `sudo rm
  /etc/sudoers.d/nemesis-host-defence-deploy` + `sudo visudo -c` (parsed OK) — closes the
  deploy-lifetime sudoers grant now that the host-defense punchlist is otherwise closed out.
  `fail2ban` restarts same window, picking up the `sshd-tailnet` jail split
  (`maxretry=3, bantime=300`, sibling to `sshd`'s `maxretry=3, bantime=86400`).
- 14:50 `eb1943c` — Window 3 commits **and pushes directly to `origin/main`**, bypassing
  Window 2. Content: `record_fail2ban_quarantine()` in `nemesis_fwd.py`, so a fail2ban ban
  now writes the same `quarantines` row alert-watcher's auto-quarantines get (previously
  audit_log only) — closes the gap where `jail.local`'s own action comment promised
  dashboard visibility that didn't exist. `fw_client.block_ip()` gains an optional `jail=`
  tag. `data_manager` grants `nemesis_fwd` a shared INSERT-only writer role on
  `quarantines`. fail2ban's peer authority unchanged — still cannot call
  `expire_quarantine`/`unblock_ip`. Reviewed post-hoc by Window 2 (Rule-8 clean, content
  sound) since it landed without the normal pre-commit review step.
- Window 2 (this session) commits `e6cbbda` (ADR 0020), `3927bb2` (ADR 0009 addendum),
  `d72cda8` (install.sh interim tailnet mitigation + PUNCHLIST), `cf1c319` (Track C build
  plan), `9bd6228`/`0fec951` (fd-leak fixes) — see prior turns for full review detail on
  each. Pushed through `e6cbbda`.
- Read-only audit (Window 2): verified the `install.sh` `configure_tailnet_enforcement()`
  comment block over-discloses relative to Rule 10 — same specificity as the private
  Finding 1 writeup, sitting in the public, already-pushed repo. Flagged; not yet fixed.
- Window 1 continues past the failsafe fix: writes the ADR 0019 addendum (defect confirmed
  live, mitigation applied, urgency downgraded — properly abstracted this time, no rule
  syntax or reproduction steps, unlike `install.sh`'s comment).
- Window 1 hands off three files to Window 2: the ADR 0019 addendum, `nemesis-fw-apply`,
  `nemesis-fw-render`. Explicitly flags the counter-agreement comparison (observe-mode
  `nemesis_enforce` counters vs. ufw's own DROP counters over real traffic) as **not done**
  — "the one thing increment 3 exists to prove."
- Window 2 reviews both scripts line-by-line (lockout-failsafe ordering, atomic-replace
  correctness, observe-only-by-construction), finds one non-blocking design gap (`apply`'s
  snapshot always trusts whatever's currently loaded, even if unconfirmed — a double-apply
  before confirming would overwrite the true last-known-good with unproven state), commits
  both as `ac07b22` + `19d9b5c`. Not pushed — held per operator instruction pending this
  handoff.
- Window 2 independently checks in with both windows' live session transcripts (not just
  their self-reported summaries) and live system state (`ufw status`, `fail2ban.log`,
  `audit_log` table, `tailscale debug prefs`, service health) to verify this handoff before
  writing it. Findings folded into `HANDOFF.md`.
