# Worklog — 2026-08-07 (Window 2)

Flight recorder. Minimal prose, chronological, append-as-you-go.

- Morning status run. Baseline `docs/audits/roadmap-state-audit-2026-08-06.md` (8/10/58, 76
  total) confirmed still current — file-count match (76=76) traced and found coincidental
  (baseline commit itself only had 74 files on disk when written; 2 same-day parked additions
  closed the gap) but no real drift. Briefing written `docs/briefing/2026-08-07.md`, mirrored.
- Investigated 2026-08-04→08-07 Tailscale exit-node incident
  (`~/work/nemesis-internal/known-limitations/tailscale-exit-node-persistence-2026-08-07.md`).
  Drafted + added CLAUDE.md Rule 13 (host network changes need a proven revert, prefer VM
  fleet). Committed `5a781cc`, pushed.
- Read-only audit: vpn-dns-guard + firewall/DNS product code for the same unverified-revert
  pattern. Three findings (`core/vpn_dns_guard.py::restore()`, `nemesis_fw_watch.py`
  auto-restore, `nemesis_agent/dns_enforce.py`). Written to private mirror only per Rule 10
  (`~/work/nemesis-internal/audits/vpn-dns-firewall-revert-verification-audit-2026-08-07.md`).
  Operator confirmed: keep private, no public PUNCHLIST entry.
- Untangled Window 1/Window 3 concurrent edits in shared files, hunk-level, verified compile +
  isolation at every step:
  - `5328ef5` — Window 3's connectivity notifier + tests (new files, clean).
  - `8e1729d` — Window 3's `connectivity_episodes` DDL hunk in `database.py`.
  - `ddf3158` — Window 1's `enrollment_tokens` preauth-key columns hunk in `database.py`.
  - `7ac0e56` — Window 1's `vpn_dns_guard.restore()` readback fix (audit Finding 1).
  - `506f049` — Window 3's connectivity-notify wiring in `vpn_dns_guard.py`.
  - `62032db` — Window 1's `vpn_dns_guard --status` bugfix.
  - Pushed through `62032db`, local==origin confirmed.
- Committed Window 1's mint-at-download build (`71ea2fd`) — `tailscale_api.py` retry/revoke
  logic, `dashboard.py` mint-at-download route changes, new `test_tailscale_api_retry.py`.
  Re-ran the 41-check suite myself before staging (handoff claimed 30/30; actual current count
  41/41 — noted, not a discrepancy worth blocking on). Pushed.
- Committed Window 3's DHCP mode-switch fail-over (`e2c55b8` — `modules/dhcp/module.py`,
  `data_manager.py` grant, new `test_dhcp_mode_switch.py`, 78/78 re-run live) and closed the
  diagnostics_watcher connectivity-notify gap that never landed earlier (`7f5f958`, pairs with
  `506f049`). Pushed.
- Committed Track C step 1 (`ccf02aa` — `nemesis_agent/consent.py` + test, 43/43) and step 2
  (`e14e5a4` — `nemesis_agent/conn_events.py` + test, 52/52) as two separate commits — operator
  had referenced both as "step 2" under one private-handoff hash; caught the mismatch (step 1
  is a different private commit, `c73cc32`, different test count) before committing, corrected
  the attribution rather than bundling. Both re-run live before staging. Pushed.
- Still dirty in the public tree, untouched all session (other windows' in-flight work, not
  swept into any commit above): `alert_manager/install_pihole_pwd.sh`,
  `alert_manager/nemesis_fw_watch.py`, `nemesis_agent/dns_enforce.py`,
  `docs/roadmap/venue-guest-network.md` (pre-existing, nobody's), plus untracked
  `scripts/nemesis-pihole-password.sh`.
- **Scoping ADR for a fully AI-managed autonomous operations mode — DONE, private repo.**
  v3+ direction, explicitly undecided per operator, no recommendation either way (Rule 10 —
  private-only, novel/undecided direction). Written to
  `~/work/nemesis-internal/adr/ADR-priv-0001-ai-managed-operations-mode.md`, new `adr/`
  directory (first entry). Private-only numbering track (`ADR-priv-NNNN`), deliberately
  separate from the public sequence (public is at 0021 live / 0022 reserved for QUIC per
  2026-08-06 HANDOFF) so it can never collide.
  - **Key finding, worth remembering:** the L0-L4 graduated-authority scaffold this direction
    would need (`ai_authority`/`ai_proposals`/`ai_standing_rules`,
    `effective_ceiling()` in `modules/ai_engine/module.py:180-336`) **already exists and is
    completely unwired** — verified by grep, zero writes anywhere outside the `CREATE TABLE`
    statements. Same "scaffold with no caller" pattern as the Data Manager's `set_actor()`.
    This is the single most load-bearing fact in the doc — any future pickup of this ADR
    should start there rather than re-deriving it.
  - Covers per operator's 5 points: architecture gap (§1), safety/liability design extending
    `NEMESIS_NEVER_BLOCK` (§2), cost model contrasting continuous polling vs. the
    tiered-explanation deterministic-gate precedent already shipped (§3), infra readiness —
    error-code ledger, Track C schema, Data Manager audit trail — vs. what's missing incl. a
    parallel-risk tie to the memory-injection-detection roadmap doc (§4), explicit
    non-recommendation + open questions (§5).
  - Committed `cc6bbc3` in `~/work/nemesis-internal`, pushed to both `local` and `usb`,
    all three confirmed at the same HEAD.
- Committed Window 1's DHCP live-run fixes (`c064e2b` — `modules/dhcp/module.py` +
  `alert_manager/test_dhcp_module.py`, 83/83 re-run live): lease-file permission
  (`SupplementaryGroups=nemesis-db`), pidfile (`pid-file=`), privilege-drop
  (`user=root`/`group=root` + `CAP_SETGID`/`CAP_SETUID`), and the `set:`→`tag:`
  segmentation fix (previously mechanically inert — tagged clients still got general
  addresses). A 5th defect (`status()` reports "running" during a crash-loop) found but
  explicitly NOT fixed. Operator's task described this as "7 held files"; ground truth
  (Window 1's handoff "MINE — ready to commit (15)" list) shows DHCP is only these 2 files
  — the other 13 are two unrelated feature groups (Track C schema v2, 10 files; Rule 13
  `nemesis_fw_watch.py`/`dns_enforce.py`, 2 files) plus 1 tooling file. Flagged the
  mismatch, scoped the commit to just the DHCP pair as described, left the rest untouched.
  Pushed, local==origin confirmed at `c064e2b`.
- **Still held, not yet committed (from Window 1's 08-07 "MINE" list, all still dirty in
  the tree as of this entry):** Track C schema v2 — `nemesis_agent/conn_events.py`,
  `test_conn_events.py`, `conn_collector.py` (new), `test_conn_collector.py` (new),
  `nemesis_agent/tools/etw_probe.py` (new), `core_module/hw_monitor/test_conn_ingest.py`
  (new), `core_module/hw_monitor/hw_monitor.py` (ingest + consent gate + reaper),
  `alert_manager/database.py` (Track C tables), `alert_manager/data_manager.py`
  (namespace), `dashboard.py` (init call) — 10 files, one feature, not yet split/verified.
  Rule 13 continuation — `alert_manager/nemesis_fw_watch.py`, `nemesis_agent/dns_enforce.py`
  — 2 files (NOTE: these are Window 1's own audit-finding-2/3 fixes referenced in
  `2026-08-07-window1-to-window2-revert-verification-fixes.md`, distinct from the
  `vpn_dns_guard.py` half already committed as `7ac0e56`). Tooling —
  `scripts/nemesis-pihole-password.sh` (new). NOT mine, confirmed do-not-touch:
  `alert_manager/install_pihole_pwd.sh`, `docs/roadmap/venue-guest-network.md`. Window 1
  flagged their own verification as incomplete when this list was written ("I was
  interrupted mid-verification... re-run them") — re-verify test counts live before
  committing any of it, same as every commit above.

