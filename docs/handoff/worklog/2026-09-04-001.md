# Worklog — 2026-09-04 (Window 2)

Raw chronological log, written mid-session ahead of an imminent desktop logout/login cycle —
not a routine closeout. Reconstructed from session actions, not appended live throughout
(same gap as 09-03; flagged again).

- Morning Status: 209,516 LOC, all 8 services active, tree clean. Roadmap baseline (09-02)
  confirmed stale (installer-email-delivery.md shipped, 2 new PARKED stubs). Elevated grants
  re-checked live, unchanged, polkit unreadable 7th consecutive session. Briefing written
  (`docs/briefing/2026-09-04.md`), committed `946c7e5`.
- ADR 0022 number-collision saga, multiple rounds with Window 1 (peer session `nemesis-1f`,
  session `01Ltar8DgRuigjPYuaXAJSEG`), each independently re-verified before acting:
  - Fixed `v2-completion-checklist.md`'s "ADR 0022" (QUIC/Piece-K) → 0031, since 0022 was
    reused for the licensing ADR. Committed `8767aa2`.
  - Window 1 caught my own commit's false claim ("only live citation") — a second citation
    existed at `gateway-mode-scoping.md:13`, missed by their own truncated `head -5` grep.
    Verified the second citation myself, fixed both files, corrected the false claim.
    Committed `5924c87`.
  - Applied Window 1's full corrected V2-completion-gate audit: 7 of 13 items were stale, all
    overstating remaining work. Independently re-verified every fact (file existence, exact
    line numbers, live DB queries, a 10MB eve.json sample) before changing any checkbox: closed
    5 items (rogue-DHCP, DNS-exfiltration, ADR 0019 Inc4, malware Layers C/D, vestigial-tables),
    1 blocker resolved (lateral-movement Tier 1's flow-logging question), 1 partial
    (enrich_ip). Same commit, `5924c87`.
  - Rule-8 pre-push scan found a real leak: my own elevated-grants entry wrote the operator's
    real first name literally instead of the file's own `<user>` placeholder convention.
    Fixed the unpushed instance (`c4fba99`); flagged (not fixed) two already-PUBLISHED
    instances (`468cc46` 09-02, `524aa16` 09-03) — operator's call on a history rewrite for a
    bare first name.
  - Window 1's own new sub-rule (measurement-corroboration) landed in the shared tree as an
    unremarked ancestor of my next commit (`0be03eb`) — flagged explicitly rather than silently
    swept along.
  - **Retraction:** Window 1's "Tier 1 blocker RESOLVED" claim (in the gate correction above)
    was itself wrong — caught by Window 3, verified by me against
    `lateral-movement-outbreak-detection.md`'s own 2026-08-30 revision (flow-event *count* is
    not peer-to-peer *visibility*; the doc had already answered this exact question wrongly
    once before). Restored item 3 to blocked, fixed item 4's phrasing, added a durable-lesson
    note to the source doc. New commit (didn't amend), `b8177ea`.
- Three write-ups handed off from Window 1's launch-readiness audit, each independently
  re-verified live before writing anything:
  1. `nemesis-drift-check.service` had been silently failing 70 consecutive runs since the
     Sep-1 Tailscale snap→native migration (stale `ReadWritePaths` in the deployed unit, not a
     code bug — both generator scripts already packaging-independent). Filed to PUNCHLIST
     (`bc750b3`), messaged Window 3 directly (session `01MzQv1oyAGUkTdwfQLrV6F7`, peer
     `nemesis-f0`) since it's a live security-monitoring gap. **Window 1 redeployed and fixed
     it same day** — independently re-confirmed live (`status=0/SUCCESS`), closed the
     PUNCHLIST entry (`892881c`).
  2. Email security verdict pipeline: verified live that 144/144 scanned messages have
     `verdict=NULL` by design (D9's measurement-first gate, not a bug — two code sites say so
     explicitly). Added a durable status note to ADR 0028 (`3fef8fa`) rather than leaving it
     only in an audit file.
  3. `vulnerability-patch-management.md`: fixed a wrong citation (claimed the appliance's own
     `hw_monitor` as agent-side evidence; corrected to `nemesis_agent/`) and flagged a genuine
     scope contradiction (default-credential checks vs. "no active probing of other devices")
     as an open design decision, not resolved unilaterally (`18206b7`).
- Landed a new CLAUDE.md sub-rule (Window 1's draft, operator-directed, citations verified
  before landing): a text-search check matches the prose describing the pattern it's looking
  for, not just the pattern — three real instances this week, all independently confirmed
  (`b2b9d56`'s docstring/count collision, `ca473d5`'s comment causing a false-positive grep,
  `cf3e00a`'s platform-guard test passing against a mutant because of its own comment).
  Committed `d4e9635`.
- **14-commit push, in stages, with the gate catching set-changes repeatedly:**
  - Confirmed by operator (relayed inaccurately at first via peer, corrected directly): the
    batch grew from 2 to 4 to 10 to 14 commits across several confirmation cycles as Windows 1
    and 3 kept committing to the shared tree while I worked. Each time, re-listed fresh
    immediately before pushing, named every new/unconfirmed commit explicitly (including two
    from Window 1 and one from Window 3 that I hadn't authored), and pushed only the exact
    confirmed SHA rather than the branch tip.
  - Window 1 flagged (relaying an operator hold instruction) that one commit in the batch —
    `d007bf4` (Window 3's licensing/entitlements code) — had no review from either Window 1 or
    2. Reviewed it myself before the operator's final go-ahead: ran its 24-check test suite
    live, checked the security shape (signature-verified only, refuses type coercion, ceiling
    against issuer bugs not forgery).
  - Operator confirmed directly (not via relay) to push the full 14-commit batch. Fresh gate
    check found no further changes. Pushed exact SHA `892881c`. `origin/main` verified at
    `892881c`, zero commits remaining unpushed at that point.
- Told the operator I'm in support role for the rest of today (audits, doc corrections, doc
  work) while Window 1 works the launch-readiness punch list. Messaged Window 1 directly to
  check in rather than waiting to be pinged.
- **Since the last push, 3 more commits landed in `/opt/nemesis` that I have NOT reviewed or
  pushed** (see HANDOFF.md §1 for the exact list) — Window 1's Layer D labeller work and
  Window 3's entitlements doc note. Tree is clean (all committed locally), just unpushed and
  unreviewed by me.
- Committed my own private-repo mirror files (`ea31319`, docs-only, exact-path staged,
  confirmed identical to already-committed public-repo source before staging) —
  **deliberately left unpushed**, since it now sits on top of ~19 other unpushed commits from
  Windows 1/3 in that repo that I haven't reviewed; pushing my SHA would sweep those in.
- This worklog entry + the HANDOFF.md refresh, written now ahead of an imminent logout/login
  cycle per explicit operator instruction — not a routine end-of-day closeout.

## Session resumed post-restart (new Window 2 session, ~17:00 onward)

- Read HANDOFF.md cold, verified its claims against live state (matched exactly). Confirmed
  identity/model, read ARCHITECTURE.md/ADR list per standing read-order.
- Window 1 (`nemesis-37`) deployed `2415cef` (audit-write canary registration) live —
  confirmed clear, verified restart independently (MainPID 4029→43549, `STATE.txt` before-
  values matched). Reviewed `2415cef` myself: Rule-8 clean, compiles, 29/29 tests — but my
  verification ran in a plain shell process, not under the dashboard's actual systemd sandbox,
  which mattered (see below).
- Reviewed and fixed two stale CLAUDE.md entries found cross-window: the core-services
  key-paths line (named `alert_manager/` for all 5 core services; none actually live there,
  moved to `core_module/` 2026-07-29) — `6a38f06`. Extended the 2026-08-26
  diagnostics-self-deception table with 3 new same-day instances (Window 3's draft,
  `f845d03` in the private repo) — `8766baa`. Added a new attribution-signal entry
  (`Claude-Session` trailer is one-directional: same-session-id proves ownership,
  different-session-id proves nothing) after resolving a live ownership dispute over `3269762`
  — `d06afaa`.
- **Production bug found and fixed**: `dashboard.service`'s `ProtectSystem=strict` +
  `PrivateTmp=no` left it with no writable temp dir at all, silently killing 4 diagnostics
  checks (`audit_write_liveness`, `schema_drift`, `dependency_preflight`, `config_drift`)
  since registration — every one "passed" every test because every test ran outside the
  sandbox. Window 1 fixed it (`05f7dc8`, `canary.scratch_dir()` probes rather than infers).
  I verified for real this time: reproduced the exact failure condition (patched
  `tempfile.gettempdir` to raise) and ran a genuine before/after control via a `git worktree`
  at the pre-fix commit — before: error/error/error/error, after: ok/ok/warn/warn, matching
  the claim exactly. Operator later deployed `PrivateTmp=yes` on the unit directly (root fix).
- Window 1 self-corrected an unmeasured claim in `05f7dc8`'s own comment (`os.access(W_OK)`
  behavior under a read-only mount) across two follow-up commits (`9197976`, `44f6252`) after
  Window 3 caught it — kept visible in the comment rather than deleted.
- Reviewed Window 3's `1c1115a` (licence downgrade guard, closes key-pack F13): ran its 46
  tests myself, spot-checked the JSON-only-override security claim directly against the diff,
  read the full private route-security audit rather than trusting its verdict line. Clean.
- Found and logged (not fixed — not my call) a pre-existing, already-public red test:
  `alert_manager/test_roles.py` 157/1, `api_usb_events` (shipped `86a3c83`, 2026-09-02) has no
  `ROUTE_MINIMUMS` entry, fails safe to admin. PUNCHLIST entry added (`14b7320`).
- **Elevated-grants tracking, multiple rounds today** (`eb61c9e`, `dd6cdef`, `f69497b`,
  `72f475f`, `c550323`): flagged the `dashboard` restart-verb NOPASSWD asymmetry (operator
  approved adding it); caught and corrected a wrong approval rationale ("restores watchdog's
  crash-recovery" — false; watchdog runs as `nemesis-watchdog`, never sudo, so a `paul`
  sudoers grant was never relevant); then Window 1's `d3a0c55` settled the underlying
  question further — the polkit rule already authorized `dashboard.service` since
  2026-07-27, so nothing was ever missing. The REAL gap `d3a0c55` fixed: watchdog supervises
  6 units, polkit only authorized 3 (pihole-FTL/clamav-daemon/suricata were silently
  unrestartable). Deployed and verified live by the operator. Also flagged, unresolved:
  an `install.sh` (0755) vs. live (0750) directory-mode discrepancy on
  `/etc/polkit-1/rules.d/`, and a new least-privilege over-grant item (4 services
  polkit-permitted but not supervised by watchdog).
- **Rule 10 disclosure decision, operator-resolved**: `3269762`'s public comment contained
  honest-limitation language about the `MAX_REMOTE_CAP_BONUS` cross-repo invariant. Operator
  decided to trim the public comment and move full detail private. Trimmed publicly
  (`35f3ad7`); full writeup committed to the private repo's `known-limitations/`
  (`e1d15c1`, not pushed — sits atop other windows' unreviewed private-repo commits).
- End state: public repo tree clean, **21 commits unpushed** (`c550323`..`3269762`), all
  reviewed by me, awaiting final operator go-ahead to push. Full list in HANDOFF.md §1.
