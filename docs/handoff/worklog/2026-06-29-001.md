# Worklog 2026-06-29-001

Flight recorder — terse, append-as-completed.

- Morning status: 30,210 LOC; tree clean; all 6 services active (dashboard, watchdog,
  alert-watcher, malware-canary, diagnostics-watcher, vpn-dns-guard). Verified ADR 0009 exists
  + complete (1893ae8). Confirmed Morning Status block already at top of CLAUDE.md (lines 10–40)
  — no move needed.
- Daily Status Report (`/api/report/daily`): read-only audit + plan produced (4 Explore agents
  mapped module contract, data sources, AI/email/scheduling, roadmap ties). Decisions locked:
  HTML+text now / PDF deferred; in-process APScheduler; Intermediate tier admin-configurable.
  **PARKED — awaiting go-ahead before any code.**
- PC Agent v1 ping monitor: read-only audit (4 Explore agents: agent arch, server ingestion +
  auth hook, fleet/timeline UI, reusable probe + roadmap constraints). Key findings: agent is
  stateless (no SQLite today); `/hw_data` device-auth live via `_agent_approved()`;
  `agent_devices` has no `actor` column; Chart.js 4.4.1 already wired for per-device timelines;
  no ICMP/traceroute/TTL code exists (build per-platform); samples dropped on POST failure today.
  Decisions: draft ADR / v1 core scope (defer traceroute + narrative + relay) / build after
  trip-readiness.
- Drafted `docs/architecture/0010-agent-ping-monitor.md` (Proposed, design-only, no code).
  Rule-8 leak scan: CLEAN (only intentional public resolver IPs 8.8.8.8 / 1.1.1.1 /
  208.67.222.222). Committed + pushed with the HANDOFF Pointers line for ADR 0010
  (same pattern as 0009). Daily report plan stays PARKED — no build until after
  pre-enrollment scan + Windows smoke test.
- Capture-only (docs, no build): created `docs/roadmap/community-reporter-identity.md`
  (free-tier key, reporter-ID derivation, ZKP-adjacent challenge-response verification, trust/
  rate-limit/abuse, three-pass sanitization) + appended 4 PUNCHLIST captures (community reporter
  identity v1.1, daily status report v2, PC agent UI + traffic readout v2, ZTNA+NAC v2).
  Rule-8 scan CLEAN (placeholders + public-resolver/RFC1918 references only). One commit, pushed.
- Capture-only (docs, no build): extended `community-reporter-identity.md` with
  "Geographic Anchoring & Compounding Verification" — extended ping pool (~10-15 regional
  targets), geographic anchor in the identity record, speed-of-light latency plausibility check
  (no GPS/IP lookup/disclosure), four compounding verification dimensions (HMAC + geo + temporal
  + behavioral). Reconciled the earlier "1 of 4" illustration. Rule-8 CLEAN (regions only, no
  IPs). One commit, pushed.
- Capture-only (docs, no build): appended "COMMUNITY BACKEND — PRE-BUILD DESIGN REQUIREMENTS"
  to PUNCHLIST (must-be-locked list, needs-design-sessions list, legal review before Phase 4,
  4-phase build sequence — Phase 1 identity layer can ship with v1.1). Rule-8 CLEAN. One commit,
  pushed.
- Capture-only (docs, no build): created `docs/roadmap/community-signal-dedup.md` — community
  backend data model: one-entry-per-unique-signal (SHA256(signal_type:signal_value) UNIQUE),
  bounded timestamp aggregates (100 recent / 168h / 90d / 24mo), computed+decaying confidence
  score, local-raw vs global-sanitized times_seen, lean DB-grows-with-threats sizing. Cross-linked
  to identity + open-source-feeds + PUNCHLIST Phase 2. Rule-8 CLEAN (illustrative numbers only).
  One commit, pushed.
- Read-only capture audit: 7/10 fully captured, 2 partial (tunnel time-based switching missing;
  signal-dedup + ADR 0010 had no PUNCHLIST entry). Then applied the 3 PUNCHLIST additions:
  (1) time-based tunnel switching line, (2) COMMUNITY SIGNAL DEDUPLICATION entry → dedup doc,
  (3) "Agent ping monitor — v1 (ADR 0010)" checkbox entry (matches ADR 0007/0008 style).
  Rule-8 CLEAN. One commit, pushed.
- Capture-only (docs, no build): added "SQLite longevity note" section to ADR 0006 — dedup keeps
  backend lean (grows with threat landscape not user count), ~730MB at 100K installs/5yr, queue→
  dedup-worker prevents write contention, PostgreSQL migration path exists but not anticipated.
  Cross-links community-signal-dedup.md. Rule-8 CLEAN. One commit, pushed.
- BUILD (HANDOFF resume #1): pre-enrollment scan (scan-before-trust). Backup first (Rule 6):
  alerts.db .backup → scratchpad/backups/, integrity_check=ok on source+copy, sha256 recorded;
  agent_devices had 0 rows. 4 files: nemesis_agent/enrollment.py (pre_enrollment_scan() ClamAV+
  YARA over generic roots, 300s cap, skip-graceful; added to enroll payload), hw_monitor.py
  (agent_devices CREATE + guarded migration: pre_enrollment_scan TEXT + enrollment_has_findings
  INT; _create_enrollment parses scan → pending_with_findings when findings, scan summary in
  ticket, HIGH priority if findings), dashboard.py (_render_agent_devices_html scan badge +
  Approve-anyway/View-findings/not-available states), static/agent-enroll.js (agentApproveAnyway
  warning). DEVIATION: migration placed in hw_monitor.py NOT database.py — agent_devices DDL is
  canonical there (CLAUDE.md: DDL in one init); database.py never references agent_devices.
  VERIFY (real output): py_compile all 4 + node --check JS OK; functional suite ALL PASS (scan
  clean/EICAR-findings/not_available, payload includes scan, migration adds both cols +
  idempotent, _create_enrollment status logic + bad-sig still rejected); render suite ALL PASS
  (3 states). FINDING: live agent_devices still at OLD schema (lacks whole enrollment layer +
  new cols) — guarded migration adds all on hw-monitor restart. Removed sensitive test residue
  (keys/, .conf) before commit; Rule-8 diff CLEAN. NOT deployed (no service restart) per STOP.
- Follow-ups: .gitignore now covers nemesis_agent/keys/ + *.conf (agent secrets, runtime-
  generated). PUNCHLIST note added: YARA rules not shipped yet (yara_rules/rules.yar absent →
  YARA always not_available; ClamAV unaffected) — acceptable v1, ship baseline ruleset before
  commercial. Rule-8 CLEAN. One commit, pushed.
- Capture-only (docs, no build): created docs/roadmap/sandbox-first-software-testing.md —
  test new software in VM sandbox before real-system run; trigger on new Downloads/Desktop exe,
  clean-snapshot → run → AI behavioral report (tiered) → user approve/reject; sandbox_verified
  top of trust hierarchy; NMS-SAND-{reporter_id}-{date} certificate in scan_cache. Cross-links
  VM Lab + reporter-identity + community feed. Rule-8 CLEAN. One commit, pushed.
- Capture session (docs, no build) — software-lifecycle + support arc. Audit-first on the
  pasted "software lifecycle" spec (no software_inventory/scan_cache today; greenfield).
  Then a run of capture-only commits, each: doc + PUNCHLIST, Rule-8 scan CLEAN, one commit, pushed:
  - `docs/roadmap/malware-detection-pipeline.md` — full pipeline (certification scan, first-run
    hash cache, 4-tier validation, triggers, gaming, clone sandbox + canary trap, sandbox-first
    install, software lifecycle/manifest/cert-chain, stale-app monthly report). V1/V2/V3 seq. (5f6a5ea)
  - `docs/roadmap/support-bundle.md` — one-click "I need help" → sanitized pre-diagnosed bundle,
    four destinations. Flagged registry-backup + private-support-intake as undesigned prereqs. (d2bbec7)
  - `product-thesis-built-in-it-expertise.md` — "SMB Software Support" accountability-layer
    section (cert + registry diff = provable evidence vendors can't dismiss). (71f2f74)
  - `docs/roadmap/verified-partner-program.md` — B2B revenue stream, NMS-CERT verification API,
    aggregate-only analytics, tiers; legal folded into existing community-feed legal bucket. (521a786)
  - Read-only capture audit (4 Explore agents over 39 items): 31 ✅ / 5 ⚠️ / 3 ❌. Real gap =
    sandbox→system migration cluster (11-15). Report saved (this commit).
  - `docs/roadmap/pre-escalation-support-search.md` — AI searches for an existing fix before
    ticketing (community→vendor KB→forums→web); self-building community KB; CUSTOM_VENDOR_SEARCH
    pattern. Flagged outbound-query Rule-8 gate (query, not just bundle). (2befa7e)
  - `docs/roadmap/sandbox-to-system-migration.md` — closes audit gap 11-15: single-install
    promotion, registry backup/diff engine, path rewriting, Linux/dpkg migration, OS/driver
    update sandboxing w/ CVE cross-ref. (9eb617c)
  - `docs/roadmap/ai-generated-tutorial-walkthrough.md` — v2, always-current tiered tutorial
    generated from docs; generation IS the doc-completeness audit. Captured thin-corpus reality
    (only diagnostics documented). (f5a180f) + addendum: first-run baseline, searchable
    tutorial_index table (NL vocab→feature, ADR-0001 prefix flagged), connected "show me"
    dashboard (live DOM highlight, not screenshots). (b9b431e)
  - `docs/roadmap/three-snapshot-vendor-package.md` — pre-install / issue-state / delta
    (smoking gun) + rebuild scripts; proves release regression. Flagged memory-state
    sanitization as highest-risk Rule-8 surface (dump can hold creds — narrow or scrub before
    any commercial export). (d6aae78)
- Closeout: committed the read-only capture audit `docs/audits/roadmap-capture-audit-2026-06-29.md`
  (sanitized, repo-relative paths) + this worklog. Referenced by 4 committed docs.
- Windows/agent enrollment SMOKE TEST (HANDOFF resume #2) — VM test-user@<test-vm-ip> (Linux,
  not Windows) → live box <box-ip>. Found + fixed TWO deployment blockers, then 8/8 pass:
  - Blocker A (firewall): install.sh opened 5001 only under INSTALL_MODE=windows_vm; standard
    install never opened it → LAN agents got ufw-default-deny TCP timeout. FIX 1: added always-on
    subnet-scoped `ufw allow ... port 5001` (uses $DETECTED_SUBNET, Rule-8). Live rule applied
    (/24). Commit c37a177. Verified VM→5001 now OPEN.
  - Blocker B (stale service): live hw-monitor running pre-enrollment code (started Jun 27;
    enrollment endpoints landed after) → GET /enrollment_status = 501. FIX 2: user ran
    `sudo systemctl restart hw-monitor` (passwordless sudo not allowed for systemctl on prod).
    Verified 501→200; restart also ran the guarded agent_devices migration.
  - Smoke 8/8: (1) pull 63da311 (2) pre_enrollment_scan OK structure/no-paths (3) ensure_keypair
    451-char PEM (4) enroll() → device_id 8cca1fc5…fc30, pending (5) DB row pending + scan stored +
    migration cols present (6) USER approved in browser → approved (7) /hw_data heartbeat (source=
    nemesis_agent) → 200 {"ok":true}, agent_last_seen set, ip <test-vm-ip> (NOT not_approved)
    (8) cleanup: Rule-6 backup alerts-PRE-SMOKE-CLEANUP-20260629.db (integrity ok) → scoped DELETE
    agent_devices+hw_metrics (1+1, 0 remain) → VM keypair files deleted. API-name adaptations noted
    (generate_keypair→ensure_keypair; build_enrollment_payload→enroll(conf)).
- FINDING (port redirect, action deferred): box topology is nginx :80 (LAN-reachable, Basic-auth,
  proxy_set_header Host $host → no port) → Flask :5000 (ufw-BLOCKED from LAN). The proposed Flask
  before_request "host missing :5000 → 301 :5000" would fire on EVERY nginx-proxied request and
  bounce all users to a firewall-blocked port = dashboard outage. NOT implemented; needs redesign
  (belongs at nginx layer, must respect that :5000 is internal). Audit-first caught a trip-breaker.
- Login issue (audit/test, no code change): <owner-login> creds CORRECT — bcrypt.checkpw True, no
  lockout; end-to-end POST /login → 302 success + /settings 200 via curl. Zero login_events from
  user's attempts → client-side (stale cookie/cache, autofill old pw, or network path to the box).
  Single healthy proc on :5000, secret key stable. Recommended incognito retry. Disclosed: test
  wrote one success login_event + last_login. Password never persisted (Rule 8).
- Capture-only (docs, no build): YARA false-positive known-good PATH exclusions. AUDIT FINDING:
  YARA is LIVE in malware_detection (6 rule files, _yara_scan working) — not dormant; only the
  agent pre_enrollment_scan lacks rules. Added exclusion section to malware-detection-pipeline.md
  (browser extension dirs, browser/Electron caches, service-worker caches, ad-blocker rulesets =
  malicious domains by design) as Tier-1 known-good PATH list; + PUNCHLIST build-candidate entry
  (live scanner has no path exclusions; needs Rule-6 backup + tests when built) and clarified the
  agent-specific YARA note. Rule-8 CLEAN (~ placeholders, no real paths). One commit, pushed.
- BUILD: YARA known-good path exclusions on the LIVE malware_detection scanner. Rule-6 backup
  alerts-PRE-YARA-EXCLUSIONS-20260629.db (integrity ok, sha256 recorded). One file:
  modules/malware_detection/module.py — config-driven exclusions (/etc/nemesis-yara-exclusions.conf
  with baked-in defaults), _load_exclusions (mtime hot-reload) + _is_excluded (glob/prefix) +
  _install_sighup_reload (SIGHUP force-reload, chains only to custom prior handler, never SIG_DFL);
  _yara_scan guard (skip excluded before match) + scan_directory prunes excluded dirs/files and
  reports excluded_known_good. Chose mtime auto-reload as the robust primary + SIGHUP as bonus
  (signal reg guarded for non-main-thread). VERIFY (real output): py_compile OK; suite ALL PASS —
  9 matcher unit tests vs shipped defaults + 4 EICAR/miner integration tests (EICAR excluded-path
  skipped, EICAR normal-path detected, uBlock0 ruleset excluded, XMRig sig detected), real Chrome
  Extensions path verifies excluded. Tests wrote only to a temp DB copy. Rule-8 CLEAN. NOT deployed
  (running process needs restart to load). One commit, pushed.
- DEPLOY (YARA exclusions): restarted dashboard via sanctioned NOPASSWD rule
  (sudo -n /usr/bin/systemctl restart dashboard) — scanner runs IN the dashboard process (no
  separate scanner service; malware-canary loads the module but only for Layer-B canary). Now
  active, fresh timestamp 13:48:59, new PID. VERIFY (real output, temp DB, alerts no-op'd):
  (1) deployed code _load_exclusions() → 14 patterns from defaults; (2) scan_directory on the real
  ~/.config/google-chrome/Default/Extensions (prior 24-FP source) → files_scanned=0,
  excluded_known_good=7, findings_total=0; (3) parent ~/.config/google-chrome prune pass →
  excluded_known_good=2, findings=0. nemesis.malware logger not wired to journald (pre-existing) —
  startup-load verified via deployed code path instead. Starter /etc config staged in scratchpad;
  /etc write needs interactive sudo → left for owner to install (baked-in defaults already active,
  so functionally complete). malware-canary/device-scanner still on old module code but unaffected
  (they don't use the Layer-A scan/_yara_scan exclusion path).
- Capture-only (docs, no build): created docs/roadmap/hardware-stable-identifiers.md — hardware
  IDs that survive MAC randomization / IP change / OS reinstall (per-platform signals;
  battery serial as the standout; SHA256 composite stable_id, hash-before-store, signals_used
  types-only). Rides in agent enrollment payload alongside public_key; strongest impossible-travel
  signal. Cross-links device-identification, ADR 0008, support-bundle, community-reporter-identity
  (MAC-randomization connection points at device-identification.md — no separate doc). Rule-8 CLEAN.
  One commit, pushed.
- BUILD: Windows self-installer (token auto-approve + dashboard generator + GitHub Actions exe
  pipeline + tkinter GUI). Rule-6 backup alerts-PRE-INSTALLER-TOKENS-20260629.db (integrity ok).
  Parts 1+2 (Linux-verifiable): database.py init_enrollment_tokens_table (core table, ADR 0001);
  dashboard.py POST /api/agent/installer/generate (single-use 24h auto-approve token) + PUBLIC
  GET /install/windows/<token> (serves pre-baked .ps1) + /exe redirect (env NEMESIS_GH_REPO, no
  slug hardcoded) + "Generate Windows Installer" UI + agent-enroll.js genWindowsInstaller();
  hw_monitor _create_enrollment atomic single-use token claim → enroll_status=approved + enrolled_by,
  falls back to pending on any failure; config.py + enrollment.py carry enrollment_token in payload.
  Parts 3+4 (Windows-build-host, syntax-only here): nemesis_agent/build_installer.py (PyInstaller
  driver), installer_gui.py (tkinter guided install), .github/workflows/build-windows-agent.yml
  (windows-latest, push+dispatch, artifact + release). VERIFY: py_compile 7 files OK; YAML OK; JS OK;
  functional suite ALL PASS — token matrix (valid→approved+use-claimed+enrolled_by; reused/expired/
  revoked/unknown/none→pending), generate route→DB row, public .ps1 download bakes server/token/hint
  + doesn't consume the use, invalid→410, exe→503 graceful. Rule-8 CLEAN (server from request.host/
  env, repo from env — no IP/token/account hardcoded). Pushing triggers the Windows CI build.
- FIX (ci): Windows agent workflow only triggered on `push: branches:[main]`, so Paul's v1.0.1
  tag built nothing and created no release → /exe redirect (releases/latest/download) had no asset.
  Added `tags: ['v*']` and dropped the `paths:` filter (paths+tags skips tag pushes — empty diff).
  v1.0.1 points at the pre-fix commit (can't build); tagged v1.0.2 at the fix commit to trigger the
  first release-producing build. YAML validated.
- DOCS: three-tier Windows install guides in docs/operation/ (ship alongside the installer):
  INSTALL_WINDOWS_BEGINNER.md (🟢 friendly, double-click→UAC→done), INTERMEDIATE.md (🔵 what/why,
  requirements, verify via Task Scheduler + dashboard, troubleshooting, uninstall), PRO.md (⚙️
  architecture, 3 install methods, token system, %APPDATA%\Nemesis files, logon-task registration,
  /enroll + /hw_data API). Grounded in the real build. DEVIATION (accuracy): dropped the
  "system-tray shield icon" verification line from the spec — the agent has no tray icon (runs as
  a headless logon task); documented the real completion signal (installer "Done!" + dashboard
  last-seen / Task Scheduler) instead. Rule-8 CLEAN (placeholders, no real IPs/emails/paths).
- DOCS+FEAT: Tailscale prerequisite added to all 3 install tiers (BEGINNER Step 0, INTERMEDIATE
  Prerequisites, PRO Prerequisites — verbatim per spec) + bundle docs with exe. Option C: inline
  beginner steps (incl. Tailscale-first) on installer_gui.py first screen. Option A: workflow
  bundles exe + 3 renamed docs (START_HERE_BEGINNER/INSTALL_INTERMEDIATE/INSTALL_PRO) into
  NemesisAgent-{version}-Windows.zip — uploaded as artifact every build, attached to release on
  tags. VERIFY: py_compile GUI OK; YAML OK; tailscale present in all 3 tiers (6/8/8); zip step
  present. Rule-8 CLEAN (placeholders, public tailscale.com URLs, literal tskey-auth-xxxxx example).
  Push triggers a CI build that produces the zip artifact.
- FIX (installer UX, one commit): (1) added GET /install/windows/<token>/zip → 302 to
  releases/latest/download/NemesisAgent-v{NEMESIS_AGENT_VERSION}-Windows.zip (env-gated, 410
  invalid / 503 if repo+version unset); added to _AUTH_EXEMPT. (2) generated links now use
  NEMESIS_PUBLIC_URL (nginx :80) instead of the internal Flask :5000 (falls back to request host).
  (3) copy button fixed — clipboard API with hidden-textarea/execCommand fallback for plain-HTTP
  LAN (navigator.clipboard needs a secure context). (4+5) UI defaults to the /zip link as the
  prominent primary (readonly field + 📋 Copy link copying the zip URL); .exe/.ps1 demoted to small
  "advanced" links. VERIFY: py_compile + JS OK; functional ALL PASS (/zip 302→versioned zip, 410,
  503; links use public URL not :5000; zip_url primary). Rule-8 CLEAN (all env-driven). NEW env the
  admin sets: NEMESIS_AGENT_VERSION, NEMESIS_PUBLIC_URL. /zip resolves once a tag ≥ the zip-workflow
  commit is released (v1.0.2 has no zip asset yet).
- Afternoon captures (docs, no build) — each: doc + PUNCHLIST, Rule-8 CLEAN, one commit, pushed:
  device-identification.md (passive mDNS + on-demand active fingerprint -> AI name suggestion;
  built on existing devices table + nmap scanner) (7346a28); MAC randomization + hardware stable
  IDs (correlation engine, battery-serial composite stable_id, hash-before-store; ADR 0008 tie-in)
  (3fa8b1a/7151bb8); installer-email-delivery.md (v2, mostly wiring existing enrollment_tokens +
  send_email + /zip) (doc rode concurrent commit 0a40f4c; PUNCHLIST 32d3ac0).
- Rule-8 PUBLIC-REPO LEAK audit + scrub (repo is public; concurrent-session installer work
  exposed gaps). 4 commits, pull-first + leak-scan each: (1) PII in test_anomaly_sim.py (real
  device names + LAN IPs -> placeholders) d63cb81; (2) handoff docs (box/tailnet IP, project
  account -> placeholders, 6 files) 6c24fe9; (3) hardcoded box IP/subnet in shipped code ->
  env-driven (PIHOLE_IP, LAN_SUBNET; dhcp defaults generic) d0be3d5; (4) CLAUDE.md handoff
  leak-scan rule + OPERATION.md nginx-entrypoint + PUNCHLIST port-canon note 10902ed. HEAD
  verified clean of real infra. History NOT rewritten (decision: others may have cloned).
- Windows agent UX — 3 commits, pull-first + compile/leak-scan each: uninstall_windows.ps1
  (removes NemesisAgent+NemesisLHM tasks, kills procs, best-effort server notify, removes our
  Defender exclusion, deletes %APPDATA%\Nemesis; never touches Tailscale) 1b3aba3; installer_gui.py
  Tailscale 5-state handling + retry loop + State-5 enroll-fail hint c58b7f7; /api/health endpoint
  (env-driven version) + _AUTH_EXEMPT + nginx auth-bypass template + installer reachability check
  b3b3a5e. DEPLOY-then-VERIFY (owner): env vars set, nginx bypass applied + /api/health 200.
- VERIFIED on real hardware (owner, other window): first NON-DEVELOPER enrollment via token
  installer (family member laptop) -> proved non-technical install flow; /api/health 200 live;
  NEMESIS_AGENT_VERSION bumped to 1.0.6; link_type detection in progress.
- CLOSEOUT: HANDOFF.md refreshed (3 deploy gates -> resolved; v1.0.6); PUNCHLIST installer-size
  item; supplement 2026-06-29-001 written. Names sanitized to placeholders per the Rule-8
  handoff rule committed today (10902ed). One commit, pull-first.
- link_type detection: all platforms, agent enroll + heartbeat, agent_devices migration, dashboard render (b3146fe)
