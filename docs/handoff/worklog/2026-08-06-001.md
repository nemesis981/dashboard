# Worklog — 2026-08-06 (Window 2)

Raw chronological log, reconstructed from `git log` mid-session (power-risk framing —
storm/battery backup, see HANDOFF.md's cold-start summary). Minimal prose, no curation;
distilled into HANDOFF.md's "what shipped" section separately.

- 07:17 `ce239a0` — roadmap-state audit refresh, baseline for 2026-08-06.
- 07:50–07:53 `0ec1e05`,`0619c82`,`a4faca4`,`9fb1239`,`a83e58c` — 5 PUNCHLIST/roadmap
  docs commits (QUIC ADR-needed, agent jitter, empty-alert-list, 2 parked roadmap stubs).
- 07:53 `0115fd0` — test_quarantine.py redirect/auth/POST fix, 35/35. Verified live.
- 07:53 `9a7b2d6` — audit_log.ts dual-format bug filed.
- 07:59 `e67c996` — test_quarantine five-week-red record correction.
- 08:14 `85813d0` — nemesis_timestamp helper added. 38/38, verified live.
- 08:14 `7da46f2` — all four audit_log.ts writers wired to the helper. 33/33 degraded_ingest.
- 08:15 `dc6c16c` — Layer D honesty fix, malware_detection module.
- 08:15 `e5a9a94` — SYN-sweep rate-vs-sweep PUNCHLIST entry filed.
- 08:38 `710620b` — analyze_alert missing-action fix. 35/35.
- 08:46 `52a9141` — local.rules versioned (net-new), service-port exclusion. 15/15 live vs
  Suricata 8.0.3.
- 09:07 `96e6736` — self-scan source-exclusion fix. 24/24 live.
- 09:07 `cc0fe33` — deploy-suricata-rules.sh added. Dry-run verified live.
- 09:07 `46e7ea8` — install.sh wired to deploy rules on fresh installs.
- 09:07 `7108d54` — HANDOFF.md: nemesis-suricata-rules sudoers grant documented. Verified
  against live `sudo -n -l` output.
- 09:13 `da53996`,`323014c` — 2 more PUNCHLIST entries (install.sh interface bug, rule
  naming vs scope).
- 09:40 `0469678` — tiered AI explanations. 32/32.
- 09:40 `1f75ae6` — chat widget unpin/float feature.
- 10:00 `773fb60` — fa52626 PUNCHLIST entry corrected backlog→done. This is the FINAL
  hash: the original commit (`7fc9d67`, since abandoned) cited the wrong commit hash in
  its own prose (`5330126` instead of `5330220`); amended in place before push per
  explicit operator instruction, since it was still unpushed at the time.
- 10:05 `e0bc3da` — device-categorization Phase 1. 56/56 (later 62/62 after vendor adds).
- 10:12 `2ee25a2` — migrate_device_categories.py added (already applied to live DB,
  verified via PRAGMA table_info + row count: 41/41 vendor-populated).
- 10:12 `c60a9ef` — device re-identification PUNCHLIST entry (assessed, rejected).
- 10:37 `6027f3c` — Sony/Select Comfort/Microsoft vendor additions. 62/62.
- 10:37 `7204331` — suricata sid:1000003 rev bump.
- 11:31 `0ee0c57` — shared alert-modal extraction, fixes /firewall-db duplicate-dashboard
  bug. 26/26, plus regression re-run of test_analyze_alert_body (35/35) and
  test_tiered_explanations (32/32).
- [gap — see HANDOFF §1 for context: dashboard restarted 11:34, watchdog/alert-watcher
  restarted 13:18 by Window 1 for unrelated staleness found independently]
- 14:38 `07985b7` — CLAUDE.md VM-rename fix (HW-GAUGE → Gateway), flagged by Window 1.
- 14:38 `13239ea` — chat-popup false-bug-report PUNCHLIST closure.
- ~14:35–14:50 — **storm/battery-backup warning received.** Read Window 1's and Window 3's
  live 2026-08-06 handoff files in `~/work/nemesis-internal/handoff/` to establish full
  cross-window state (gateway-zone VM work, reassignment history, verification evidence)
  before writing.
- 14:44 `f624413` — HANDOFF.md rewritten with power-risk cold-start summary leading the
  file. Mirrored to `~/work/nemesis-internal/handoff/HANDOFF.md`.
- This worklog file — written and about to be mirrored.

## Verification instrument note (worth keeping)

While checking the post-restart dashboard journal for errors, an early version of the
check captured `$?` from the wrong command (an intervening `echo`, not the actual
`grep`/`head` pipeline) — always reported success regardless of the real result. Caught
before trusting it; redone as a direct `grep -c` count. Same "instrument that can only
say yes" class this repo already tracks as a standing practice.

## Continued — 14:54 onward

- 14:54 `20f2769` — worklog file (this one) committed and mirrored.
- 16:45 `3f4b933` — ADR 0001: reserved `error_*` as core-owned, at Window 1's request
  ahead of their error-code-system build.
- 16:47 `ea15717` — HANDOFF refresh after the ADR change.
- 16:56–16:57 `35d3660`,`2a24803`,`540e224`,`18513c5` — nemesis_errors.py tier-1 build
  (57/57); QUIC static-policy block artifacts (.nft/.service/verify script);
  deploy-quic-block.sh; install.sh wiring. All Window 1's held work, landed together.
- 16:59 `d2183c9` — HANDOFF refresh.
- 17:06–17:07 `aa6916c`,`db9e0c4` — careful two-window staging: Window 3's DHCP module
  rewrite (56/56) and Window 1's initial error-code-system wiring (5 paths), both with
  Window 1/3 uncommitted work live in the same tree simultaneously. Each isolation
  verified via `git show --stat` before push.
- 17:09 `e4c467f` — HANDOFF refresh recording the two-window staging pattern.
- 17:21 `272db73` — error-code-system round 2: `make_recorder()` generalizes the
  tickets-module pilot; 4 new retrofit sites (dashboard.py x2, hw_monitor.py,
  watchdog.py); real `devices` schema-drift fix + `reconcile_dhcp_hostnames()` in
  database.py. 73/73. Same careful staging (dhcp files live in tree throughout).
- 17:22 `d026a34` — HANDOFF refresh.
- **17:35–18:11 — dhcp module review, the session's most consequential finding.**
  Window 1 reported the DHCP module wired to a background sync thread per their own
  recommendation, 78/78 passing. Full review surfaced a real gap: `data_manager.py`'s
  `"dhcp": ("dhcp_leases",)` grant was commented "EXPLICIT table, not a prefix grant"
  but `allowed()` treats a plain tuple as a PREFIX match — demonstrated live,
  `dm.allowed('dhcp', 'dhcp_leases_archive')` returned `True` for a table that doesn't
  exist. Worse: the new test's check for this property grepped source text rather than
  calling `allowed()`, so it would have passed regardless. Not exploitable that moment
  (no second `dhcp_`-prefixed table exists), but flagged before commit rather than
  after, given the overnight run. Held all 4 files (`data_manager.py`, `module.py`,
  `manifest.json`, `test_dhcp_module.py`) and filed `9a52dd5` (URGENT PUNCHLIST entry)
  to relay the finding — Window 2 doesn't edit code content, so this was the available
  channel, not a direct message.
- 18:14 `a184721` — separately (unrelated content, same request): fixed
  `devices.hostname` never reaching `nemesis_device_category`'s iOS branch —
  `get_network_devices()` never selected the column into the dict `classify()` reads.
  New AST-based pipeline-contract test (67/67) so a future new signal is covered
  automatically.
- 18:14 `864ef1f` — vestigial-tables PUNCHLIST entry (Window 1 finding, unrelated to the
  hostname fix, committed separately).
- 18:16 `266dc01` — HANDOFF refresh surfacing the held dhcp item as its own top-level
  section.
- **18:21 `2f2d9e9` — dhcp module landed.** Window 1 fixed the grant to
  `{"tables": ("dhcp_leases",)}` (dict form, exact-match, matches `integrity_watch`) and
  rewrote the test to call `allowed()` directly. Re-verified independently before
  committing, not just trusted: `dm.allowed('dhcp', 'dhcp_leases_archive')` now `False`.
  Confirmed no regression to `integrity_watch` or the tuple-prefix modules. 81/81 live.
- 18:22 `062727f` — HANDOFF refresh recording the resolution.
- **18:30 `079a33e` — table-identifier case-sensitivity fix**, found by Window 1 while
  verifying the dhcp grant fix: `allowed()`/`allowed_columns()` lowercased only the
  grant side, so `INSERT INTO DHCP_LEASES` matched neither the exact-table set nor the
  prefix path and was DENIED — fail-closed (never permits, only denies) but LIVE, not
  latent, since every namespace resolves to `MODE_ENFORCE`. Fixed at `_ident()`, the
  single funnel every table token passes through. Verified before committing: all 4
  affected suites green (`data_manager` ALL PASS, `dhcp` 81/81, `nemesis_errors` 73/73,
  `nemesis_device_category` 67/67).
- 18:32 `14e89e9` — HANDOFF refresh.
- ~18:35 — Window 1 reported clear (no more commits expected). Interim closeout health
  check run at operator request: clean, synced, leak-free, open items captured (121
  unchecked PUNCHLIST items, all durable). Not a full closeout — worklog/supplement
  catch-up deliberately deferred until Window 1 confirmed fully done.
- 18:52 `a185b3e` — Window 3's held finding: Windows DHCP hostnames truncate at 15
  characters (NetBIOS limit), observed on the gateway test zone with a same-segment
  Linux-client control ruling out the DHCP server/lease file as the cause. Nothing
  broken today (only the short iOS hint list reads hostname); flagged as a future hazard
  for anything doing hostname equality/joins.
- This entry — worklog caught up to present, full closeout proceeding.
