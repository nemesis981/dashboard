# Worklog — 2026-06-28-001

> Raw chronological flight recorder. Append-as-you-go, terse, no curation.
> Distilled into the session supplement at closeout.

- Session start: read CLAUDE.md, HANDOFF.md, supplement 2026-06-27-001, worklog
  2026-06-27-001, ADR 0004, ADR 0005. Confirmed current. Resume point per HANDOFF:
  Starlink SSH test #1 (gated).
- Starlink DELAYED → pivot to buildable threads ("build to spec until it arrives").
  User chose: Diagnostics connectivity watcher, spec-first then build.
- Grounding gathered: roadmap stub (diagnostics-connectivity-watcher-tool), the
  throwaway watcher (~/work/vpn-watcher/vpn-watch.sh, 137 MB logs/day at 3s),
  classification + standalone-runner roadmap docs, the canary self-gating pattern
  (malware_canary.py + malware-canary.service), module contract + settings, install.sh
  svc_names + uninstall.sh SERVICES.
- SPEC written: docs/specs/diagnostics-connectivity-watcher.md (DRAFT for review).
  New modules/diagnostics/ module + standalone service mirroring the canary; Rule-8
  flat-file(raw)/DB(sanitized) split; is-it-me-or-them classifier; quiet-continuous vs
  opt-in-verbose cadence + rotation; new diagnostics_* ADR-0001 prefix; 4-pass build plan;
  4 open questions. STOPPED for review (no code changed).
- Spec review: user accepted all 4 recommendations. §13 marked RESOLVED
  (/var/log/nemesis/diagnostics, PIA optional plugin, both retention caps, no new ADR).
- Pass 0 — module skeleton. Built modules/diagnostics/ (manifest + module.py:
  DEFAULT_SETTINGS, _conn, _init_db with 3 diagnostics_* tables, _get/_set_setting,
  contract stubs — card/routes None until Pass 2, no service/probes). ADR 0001 prefix
  list updated to add diagnostics_*.
  VERIFIED (real output): _init_db creates diagnostics_connectivity_samples /
  _status / _settings (+ ts index), idempotent on re-run, 10 settings seeded,
  get/set round-trip OK, status CHECK(id=1) enforced, leak-scan clean (only the
  intentional universal 1.1.1.1 default). STOPPED for review (not committed — awaiting go).
- Pass 0 COMMITTED + PUSHED: 2e4f3e3 (eac517b..2e4f3e3 main). Leak-scan clean.
- Pass 1 — probe library. Built modules/diagnostics/watcher.py: 6 probes (VPN/piactl
  optional, tunnel iface, routing, DNS + ADR-0005 loopback-refusal dig probe, raw egress
  by IP, KEYTEST default/-4/-6), is-it-me-or-them classifier (ALL_OK/DEGRADED/
  UPSTREAM_FAIL/LOCAL_FAIL), flat-file writer (verbose=full blocks / quiet=1 summary line,
  size-rotate + age-prune), sanitized DB sampler (row-count cap), verbose time-box
  auto-revert. Observe-only (no system changes). Hand-runnable via __main__.
  VERIFIED (real output): live probe of this box = ALL_OK lat 19.6ms (VPN-off);
  classifier returns all 4 verdicts correctly; DB sample+status rows written sanitized;
  flat log carries raw KEYTEST detail; RULE-8 grep of sample/status rows = NO IP-shaped
  strings; retention cap honored (3 rows after 5 runs, cap=3); rotation produced a
  rotated file on forced threshold. watcher.py leak-scan clean. Not committed — awaiting go.
- Pass 1 COMMITTED + PUSHED: 53975ea (2e4f3e3..53975ea main). Leak-scan clean.
- Pass 2 — dashboard surface. Added to modules/diagnostics/module.py: _status_dict /
  _recent_samples / _verdict_meta (single source of truth for label+color+blurb) /
  _fmt_age / _dot / _trend_strip / _render_card / _card_js / _api_status / _api_settings.
  Wired Module.get_dashboard_card + get_routes (GET /api/diagnostics/status,
  GET|POST /api/diagnostics/settings); status() now reports the live verdict. Manifest
  provides_dashboard_card flipped true. Card: color-coded is-it-me-or-them verdict banner,
  routing/dns/egress/upstream dots, latency, last-check age, recent-trend strip, collapsible
  settings panel (enabled/interval/verbose/egress-ip/api-host); JS poller refreshes every 15s.
  CLAUDE.md #1 guarded: verdict text stored as plain strings, html.escape at render, JS in a
  non-f string.
  VERIFIED (real output): module imports (f-string compiles — no SyntaxError); card renders
  empty ('No data yet') and populated (ALL_OK/'All clear'); trend+dots present; routes via
  Flask test client — GET /status returns verdict+samples, POST /settings updates allowed keys
  and rejects unknown, GET reflects; only '{' token is the legit JS fetch-headers object;
  Rule-8 scan of rendered card = no private/tunnel IPs. Not committed — awaiting go.
- Pass 1 FOLLOW-ON (VPN provider probes) — NOTE: Pass 1 already committed+pushed (53975ea),
  so per instruction these go as a follow-on commit, not an amend. Added to watcher.py:
  five VPN probe plugins (_probe_pia refactored + _probe_mullvad/_protonvpn/_wireguard/
  _tailscale), each shutil.which-gated skip-if-absent, returning
  {provider,connected,server,protocol,raw} or None. _VPN_PROBES registry + _probe_vpn
  aggregator: raw per-provider detail -> flat log; only an aggregated vpn_connected boolean
  -> DB (provider-agnostic, address-free). Threaded vpn_connected through _probe/run_once/
  _record + summary line. Schema: added vpn_connected INTEGER to both diagnostics_ tables
  (canonical CREATE updated) + guarded PRAGMA/ALTER migration for old DBs. Pass 2 card
  untouched (column recorded, not yet surfaced).
  Added docs/modules/diagnostics/CUSTOM_VPN_PROBE.md — beginner guide (contract, skip-if-
  absent pattern + why, 20-line copy-paste MyVPN example, one-line registration, Rule-8
  split rationale, copy-WireGuard pointer).
  VERIFIED (real output): fresh schema has vpn_connected both tables; old-DB migration adds
  it; on this box PIA present(connected=False) + Tailscale present(connected=True), other 3
  skip to None; run_once recorded vpn_connected=1; RULE-8 — Tailscale tailnet IP is in the
  verbose flat log but DB sample/status rows have NO IP/hostname, only the boolean; watcher.py
  + doc leak-scan clean. NOT committed — awaiting authorization for the follow-on commit.
- CLAUDE.md: added "### Vendor-specific integrations" build-discipline mandate (vendor probe/
  plugin/integration must ship a CUSTOM_*.md guide in the same commit — definition of done).
  Also added the one-liner to ~/work/projectStart.md (outside repo).
- COMMIT 1 (follow-on) PUSHED: b81a3e2 (53975ea..b81a3e2 main) — VPN provider probes +
  vpn_connected boolean + schema/migration + CUSTOM_VPN_PROBE.md + CLAUDE.md vendor mandate.
  module.py split surgically (Pass0-baseline + VPN-schema-only) so this commit excludes Pass 2.
  Leak-scan clean (CLAUDE.md hits at lines 62/63/163 are PRE-EXISTING Rule-8 examples +
  documented PIHOLE_IP known-issue, not from this edit).
- COMMIT 2 PUSHED: 90d62ee (b81a3e2..90d62ee main) — Pass 2 dashboard card + status/trend
  routes + settings panel. Full module.py restored; diff was Pass-2-only (no vpn_connected
  churn). Verified: card renders, GET /status 200, POST /settings ok. Leak-scan clean.
- STOPPED per instruction — Pass 3 (self-gating service + install/uninstall + boot-enable)
  not started.
- Rule 6 backup BEFORE Pass 3: SQLite .backup of alerts.db to independent disk
  /dev/sdb2 (repo/home on /dev/nvme0n1p2) at /run/media/<user>/storage/nemesis-backups/
  alerts-PRE-PASS3-20260628-080808.db; integrity_check=ok; test-restore opened (39 tables,
  7 alerts rows); sha256 8d534c7e25e3ea36a1da201d74f8c77b83d1c97da4de4736c0bb6e056e21d98e.
- Pass 3 BUILT (modeled exactly on malware-canary Pass 3):
  1. alert_manager/diagnostics_watcher.py — standalone service: set_shared_db_path BEFORE
     module import; _init_db at startup (idempotent); creates watcher_log_dir if absent;
     two-gate self-gating (diag._module_enabled('diagnostics') AND watcher_enabled);
     interval read fresh each loop (scheduler-aware); SIGTERM/SIGINT graceful; "started"
     logged to BOTH stdout(journald) AND flat service log.
  2. alert_manager/diagnostics-watcher.service — Type=simple, Restart=always, User=root,
     After=network.target, /home/<user> placeholder (deploy-time sed), RestartSec=10.
  3. install.sh — added diagnostics-watcher to svc_names + all 3 restart/status sites.
  4. dashboard.py HEALTH_SERVICES — added diagnostics-watcher (canary-omission avoided).
  5. uninstall.sh — added to SERVICES; new block removes watcher_log_dir (DB-driven path,
     default /var/log/nemesis/diagnostics) + rmdir empty /var/log/nemesis; alerts.db +
     diagnostics_* tables preserved.
  Added diag._module_enabled() helper (mirrors malware; direct modules_enabled read).
  VERIFIED (isolated temp-DB copies — live alerts.db untouched, still 0 diagnostics tables):
  py_compile + bash -n clean; in-process — gates closed=False/open=True, interval reads
  setting, run_once records sanitized sample (ALL_OK, vpn=1, actor=diagnostics-service),
  NO IP in DB rows, flat service log written, SIGTERM handler flips _running. Real subprocess
  main() w/ OS SIGTERM — gated-ON probes (verdict=ALL_OK vpn=True lat~21ms) + gated-OFF
  "self-gated off", both "started"/"stopped cleanly" to stdout, flat dir gets connectivity.log
  + diagnostics_watcher.log. Leak-scan of new files clean (/home/<user> placeholder only).
  NOT committed, NOT deployed — awaiting authorization (commit-first then deploy per Rule 5).
- Pass 3 COMMITTED + PUSHED: 086a659. Leak-scan clean (flagged dashboard.py/install.sh hits
  were all PRE-EXISTING, not from this diff). Prod deploy handed to user via ! sudo (sudo needs
  password); opened both gates on prod live DB first so deploy yields ALL_OK.
- Standalone fix COMMITTED + PUSHED: b3c428b — sanitized Rule 8 examples in CLAUDE.md
  (real home path→/home/<user>, real hostname→<hostname>, real LAN IP→<pihole-ip>).
- VM AUDIT (diagnostics module) on test VM <vm-ip> (test-user, Ubuntu 26.04). Same drill as
  canary. Note: passwordless sudo had drifted OFF on the rebuilt VM → drove sudo via `sudo -S`
  with the throwaway test cred (not committed). Real output captured at each step.
  STEP 1 uninstall: VM was a clean checkout at ef5ad6f (predates diagnostics, nothing
    installed) → uninstall.sh --yes ran a clean no-op (exit 0).
  STEP 2 verify clean: service not-found, /var/log/nemesis absent, no process. Confirmed.
  STEP 3 fresh install: git showed VM already at 62ffdc3 (origin/main advanced past Pass 3
    086a659 with unrelated docs commits, verified 086a659 is ancestor). Ran install.sh guided
    (fed sudo pw + dashboard pw + defaults; --mode=1; non-interactive pihole). diagnostics-
    watcher DEPLOYED + active + enabled; ExecStart path-rewrite fired
    (/home/test-user/dashboard); /var/log/nemesis/diagnostics created; default gates closed →
    log "gates_open=False / self-gated off". Opened both gates (watcher_enabled=1 +
    modules_enabled.diagnostics=1), restart → "started gates_open=True" → verdict=ALL_OK
    vpn=False lat=20.4ms.
  STEP 4 probe: flat connectivity.log verdict=ALL_OK routing/dns/egress/api=1 vpn=0; DB sample
    ALL_OK; Rule-8 — ZERO IP-shaped values in diagnostics_* sample/status rows (split holds on VM).
  STEP 5 uninstall again (full --yes, also purged suricata/clamav): diagnostics-watcher
    stopped+disabled+service-file removed; /var/log/nemesis/diagnostics removed + empty
    /var/log/nemesis rmdir'd; diagnostics_* tables PRESERVED (samples 5, settings 10, status 1);
    ~/dashboard preserved. Post-verify: service not-found, /var/log/nemesis gone, no process.
  RESULT: ZERO gaps in the diagnostics install/uninstall lifecycle (canary VM-audit lessons
    applied proactively from the start). No fixes to commit.
  ASIDE (NOT diagnostics): Pi-hole unattended install exits at its static-IP whiptail notice →
    never fully installs → uninstall reports Pi-hole "not installed". Pre-existing install.sh
    /pihole quirk — candidate PUNCHLIST item, unrelated to this audit.

--- SESSION CLOSEOUT (2026-06-28) ---
- Diagnostics module COMPLETE: Passes 0-3 committed + deployed (2e4f3e3 -> 086a659);
  VM audit passed CLEAN (zero gaps in install/uninstall lifecycle).
- VPN provider probes added (Mullvad/ProtonVPN/WireGuard/Tailscale, + PIA) +
  CUSTOM_VPN_PROBE.md; vendor-integration build mandate added to CLAUDE.md.
- CLAUDE.md Rule 8 examples sanitized (b3c428b).
- Project identity: support@nemesis-sw.com; Tailscale migrated to project account
  nemesis.tailscale@gmail.com (100.87.130.25); demo-ready, no PII (5b9f9d6).
- Major roadmap captures: VM Test Lab + sandbox integration; agent-rebuild config-driven;
  config-change procedure (docs/operation/CONFIG_CHANGE_PROCEDURE.md); open-source threat
  feeds (v2); enterprise-gap-audit-2026; vm_templates scaffold; Pi-hole quirk;
  lateral-movement core->v2.
- PUNCHLIST adds: broken-API self-healing; transparency audit; tiered-output audit;
  recurring-error audit; MITRE ATT&CK mapping; vulnerability management; auth/login
  monitoring; process-execution monitoring; emergency backup on canary trip; Nemesis
  overhead meter; Pi-hole unattended quirk.
- Closeout docs: supplement 2026-06-28-003.md written; HANDOFF.md overwritten to current state.

--- END-OF-DAY CLOSEOUT (2026-06-28, full day) ---
MORNING:
- Diagnostics watcher complete: Passes 0-3 committed + deployed; VM audit zero gaps
  (2e4f3e3 -> 086a659).
- VPN provider probes (Mullvad/ProtonVPN/WireGuard/Tailscale) + CUSTOM_VPN_PROBE.md +
  vendor-integration mandate in CLAUDE.md.
- CLAUDE.md Rule 8 examples sanitized (b3c428b).
- Project identity: support@nemesis-sw.com; Tailscale migrated to nemesis.tailscale@gmail.com
  (100.87.130.25) (5b9f9d6).
- Morning status block added to CLAUDE.md (84ddc67).
AFTERNOON:
- Data Manager v0: 4 RMW races fixed atomically + verified with concurrent load testing (2d200e0).
- Actor attribution threading: request.remote_addr/system actor through all call sites (ca3350d).
- _client_ip() proxy fix: X-Real-IP header for accurate attribution behind nginx (follow-on to ca3350d).
- ADR 0006 Data Manager + CONTRIBUTING.md contributor contract + CLAUDE.md mandate (committed).
- Flask-Login minimal auth FULL BUILD (21c8931): first-run /setup, passphrase generator,
  tiered lockout (3/5/10 attempts -> 5/15/60 min), login_events table, concurrent session seam,
  SSH recovery CLI (core/manage.py), entitlements stub (core/entitlements.py), @login_required
  all routes, header user display.
- Setup page copy button fix + login recovery hint (618d31b).
- Header status lights: green/amber/red global health indicator, 30s polling, colorblind-friendly,
  tiered tooltip (00206c6).
- Auth live: first-run setup complete, "<owner>" account created, header light green.
- Tiered lockout + login_events + concurrent session seam (c521d57).
- ADR 0007 device-user model + ADR 0008 impossible travel + MSP management plane roadmap (ea0656c).
- 2FA by default marketing capture (2fa4935).
- Windows agent v0: owner-gated RSA enrollment, reconciled into existing cross-platform agent
  (additive), agent_devices migration (11 columns), enrollment UI, install_windows.ps1 extended,
  Rule-8 sanitized (committed).
- platform/ -> platforms/ rename: fixes stdlib shadow bug that prevented agent from launching on
  Windows (e451867).
- Architecture captures: ADR 0009 (Security Inspection Proxy/SSE) [decision captured; ADR FILE
  NOT YET WRITTEN - TODO]; ZTNA + NAC + venue guest network; AI hacking inflection point +
  product thesis; enrollment enriches lateral movement detection; connection-type awareness;
  post-update module repair + community distillation loop + repair agent behavioral contract;
  VM Test Lab + sandbox integration; agent rebuild config-driven; open source threat feeds (v2);
  enterprise gap audit 2026.
- Closeout: supplement 2026-06-28-004.md written; HANDOFF.md overwritten to current state.
