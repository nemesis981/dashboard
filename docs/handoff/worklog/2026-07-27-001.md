# Worklog — 2026-07-27-001 (Window 2, docs/audit/git-writer)

Raw, chronological. Curated version: `docs/handoff/supplements/2026-07-27-001.md`.

---

- Session start. Confirmed Window 2 / Sonnet 5 (no mismatch). Ran Morning Status: 34,819 lines,
  tree clean, all six core services active, roadmap baseline `roadmap-state-audit-2026-07-26.md`
  (4 SHIPPED / 8 PARTIAL / 51 PARKED, 63 total) — file-set drift none (63→63), shipping drift
  none (3 commits since baseline were all `docs(claude)` process edits). Wrote
  `docs/briefing/2026-07-27.md`.

- **Tranche 1 — fd-leak fix + privsep.** Verified on disk against Window 1's report: 7 files
  changed (+209/-171, matched diffstat exactly), untracked `nemesis_privsep.py` (159 lines).
  Rule-8 clean, `py_compile` clean on all 8 files. Read `nemesis_privsep.py` in full and the
  `anomaly_detection` diff — confirmed the `_db()` contextmanager guarantees `close()` in
  `finally:`, checked the two remaining bare `_conn()` call sites and confirmed both already
  closed safely (not part of the leak). Split into two commits (Rule 2): `a38a068` (fd-leak
  fix), `84a57d2` (privsep + 6-service wiring + `_resolve_dashboard_user()` retirement + 3
  `LOGS_DIRECTORY` paths). Listed unpushed commits, pushed. Verified HEAD == origin.

- **Tranche 2 — DB path resolver + migrate_to_opt.sh.** Verified 7 changed items against
  claim. Found a real bug independently: standalone `--verify` (not chained after `--run`)
  false-fails after a successful migration, because `detect_src()` searches `/opt/nemesis`
  first and returns the NEW tree as `SRC`. Held the commit, wrote up the bug for Window 1.
  Fix returned: new `resolve_verify_src()` reads the true pre-migration path from the stamp
  file. Verified the fix by diffing byte-for-byte against the exact version I'd flagged —
  confirmed the change was scoped to only that function plus a defensive `SRC==NEW_ROOT`
  guard and a stamp-exclusion in the residual-grep. Re-ran `bash -n`/Rule-8. Committed as two
  commits (resolver+call-sites not actually git-coupled to the bash script): `597c302`,
  `a133892`. Pushed, verified sync.

- **Tranche 3 — 8 systemd units + de-privileging + gen_units.py.** Verified diffstat (11
  files, 397/63 as claimed at the time). Ran the generator myself and diffed output against
  the staged files byte-for-byte (identical) — confirms the "byte-identical from its new
  location" claim independently rather than trusting it. Read every unit + the generator in
  full. Found an unflagged regression: `vpn-dns-guard.service` silently lost its
  `After=/Wants= pihole-FTL.service` ordering — the generator's schema had no `Wants=` field
  at all. Checked severity by reading `vpn_dns_guard.py`'s `main_loop()`: wrapped in
  try/except, retries every `CHECK_INTERVAL_SECONDS` (20s default) — self-healing, not a hard
  failure, but still a real unflagged loss. Held for a decision; sent back to Window 1.

  Fix (REVISION 2) came back broader than the one instance I'd flagged: a systematic
  directive-diff against `git HEAD` found losses in **four** units (alert-watcher/suricata,
  vpn-dns-guard/pihole+watchdog.env, watchdog/watchdog.env, device-scanner's `After=` silently
  changed target). Re-verified from scratch: pulled every `After=`/`Wants=`/`EnvironmentFile=`
  line from `git show HEAD:<file>` as ground truth for all 8 units, confirmed all four losses
  restored exactly. Regenerated myself and diffed byte-for-byte again. Tested the new
  `gen_units.py --check` mode both directions myself (hand-edited a unit, confirmed exit 1;
  restored, confirmed exit 0). Ran a full line-by-line diff of every unit against HEAD — found
  nothing else unaccounted for (also incidentally confirmed two Description-text fields I'd
  misread as regressions were actually correct restorations of the true original hand-
  maintained text). Re-ran `bash -n`/`py_compile`/`systemd-analyze verify` (exit 0, all 8) /
  Rule-8 — all clean. One commit (`5e9fcc0`, judged genuinely interdependent: install.sh
  creates the exact users the units reference, `diagnostics_watcher.py`'s log-dir fix only
  matters paired with its unit). Pushed, verified sync.

- **Tranche 4 — DB path completion + log paths + privsep observability + polkit rule +
  rollback guidance.** Window 1's own handoff was candid about five defects in its own prior
  work ("What I got wrong"), surfaced only by starting services individually rather than via
  the end-to-end migration path. Verified independently rather than taking the count on faith:
  swept the whole repo for `"alerts.db"` construction and confirmed the claimed completeness
  (all production call sites resolver-aware; remaining raw paths confined to test/migration
  tooling). Ran the new polkit rule file through `node --check` (via a temp `.js` copy — the
  `.rules` extension isn't recognized) — clean. Read the rule logic: matches the claimed 7-unit
  allowlist, restart/try-restart only, watchdog correctly excluded from its own grant.

  Found a second gap not in the handoff: `migrate_to_opt.sh --rollback`'s new stale-unit
  warning pointed operators at `<snapshot>/units-before/*.service` — a path nothing in
  `install.sh`/`migrate_to_opt.sh`/`uninstall.sh` ever creates. Also noted (non-blocking) the
  handoff's "remaining raw paths" list missed a sixth file, `alert_manager/test_quarantine.py`
  (confirmed by reading it — synthetic test tooling, not a production service). Held for a
  decision on the rollback-guidance gap; sent back.

  Fix came back real, not cosmetic: `install.sh` now backs up any existing unit file to
  `/var/backups/nemesis/units-<timestamp>/` before overwriting it; the rollback warning points
  at that real path when populated, and falls back to git-reconstruction instructions when it
  isn't. Also claimed a `set -euo pipefail` interaction bug (bare `ls` on a non-matching glob
  aborting the script mid-warning) was found and fixed with `|| true`. Didn't just read the
  comment and trust it — reproduced both the failure and the fix in an isolated throwaway
  script (`bash pipefail_test_broken.sh` → exit 2, aborts before printing; the `|| true`
  version → exit 0, completes). Confirmed only `install.sh`/`migrate_to_opt.sh` had changed
  since my last pass (everything else byte-identical). Re-ran full gate: `bash -n`,
  `py_compile` (9 files), `gen_units.py --check`, Rule-8 — all clean. One commit (`7e804a6`,
  install.sh/migrate_to_opt.sh genuinely coupled — one produces the backup path, the other
  consumes it). Pushed, verified sync.

- **Closeout brief received** (`~/work/WINDOW2-CLOSEOUT-2026-07-27.md`, from Window 1) —
  describes a live production incident: the operator ran `migrate_to_opt.sh --run` for real.
  Code+DB moved cleanly, but the script restarted all 8 services at the end of its run while
  the deployed units still pointed at the now-deleted `/home/<user>/dashboard` — driving all 8
  into crash loops for ~25 minutes before individual recovery. Root cause claimed as Window
  1's: `do_migrate` actively restarts services (rather than leaving a quiet inconsistent
  state) and `--verify` never checks that units point at the new path (only code/DB/old-path-
  gone).

  Did not transcribe this narrative on trust. Independently verified against the live system
  before writing any doc:
  - `/opt/nemesis` confirmed as the real relocated repo: `origin` = `github.com/nemesis981/
    dashboard.git`, `HEAD` = `7e804a6` — matches my own last push exactly. No data loss, no
    divergence.
  - `~/dashboard` confirmed dead as a git repo (no `.git`); contains debris (an empty, 0-byte
    resurrected `alert_manager/alerts.db` and a `quarantine/` dir) — consistent with the
    incident's crash-loop timeframe, left as-is (not mine to clean up; documented, not acted
    on).
  - `/opt/nemesis` git status: exactly one untracked file, `.nemesis-premigration-mode`
    (content `775` / `/home/<user>/dashboard`), confirmed NOT gitignored — matches the claimed
    open item precisely.
  - All 8 services independently checked via `systemctl show`: `ActiveState=active`,
    `SubState=running`, `NRestarts=0` for every one, `ExecStart` paths all correctly under
    `/opt/nemesis`, `User=` matching the de-privileging model (<user> for dashboard/device-
    scanner, dedicated `nemesis-*` accounts for the other six).
  - UIDs for the six de-privileged users confirmed via `id`: 985/990/991/992/993/994 — matches
    the claimed 985–994 range. All share `gid=972 (nemesis-db)`.
  - `dashboard.service`'s in-file hardening-deferred comment read directly — matches the
    claimed reasoning (`sudo -n` in ~10 places, `NoNewPrivileges=yes` would break sudo
    elevation via the kernel's no-new-privs bit).
  - USB snapshots: all three claimed dated sets exist exactly as named
    (`2026-07-27-1537-pre-service-deprivileging`, `-1625-pre-opt-relocation`,
    `-1846-pre-opt-migration-run`). The current (1846) set's contents verified directly:
    `alerts.db`, `STATE.txt`, `nemesis.env.KEYNAMES-ONLY.txt` (16 key names, zero secret
    values — confirmed by reading it), `units-before/` (all 8 `.service` files present),
    `sudoers/` (6 files: `etc-sudoers`, `nemesis`, `nemesis-deprivilege-step1`,
    `nemesis-relocation`, `nemesis-restart`, `nemesis-vpn`).
  - `/etc/nemesis.env` line 18 read directly (readable — group `nemesis`, which `<user>` is a
    member of): `NEMESIS_AGENT_EXE=/home/<user>/nemesis-dist/NemesisAgent-Setup.exe` — confirms
    the claimed still-hardcoded path exactly.
  - `PUNCHLIST.md`'s `[FIX-NOW]` fd-leak entry read directly: still describes the wrong root
    cause (`_detection_cycle` leaking on `eve.json`) — confirmed stale as claimed. Corrected it
    (pure docs fix, not code) rather than leaving it wrong overnight.
  - `docs/reference/operational-notes.md` also still carries the same stale eve.json framing
    (line 116) — flagged, not fixed tonight (deserves its own pass, not a rushed tack-on).

  Two things I could NOT verify and said so rather than guessing:
  - Attestation journal lines for the six services — `sudo journalctl` requires interactive
    auth in this session (no passwordless sudo available to me here); could not confirm the
    specific log lines. The UID/capability facts underneath are independently confirmed via
    `id`, but the journal entries themselves are unverified, not disproven.
  - Live `/etc/sudoers.d/` listing — same permission gap. Cross-checked against the 18:46
    pre-migration USB snapshot instead: 2 of the 3 named "temporary grants"
    (`nemesis-deprivilege-step1`, `nemesis-relocation`) appear there; `nemesis-dashboard-unit`
    does not (plausibly created later, during incident recovery, after that snapshot was
    taken); two unnamed-in-the-brief files (`nemesis-restart`, `nemesis-vpn`) also appear.
    Reported as partially corroborated, not confirmed, given the snapshot predates the
    incident and I have no live read access from here.

  One finding of my own, not in the closeout brief: `device_scanner.py` still shells out via
  `["sudo", "nmap", "-sn", ...]` unconditionally — no code path was ever added to skip `sudo`
  when the unit's new `AmbientCapabilities=CAP_NET_RAW` is already held. Read the file directly
  to confirm. This means the "CAP_NET_RAW replaces the unrestricted sudo nmap grant" framing
  in the units commit isn't actually realized at the code level: either the old sudoers grant
  is still live and unused (capability added, but the exposure it was meant to replace is still
  there) or it was revoked and device-scanner is now silently broken. Flagged as an open item.

- Wrote `HANDOFF.md` (overwrite), this worklog, and the paired supplement, all at the correct
  post-relocation path (`/opt/nemesis/docs/handoff/...`, not the dead `~/dashboard`).
