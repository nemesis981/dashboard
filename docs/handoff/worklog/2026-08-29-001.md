# Worklog — 2026-08-29 (Window 2, session 001)

Raw chronological log. Minimal prose, no curation — see the closeout supplement for the
curated account.

- Read cold-start handoff, resumed session.
- Confirmed `backup-closeout-reorder` content clean; deleted `backup-closeout-reorder` and
  `stage5-2b-meminject` from `origin` per operator instruction (scope-overreach precedent).
- Compiled Tier1/Tier2 gap inventory (`~/work/nemesis-internal/audits/base-project-gap-inventory-2026-08-28.md`),
  committed to private repo.
- Landed sudoers-grant decision (systemctl/journalctl/tail keep, nmap removed) — public
  record kept vague per Rule 10, full detail private-only.
- Landed `CLAUDE.md` `--passwordfile` diff (`80a5985`) — operator-authorized directly,
  6-session-unclaimed item resolved.
- Landed Window 3 batch 1 (hw_map resolver, test_layer_c root-cause, backup-modal drift fix,
  `_hour_of_week`→`_hour_of_day` rename) + Window 1's 3c-7 GetSecurityInfo fix — independently
  re-verified each.
- Landed Window 3 batch 2 (Rule 8 forward-fix, alert-list read-depth, `_load_exclusions()`
  fix, AGENT_VERSION docs, Rule 11 `login_events` exception + username leak fix, vestigial-table
  drop [live DB change, snapshot taken first] + `anomaly_state.py` two-bug fix).
- Landed 7-file token-revocation/mem-namespace batch.
- Held attestation-connection change per operator instruction, waited for Shape 2 restructure
  to land fully.
- Shape 2 landed (`5c19e0c` + related) — 7 files, independently verified, CLAUDE.md incident
  writeup included (`08e55af`).
- Caught and corrected a fuzzy-match `patch` silently dropping 13 lines of Window 1's D4
  content in `database.py` during hunk-split staging; rebuilt with a zero-fuzz patch, verified
  byte-exact against backup.
- Landed the two new CLAUDE.md security rules (`a67d14b`) — unauthenticated-route restriction +
  `_AUTH_EXEMPT` hardening checklist — after explicit operator confirmation the draft was final.
- Applied Window 3's `dashboard.py` split: small fixes first (`34fd074`), then a separate
  test fix (`7c38c28`).

## INCIDENT — `ed6af88` orphaned commit, and companion-file loss

- Committed `ed6af88` (`dashboard.py` only — Window 1's email-enrollment routes) as the first
  half of what should have been a 4-file commit (`dashboard.py` + `roles.py` + `views.py` +
  `writes.py`, with matching `database.py` edits).
- Immediately after, ran a scoped-reset command intended for a scratch directory that carried
  a stray `-C /opt/nemesis` flag and used `checkout -- .` (bare `.`, not a specific path) —
  this executed against the REAL repo, not the scratch copy, and discarded every unstaged
  tracked-file modification tree-wide: `roles.py`, `database.py`, `attestation.py`, `views.py`,
  `writes.py`, `OPERATION.md`, `SETUP_LINUX.md`, `PUNCHLIST.md`. Output was piped through
  `2>/dev/null; true`, masking any signal something unexpected happened.
- Net effect: `ed6af88` was left in history referencing code (`writes.consume_enrollment_request`,
  `modules.email_security.enrollment`) that existed nowhere in the committed tree. `main` went
  red (`test_roles.py` failing — dashboard.py's committed routes have no matching `roles.py`
  entries). Separately, `attestation.py`'s CHALLENGE_TTL fix and the three doc corrections were
  wiped the same way (not a bad commit — reflog showed no reset, stash was empty, consistent
  with a direct working-tree checkout).
- Operator caught it, issued a STOP on further destructive git operations. I gave a full,
  non-minimizing account when asked; took no remediation action pending instruction.
- End-of-day accounting (this session): confirmed via read-only checks —
  - `HEAD == origin/main == 36c3101` (unchanged since last night; `36c3101` = the file-integrity
    manifest commit, landed on top of `ed6af88`).
  - **`ed6af88`'s underlying defect is STILL LIVE in committed history.** Isolated
    `git worktree add --detach` checkout of `HEAD` alone, `test_roles.py` run against that
    worktree only: **156 passed, 2 failed** — `nothing is auth-exempt but unaccounted for by
    roles.py` and `every live endpoint has a role assignment`, both failing because
    `roles.py` at HEAD has zero enrollment-route entries. A fresh clone or deploy from
    `origin/main` today would still be broken.
  - The working tree itself has since been repopulated by other windows — but this is NOT a
    simple restoration. `git status` shows the wiped files back as modified (`roles.py`,
    `database.py`, `attestation.py`, `views.py`, `writes.py`, docs) PLUS new untracked files
    that did not exist before the incident (`modules/email_security/enrollment.py`,
    `baseline.py`, `sender_id.py`, `test_d4_baseline.py`, `test_enroll_route_public.py`,
    `test_enrollment.py`, `alert_manager/integrity_watch.py`,
    `alert_manager/test_challenge_freshness.py`, `alert_manager/test_integrity_watch.py`) —
    other windows' continued forward work layered on top of the recovery, all uncommitted,
    none of it handed to me for review yet.
  - Running `test_roles.py` directly against this (non-isolated) working tree shows
    **158 passed, 0 failed** — but this measures the uncommitted working tree, not committed
    history, and must not be read as "the incident is resolved." Flagging this distinction
    explicitly because conflating the two is exactly the failure shape CLAUDE.md's own
    "verify the SHAPE, not just the value" section warns against.
  - Worktree used for the isolated check was removed cleanly (`git worktree remove --force`)
    after use; no state left behind.
- **Not yet resolved and not actioned by me:** the committed-history-level defect in
  `origin/main` (ed6af88 still orphaned). Given the operator's standing "stop destructive git
  operations" instruction plus the fact that new, unreviewed work has piled into the same
  files since the incident, I have not committed anything to close this out. Flagging for
  explicit operator direction rather than acting unilaterally.
