# Worklog — 2026-08-30 (Window 2, session 001)

Raw chronological log. Minimal prose, no curation — see the closeout supplement for the
curated account.

- Ran Morning Status: 171,155 LOC; 6/6 services active; `git status` showed 8 modified/
  untracked files not matching HANDOFF's §3 list.
- Roadmap-vs-state audit against `docs/audits/roadmap-state-audit-2026-08-24.md` baseline
  (83 total). File-set: +1 (`child-safety-monitoring.md`, added `5785d15` 2026-08-25,
  correctly filed PARKED). Keyword-swept 126 commit subjects since baseline against all 84
  roadmap filenames; spot-checked the two highest-signal candidates
  (`enrollment-modes-build-spec.md`, `gateway-mode-scoping.md`) directly — both false
  positives, no reclassification. New tally: 11 SHIPPED / 12 PARTIAL / 61 PARKED — 84 total.
- Read `HANDOFF.md` (`5086b51`): §1 claimed `ed6af88` orphaned-commit incident still open,
  companion files not restored, `origin/main` unsafe to deploy.
- Cross-checked against `git log`: found 4 commits landed after `5086b51`
  (`ecd6e35`→`c0e3b57`→`88d8776`→`fc40e31`), all on `origin/main`
  (`HEAD == origin/main == fc40e31` at time of check). `ecd6e35` restores the full
  companion set (`alert_manager/database.py`, `alert_manager/roles.py`,
  `modules/email_security/{enrollment,views,writes}.py` + 2 tests).
- Ran `alert_manager/test_roles.py` directly against the live tree: **158 passed, 0
  failed**, confirming the incident is genuinely resolved, not just committed-but-untested.
- Elevated-grants check: `sudo -n -l` on production box — no broad `(ALL) NOPASSWD:`
  grants, no `nmap` entry, all entries narrowly scoped. `getent group pihole nemesis-db
  nemesis-fw` + `id <user>` — `pihole` still held (known open item), `nemesis-db`/
  `nemesis-fw` expected. `ls /etc/polkit-1/rules.d/` — Permission denied (needs root),
  flagged not skipped.
- Traced `git log -p -- docs/handoff/HANDOFF.md` for the "Standing elevated grants"
  section's history: full at `f79f5ad` (08-26) → pointer-only at `670ab6b` (08-27) → gone
  entirely by `f20d696` (08-28) and `5086b51` (08-29). Root cause: HANDOFF.md is
  overwritten wholesale each closeout (Rule 9); embedded content only survives a closeout
  author manually retyping it, with no diff/reflog signal when it's dropped.
- Wrote `docs/briefing/2026-08-30.md`, mirrored to `~/work/nemesis-internal/briefing/`.
  Reported both findings (stale §1, thinned elevated-grants section) plus the roadmap tally
  and the 8 uncommitted files.
- Operator directed 3 closeout items: refresh HANDOFF (close §1, restore elevated grants),
  a structural fix for the thinning pattern, and confirmation that Window 1's local commit
  of the 8 pending files landed.
- Re-checked `git status` — clean. Two new local commits present: `30efe9c`
  (integrity fact-file → ticket path, 5 files) and `e302768` (D4 personal sender baseline,
  3 files) — together account for all 8 files flagged in the morning briefing. Confirmed
  via `git show --stat` on both. Neither is pushed yet (`HEAD == e302768`,
  `origin/main == fc40e31`).
- Created `docs/handoff/elevated-grants-tracking.md` — standalone, edited-in-place file
  (PUNCHLIST.md discipline, not HANDOFF's overwrite discipline) holding the full grant
  detail. Populated with today's live findings + the thinning postmortem.
- Amended `CLAUDE.md` Morning Status item 7: points at the new tracking file instead of
  "keep the detail in HANDOFF.md's existing section"; documents the root cause and why the
  fix is structural (file that's edited, not regenerated, can't lose untouched content).
- Refreshed `HANDOFF.md`: closed §1 (resolved, cites `ecd6e35` + `test_roles.py` 158/0),
  replaced the elevated-grants section with a pointer + one-line summary referencing the
  new tracking file, updated push status (2 local unpushed commits) and working-tree
  status (clean).

## Rest of session — roadmap docs, then a full day of push-batch verification

- Operator reassigned DNS-exfiltration + rogue-DHCP detection into V2 scope. Wrote
  `docs/roadmap/dns-exfiltration-detection.md` and `rogue-dhcp-detection.md`, grounded in
  live code reads (`_QTYPES = {"A","AAAA"}` at `modules/anomaly_detection/module.py:101`,
  `_root_domain()` at `:1336`, confirmed `modules/dhcp/module.py` had no rogue-detection,
  confirmed `install.sh` had no DHCP eve-log config). Updated
  `v2-completion-checklist.md` recording the reopening decision. Committed `20a5d83`.
  Window 1 built both same-session (`2ce0866`, `cc4aef5`, `5b15b3c`).
- Lateral-movement reassigned next. Found an uncommitted checklist correction already
  sitting in the working tree (Tier 1 was a silent omission, not a new item) —
  independently re-verified every claim (PUNCHLIST:391, `eec1a00`, a repo-wide grep for
  zero existing lateral-movement code, the Tier 2 ARP-anomaly signal) before committing it
  (`331089d`). Wrote the Tier 1 spec and Tier 2 scoping pass (`4713629`) — grounded in
  real schema (finding tables, device/IP identity) and flagged one open question (Suricata
  flow-event visibility not verified from the repo) plus the PUNCHLIST Suricata-mislabel
  fix decision (rename, not split; exact text specified).
- Added the module route-registry-completeness standing practice to CLAUDE.md
  (`a981c5e`), prompted by `8a8580f`'s crash-loop fix narrowing the loud startup check.
- **Remainder of the session was push-batch verification, repeated ~8 times as Window 1
  and Window 3 landed work continuously.** Pattern held throughout: fetch, independently
  list `origin/main..HEAD`, verify commit content against whatever was claimed (never
  trusted a named SHA list at face value — caught several count/attribution mismatches:
  "3" that was actually 5, "8" that was actually 7 (with `d47e86f`/`a0b283d`/`a209695`
  unnamed), "4" that included an already-pushed SHA, "~25 including 5333a0c" that was
  actually 2 with `5333a0c` already published), Rule-8 leak-scan the full diff, report,
  wait for explicit confirmation, re-check the unpushed set hadn't moved since
  confirmation, push the confirmed SHA (never the branch name), verify
  `origin/main == HEAD` after.
- Batches landed, in order: elevated-grants/roadmap work (`4532685`, 16 commits) →
  lateral-movement spec/scoping fold-in (`4713629`) → checklist correction (`331089d`) →
  quarantine-restore + ai_engine ceiling (`f555dce`, 5 commits) → registry-completeness
  rule (`a981c5e` + `3015f1b`) → the Gateway Mode / admin-approval / drift / forkb mega-
  batch (`cc75d5c`, 22 commits) → the VPN-classification revert/rebuild cycle
  (`d33f0b8`, 4 commits: `fbed8ef` revert, `14b8646` installer Basic Auth removal,
  `b5189a0` COSE-wrapper fix, `d33f0b8` tunnel-aware rebuild) → tunnel-predicate
  generalisation (`74bda0c`, 2 commits) → RP-ID pairing fix + VPN-display UI (`c8bcb4f`,
  2 commits).
- Diagnosed the RP-ID pairing bug read-only, without touching the in-flight
  `dashboard.py`/`test_admin_approval_routes.py` edit: `pin_rp_id()` called with no
  argument at `dashboard.py:~8555`, when `rp_id_hash()` two lines later already calls
  `rp_id()` internally (which does the derive-and-pin correctly). Flagged the response
  needs `pinned_now` so a bare delete-and-call-rp_id_hash() wasn't quite right; also
  found zero test coverage on the derive-and-pin branch (every test supplied
  `rp_id_hash_b64`, so the buggy branch was structurally unreachable in the suite).
  Window 1's actual fix (`c78d929`) matched this and went further, also catching a
  check-then-act race.
- Confirmed the VPN-aware→revert→tunnel-aware sequence live: independently verified via
  `piactl get connectionstate` (Connected) and `ip route show` that the box currently has
  the exact `/1`-straddle route shape (`0.0.0.0/1` + `128.0.0.0/1` via `tun0`) the day's
  `tunnel_carries_egress()` fix was built to classify as full-tunnel.
- End of session: `origin/main == HEAD == c8bcb4f`, 0 unpushed, working tree clean. Fired
  the full nightly closeout at operator's request.
