# Worklog — 2026-06-27-001

> Raw chronological flight recorder. Append-as-you-go, terse, no curation.
> Distilled into the session supplement at closeout.

- Pass 0 readiness · Tier A — fixed devices-table fresh-install crash (no CREATE
  anywhere → added canonical CREATE); anomaly_detection switched to shared get_db()
  accessor; added guards on cross-process reads. Commit fb52a83.
- Pass 0 readiness · Tier B — added attribution (actor) seams to module tables;
  config-change audit; CLAUDE.md build-discipline mandates (firewall chokepoint,
  multi-user seams, schema canonical-init). Commit 31337e1.
- ADR 0005 authored + ADR 0002 root-cause correction (DNS = client-refusal-by-source,
  not policy-routing layer). ADR 0002 superseded by ADR 0005. Commit 07699c2.
- Product thesis doc (built-in IT expertise / enterprise capability without enterprise
  pricing). Commit 9d147da.
- Adaptive link-aware agent + clock sync capture (346a1d3); VPN watcher productization +
  diagnostics captures (5f88697); tool-aware AI diagnostics loop (0be9b92).
- Layer B Pass 0 — schema seams (actor on malware_findings + malware_canary_files
  baseline table). Commit 7fdadbd / d752492.
- Layer B Pass 1 — ransomware canary planting + baseline recording (no service yet).
  Commit def1b13.
- Layer B Pass 2 — canary poll loop + trip detection + alert/ticket on tamper.
  Commit 0233510.
- Layer B Pass 3 — malware-canary.service (always-on, self-gating), deployed +
  boot-enabled. Commit 60c19ff. Layer B v1 verified end-to-end on dev box (4 bait
  files, 30s poll, trip→finding→ticket→alert).
- VM audit IN PROGRESS — uninstall → fresh install → forced canary trip. (Note in
  progress at closeout; results + uninstall-script fixes resume next session.)
- Live punchlist capture — replace hardcoded static Anthropic prices with live capture.
  Commit 900530b.
- Session closeout: supplement 2026-06-27-001 written; HANDOFF.md overwritten; this
  worklog finalized; docs commit + push.
