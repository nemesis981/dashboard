Worklog — 2026-08-18, Window 2

RECONSTRUCTED AT CLOSEOUT, not written live — third occurrence of this same gap
(prior instances 2026-08-08, 2026-08-17). Flagging again rather than treating it
as normal; if it recurs a fourth time it stops being a lapse and starts being
this window's actual practice.

- Status check requested: confirmed public repo clean (HEAD == origin, no
  unpushed commits, no modified tracked files) ahead of Window 1/Window 3
  starting memory-injection and appliance-scan-scoping work. Only untracked
  docs held (my own nginx audit, Window 3's old error-code batches).
- Committed the nginx-config-drift audit (718e1d0), re-verified clean before
  landing.
- Messaged Window 1 (nemesis-86) to confirm sequencing before committing
  procmem.py/test_procmem.py per its own handoff warning about manifest
  rebuild timing. Verified independently that no currently-enrolled device
  could be affected (live query: zero attest_manifest dispatches ever, all 13
  agent_devices rows attestation_state=absent). Window 1 corrected its own
  earlier claim (failure shape is 'missing' not 'unexpected') and flagged a
  real gap: AGENT_VERSION needed bumping in the same commit. Agreed Window 1
  would make that edit and hand it over.
- Placed Window 3's appliance-self-scan scoping doc as an ADR 0004 amendment
  (6e0b27d) -- ADR 0004 amendment for the trigger design + engine-swap
  rationale, generalized per Rule 10 (exact resource/timing figures kept
  private, matching the ADR's own existing treatment of its Step 3 gap); two
  derived PUNCHLIST entries (Layer B behavioral overclaim, dead
  scan_schedules UI). Updated Window 3's own scoping doc in the private
  mirror with a placement-resolved note rather than duplicating content.
- Window 1 delivered the AGENT_VERSION bump (1.0.1 -> 1.0.2) with a proven
  before/after test (old-version-vs-new-manifest -> ABSENT; matching-version-
  differing-fileset -> FAILED with 'missing'). Independently re-ran
  test_attest.py (16/16) and alert_manager/test_attestation.py (21/21) before
  committing procmem.py + test_procmem.py + the version bump as one logical
  change (165be15). Updated the AGENT_VERSION-divergence PUNCHLIST entry to
  reflect the bump and Window 1's cleaner framing (77f12c7).
- Committed the Layer B behavioral-overclaim honesty fix (module.py,
  manifest.json, new roadmap stub) once Paul confirmed it was reviewed and
  approved, applying the same Rule 10 disposition Window 3 flagged (public,
  Layer D precedent) (f7dc88f). Verified: py_compile, valid JSON, repo-wide
  grep for dangling references to the removed layer/settings names (none).
- Window 1 flagged a structural finding while holding membudget.py
  uncommitted next to a committed pair: the attestation manifest is built
  from the server's live working tree, not any committed/released build, so
  uncommitted WIP under nemesis_agent/ can poison the fleet manifest
  independent of any version-bump mistake. Filed as its own PUNCHLIST entry
  at Window 1's request, since docs are Window 2's lane (b67ff65).
- Committed the RAM budget model + appliance adapter (membudget.py,
  test_membudget.py, mem_appliance.py, test_mem_appliance.py) as one logical
  change (b143c10) once asked. Independently re-verified rather than trusted:
  both test suites (15 + 8 functions) run fresh, both modules' own
  self_test()s run fresh, and mem_appliance.py's standalone demo run live
  against this box's real processes (63.7 GB total RAM, clamd at 429.5 MB
  against a 1600 MB budget, zero breaches) rather than relying on the test
  suite alone.
- Window 1 surfaced two more ready pieces (memladder.py/test_memladder.py,
  the escalation ladder) unprompted, twice -- once initially, once again
  after finding and fixing a real gap in its own delivery (shadow-mode
  promotion counted only resolved records, but nothing resolved any, so
  promotion would have been blocked by missing machinery rather than missing
  evidence; also fixed two bugs in its own test runner). Held both times,
  per standing discipline: Window 1 offering readiness is not the same as
  Paul asking for a commit.
- Status check requested on the Linux-side privilege work (Window 3): found
  a real, verifiable bug before committing anything -- core_module/
  malware_scan/malware-scan.service has PrivateTmp=no (the deliberate,
  heavily-commented fix) followed later by PrivateTmp=yes inside a
  copy-pasted hardening boilerplate block. systemd resolves duplicate simple
  assignment directives by last-occurrence-wins, so the effective value
  silently undoes the fix the file exists to ship. systemd-analyze verify
  does not catch this (not a syntax error). Held the commit, reported to
  Paul rather than editing Window 3's file myself or committing broken code.
- Filed the install.sh-wiring + clamd.conf-phishing-settings gap as its own
  PUNCHLIST entry per direct instruction, independent of the blocking bug
  above -- the open-items flag doesn't depend on the held code landing first
  (f893396). NOT YET PUSHED as of this closeout -- caught during the
  closeout's own sync check, not before.
- (Private repo, no action needed here) Window 1 reported a shared-index
  commit accident in ~/work/nemesis-internal: its own closeout commit swept
  in Window 3's already-staged handoff file, pushed to local+usb before
  being noticed. Content intact, just wrong commit attribution; correctly
  left unrewritten since it was already pushed. Noted for the record, not
  Window 2's repo or responsibility.
