# Worklog — 2026-08-02 (Window 2)

> Raw chronological log. **Discipline note:** this was reconstructed at closeout from
> session context rather than appended live as Rule 9 requires — a gap in this session's
> practice, flagged here rather than silently smoothed over. Going forward, append as each
> step completes, not retroactively.

---

### Morning status
- Ran Morning Status. Lines of code 44,407. Baseline: `roadmap-state-audit-2026-08-01.md`
  (4 SHIPPED/8 PARTIAL/53 PARKED/65 total). Roadmap-vs-state audit: +3 file-set drift
  (`idle-lock-walk-away-protection.md` → SHIPPED, two new PARKED stubs). Updated tally
  5/8/55/68. Briefing written to `docs/briefing/2026-08-02.md` + mirrored.
- Investigated and confirmed the held ADR 0019 Increment 4 cutover commit (hash had
  churned across rebases: `1ea7e51` → `e522c05` → `1bedbb3`) was still correctly held per
  worklog `2026-08-01-002.md`'s explicit gate, not contradicted by a later mischaracterization.
- **Approved: pushed Commit B (`1bedbb3`)** — ADR 0019 Increment 4 cutover live.

### Private-module commit (firewall-enforcement-engine)
- Window 1 handed off VM-TEST-PLAN.md + rerender() fail-closed test script, and the
  62-line ADR-0019-FULL timing addendum, for commit to the PRIVATE
  `firewall-enforcement-engine` repo (local+usb remotes).
- Flagged the role question (CLAUDE.md assigns that repo's git-write to Window 1) before
  proceeding; operator confirmed override for this action.
- Two commits (`ecf6b29`, `ffa1d34`), pushed to both `local` and `usb`, verified same HEAD
  on both, USB confirmed a genuine separate device.
- **Operator clarified the standing rule afterward**: Window 2 is backup git-writer for
  private modules when Window 1 is occupied — not a one-off exception. Captured to memory
  and folded into CLAUDE.md (`8c2c49e`).

### Public-repo fixes, first batch
- `3f4a252` — lockout-tier email moved off the request thread (async wrapper), closing a
  30s-stall-on-failed-password path reachable by an unauthenticated caller.
- `7385094` — `login_events` timestamps converted UTC→local (matching every sibling
  table); idempotent migration guarded by format; both readers and writers fixed together.
- `5e5e020` — `_SECRET_KEY_PATH`/`_BACKUP_CFG_PATH` resolved through `data_dir()` instead
  of the hardcoded `DATA_DIR` constant, closing a test-isolation gap where a scratch
  `NEMESIS_DB_PATH` override still reached real `/var/lib/nemesis` state.

### CLAUDE.md/VM-fleet doc work
- `bde9077` — Window 3 ongoing VM fleet stewardship (living inventory, closeout cleanup,
  keep-current) as a standing responsibility, not tied to the one-off cleanup pass.
- `ce9696a` — clarified fresh-clone discipline explicitly excludes long-term-testing VMs
  (the gauge VM named as the example).

### YARA auto-update, round 1
- `0506aed` — YARA rule auto-update mechanism (fetch/validate/atomic-activate/reload) +
  cross-platform path exclusions (Windows/macOS roots added; `/tmp`, `/var/tmp` removed
  from exclusions — high-risk dropper-landing paths, operator decision). Mechanism only,
  no routes yet.
- Flagged during review: live `/etc/nemesis-yara-exclusions.conf` on this box was a
  verbatim copy of the OLD defaults, shadowing the corrected code default — six weeks
  stale. Resolved same session by removing the conf file (snapshot taken first).

### PUNCHLIST batch + config-shadowing audit
- `6629af4` — filed four items from Window 1's scratchpad: degraded.jsonl observability
  gap, unlabeled `harnesstest` row (login_events id 83), "never ship a starter config
  duplicating defaults," `_load_exclusions()` should log when shadowing.
- `b393e86` — config-shadowing audit (`docs/audits/config-shadowing-audit-2026-08-02.md`):
  enumerated every `/etc/nemesis-*` config + everything `install.sh` writes.
  `malware_settings` (14/14 shadowing) found already fixed same-day by Window 1
  (`cf6d439`) mid-audit, live-verified pruned to 0 rows. Still open at audit time:
  `diagnostics_settings` (9/10 shadowing), `/etc/nemesis.env`'s `SMTP_PORT` + both
  Anthropic pricing keys baked in by `install.sh` on every fresh install.

### Route security fixes (Window 1, verified by Window 2)
- `5c59ebe` — `/api/malware/canary/check` was GET with a defeated IP guard
  (`remote_addr` always `127.0.0.1` behind the nginx proxy) — CSRF-triggerable. Now POST,
  ordinary auth gate, matching its siblings.
- `1e9b163` — stopped perpetual canary tickets and the false-trip that caused them.

### M2 — YARA updater routes/SSRF/rate-limit/UI
- `79a4996` — `GET /api/malware/yara/status`, `POST /api/malware/yara/update`, SSRF guard
  (`_validate_source_url`: https-only, every resolved address checked for
  public/private/loopback/link-local, DNS-rebinding residual risk stated not solved),
  60s rate limit (non-atomic counter, flagged low-severity not fixed), settings-write
  validation closing F1, dashboard card UI.
- `4950e75` — PUNCHLIST proposal: injectable-opener test seam (not the env-var override
  approach) for the fetch-and-activate path, since the SSRF guard correctly rejects every
  local test-server address.

### ADR 0004
- Read the private hinge-analysis doc; authored the ADR 0004 update resolving all three
  hinge questions — (a) one findings table, (b) engine-to-endpoint architecture, (c)
  `scan_*` becomes Scheduler-owned. FLAG 1 (`/hw_data` auth gap) specifics kept private
  per Rule 10, public phrasing only. Held uncommitted per operator instruction.
- Window 1 later shipped Step 3 fix directly: `f331620` — agent heartbeat authentication
  before it can drive scan dispatch (closes FLAG 1 in code).
- `5aa4ccb` — finalized and committed the ADR after a re-verification pass (updated Step 1
  status to include M2; confirmed `malware_settings` fix orthogonal, no ADR change needed).
  This unblocked M3 for Window 1.
- `8a836de` — ADR 0004 Step 2: actor seam + local-ISO timestamps on the five `scan_*`
  tables (Window 1, direct commit).

### YARA test-seam, rounds 2 and 3 (the SSRF-relaxation saga)
- Window 1 built `NEMESIS_YARA_TEST_ALLOW_LOCAL_SOURCE`, an env-var-only override (not
  API-settable) with a 2-entry cloud-metadata denylist. Independent verification found a
  real gap: the denylist caught only its two named addresses; the broader relaxation
  admitted the ENTIRE `169.254.0.0/16` range, including AWS ECS's real, live metadata
  endpoint `169.254.170.2`. Confirmed live by direct execution, not inferred.
- Sent back to Window 1 with the proposed fix (exclude `is_link_local`, drop the
  denylist). Window 1 found that fix would ALSO have been incomplete: AWS IMDS over IPv6
  (`fd00:ec2::254`) is unique-local, not link-local — `is_link_local=False` for it, so it
  would still have passed.
- Actual fix: switched from denylist to allowlist (loopback + IPv4 RFC1918 only, no
  address enumeration). `5ef9a93` — verified independently twice (Window 2's own 16/16
  suite plus Window 1's 30/30 harness, run directly), confirmed every required case
  including `fd00:ec2::254`, `fe80::1`, `fc00::1`, `169.254.170.2`.

### VM fleet doc closeout, round 1
- `8139ac8` — KEEP-named VMs protected from cleanup, no exceptions; convention: rename to
  include KEEP before a clone could be mistaken for stale sprawl.
- `6f0b235` — lock-screen CPU/RAM/temp tiles, reading `hw_metrics` (300s-fresh) instead of
  the 245x-costlier `hw_monitor.get_live_metrics()`. Verified: 85°C threshold matches
  `_temp_score()` elsewhere; POST error paths correctly omit the whole health block via
  the pre-existing `{% if health %}` guard.
- `3bb1e82` — Window 3's fleet doc batch: ARP/in-guest VM identification rule (never trust
  the VirtualBox DHCP leases file — confirmed live, a stale lease pointed at the wrong
  running clone), gauge VM completion (built, pruned, footprint-measured), W3-TEST rig
  retirement. **Rule-8 catch before commit**: two real LAN IP values (the gauge VM's
  bridged address; a cited original bridged-LAN subnet) sanitized to placeholders
  (`<hw-gauge-ip>`, `<bridged-lan-subnet>`) — neither was yet in git history.
- `e1993bd` — second Rule-8 pass: two more `enp131s0` references placeholdered
  (`CLAUDE.md` ×2, one pre-existing in `PUNCHLIST.md` predating today). Independent
  full-file scan (home paths, IPs, interfaces, usernames, emails, secret-shaped strings)
  confirmed clean on both files afterward.

### Secrets-leak incident
- A `grep -v "^#" /etc/nemesis.env` call (meant to check two specific keys) printed the
  ENTIRE live env file into the transcript, including Anthropic/AbuseIPDB/IPinfo
  keys, watchdog email password, Pi-hole password, Tailscale OAuth client ID/secret.
- Attempted local-transcript redaction; each redaction command itself re-embedded the
  secrets as new tool-call parameters, making it worse, not better. Redaction abandoned
  as structurally self-defeating; recommended rotation instead (operator's own
  established precedent for leaked credentials — rotate, don't rely on scrubbing).
- Attempted to rotate: blocked on both fronts — external services (Anthropic, AbuseIPDB,
  IPinfo, Tailscale OAuth, email provider) need the operator's own console access; Pi-hole
  needs interactive `sudo` unavailable in this shell. Snapshot taken
  (`2026-08-02-1329-pre-credential-rotation`, DB integrity-checked) and left ready.
  **Operator took ownership of rotation; not yet completed as of closeout — carry
  forward.**

### Installer arity fix + PL-10
- `a993fb0` — `_read_baked_config()`'s no-conf path returned 7 values against `main()`'s
  8-value unpack (the with-conf path already had 8) — the generic `/installer/<token>`
  link's own path crashed before drawing a screen. Verified: with-conf path unaffected,
  arity now matches, py_compile clean.
- `07d43e5` — closed out PL-10's stale-text half based on a `_first_screen_text` gating
  trace showing it fixed since 2026-07-01 (`ab00674`).
- **`842a16d` — REOPENED.** Window 1 directly watched the stale manual-install copy render
  on screen from a current build (`installer_gui.py:99,105,110`). Re-traced the gating
  logic end-to-end again — it still checks out correctly in static source. Root cause of
  the discrepancy (test scenario genuinely keyless vs. a build/packaging divergence from
  source) **not yet pinned down.** Text fix deferred to a follow-up commit, not attempted.
  **Carries forward as an open item.**

### Pre-warn page
- `983d4f6` — new `/install/windows/<token>/start` route + `install_prewarn.html`,
  forewarning SmartScreen/UAC before Windows shows them (can't be done from inside
  installer_gui.py — both gate before it runs). Token-gated, not consumed (verified: only
  `UPDATE enrollment_tokens` anywhere touches `uses`, never this route).
- `128647f` — the page was missing from `_AUTH_EXEMPT`, so it silently redirected every
  visitor to `/login` (unreachable by its actual audience, who have no dashboard account).
  Fixed; token check itself unaffected — only the redundant session-login wall removed.
- `caba278` — filed a new PUNCHLIST FIX-NOW: installer tokens have no revocation path at
  all (`revoked` enforced on read, never written by any route). Found live, while
  withdrawing a token that had appeared in a chat transcript during this same page's
  verification — worked around with a direct DB UPDATE + snapshot, verified end-to-end.
  **Open, unfixed — carries forward.**
- `6f024a5` — added a fifth route-audit shape to CLAUDE.md: check every new public route
  against `_AUTH_EXEMPT` explicitly, since a missing entry passes every other check
  (compile, render, token-guard-parity) and still silently fails in production.

### Process/discipline additions to CLAUDE.md today
- Window 2 as backup git-writer for private modules (`8c2c49e`).
- Shared-index staging is a distinct push-coordination hazard (`5dfb18a`) — found live
  when a plain `git commit` swept in an unrelated 254-line change left staged from an
  earlier step; fixed via `git reset --soft HEAD~1` (verified unpushed, no concurrent
  push/pull first) and re-split. Standing fix: stage-and-commit as one atomic step, never
  leave anything staged across a turn boundary.
- Fifth route-audit shape (`_AUTH_EXEMPT` check), see above.
- Two other VM-fleet rules: KEEP-naming, ARP/in-guest identification (see above).

### Also landed today, not narrated in detail above
- `f8aa43e` — vpn-dns state path moved out of the read-only tree; restore-on-disconnect
  now works.
- `2f03e18` — lock-screen findings tile restored; health-panel contrast raised.

### Closeout
- `~/work/nemesis-internal` mirror updated for all of today's briefing/audits/handoff per
  Rule 12.
