# Worklog — 2026-07-26-001 (Window 2, docs/audit/git-writer)

Flight recorder. Append-as-you-go, terse, chronological. Distilled into
`docs/handoff/supplements/2026-07-26-001.md` at closeout.

---

- **Session start** — operator assigned Window 2 (docs/audit, Sonnet, sole git-writer).
  Read order followed: `CLAUDE.md` → `docs/COLLABORATION.md` → `docs/handoff/HANDOFF.md`.

- **Morning status run.** Lines of code 34,819. HEAD `4425829`. All six services
  `active`. Working tree clean. Local == origin (no unpushed commits: `git log @{u}..HEAD`
  empty).

- **Roadmap-vs-state audit (read-only, Rule 1).** Baseline
  `docs/audits/roadmap-state-audit-2026-07-25.md` = 4/8/47, 59 total. Current file count
  63 → **+4 file-set drift, none removed**. All four traced via
  `git log --diff-filter=A` to post-baseline-doc, same-session 07-25 commits
  (`ebf0aae`, `1d6d2d2`, `946d7b4`, `68c0eb1`); all capture-only → PARKED.
  **Shipping drift: none** — `git log 035e29b..HEAD` touched zero of the 12
  SHIPPED/PARTIAL roadmap files; the only feat commits (ADR 0001 Stages 5–6, ADR 0006
  Data Manager v1) have no roadmap file. New tally **4 SHIPPED / 8 PARTIAL / 51 PARKED —
  63 total**. Nothing changed; report only.

- **Briefing written** → `docs/briefing/2026-07-26.md` (gitignored, local-only).

- **Open at start of session (carried from HANDOFF, unchanged):**
  `installer-unified-v1.0.6` two pre-trip fixes (auto_approve default, double-enroll);
  `agent_devices.last_heartbeat_data` not populating (PUNCHLIST, open since 2026-07-03);
  the three L3 tier scoping docs all still estimate-TBD, blocked by Open Item 3 (no
  target hardware baseline).

- **TODO at closeout:** refresh the baseline audit as
  `docs/audits/roadmap-state-audit-2026-07-26.md` (drift found this morning).

- **CLAUDE.md model-role edit** (mid-morning, operator-directed). Pinned Window 1 =
  Opus (currently Opus 5), Window 2 = Sonnet (currently Sonnet 5) as explicit
  expected-model rules (role-first, version as bumpable detail); added a first-action
  load-time model self-check convention (not a bash check — confirm via `/status` or
  self-report, flag mismatch immediately, then switch). Rule-8 clean. Committed
  `2805af4`, pushed (`4425829..2805af4`).

- **Three new ADRs — deployment appliance model + venue market direction (docs only,
  operator-directed, source: operator's same-day summary; `/areas/nemesis-firewall.md`
  not accessible this session).**
  - **ADR 0014** (`docs/architecture/0014-deployment-appliance-model.md`) — Linux
    appliance (mini PC) becomes primary SMB/venue deployment target, replacing
    Windows-hosted-VM-as-primary; agent-only cross-platform scope narrowing; home-user
    VM path retained unchanged (WSL2/Hyper-V/dual-boot re-confirmed rejected, VM
    overhead objection closed). **Note:** searched repo for the referenced "locked
    June 22 release sequence" doc — not found anywhere in-repo (`ROADMAP.md`,
    `docs/architecture/`, `docs/roadmap/`, `docs/business/`, `docs/specs/`, git log
    2026-06-20→24); likely lives only in the external tracker. Treated `ROADMAP.md`'s
    "Windows Support Status" section as the in-repo analog and updated it accordingly
    — flagged this substitution explicitly in the ADR and to the operator.
  - **ADR 0015** (`docs/architecture/0015-guest-self-service-enrollment.md`) — venue
    guest self-service enrollment (QR/captive-portal). Cross-referenced ADR 0011
    (why distinct) and, more importantly, **ADR 0012's existing VENUE AUTO mode**
    (the actual closer relative — this ADR specifies VENUE AUTO's concrete
    enrollment mechanism, not a new parallel system). **Flagged, not resolved:**
    real tension with the existing `venue-guest-network.md` stub's "app IS the
    credential" framing vs. today's captive-portal-without-mandatory-app direction —
    noted in both docs, cross-linked, left open for the operator.
  - **ADR 0016** (`docs/architecture/0016-guest-marketing-capture.md`) — opt-in guest
    marketing capture, export-API-only design (explicit non-goal: no email-sending/
    campaigns/ESP), scoped per-consumer API keys, mandatory retention/deletion policy,
    **legal-review gate stated in the status line** (GDPR/CCPA/CAN-SPAM-adjacent, new
    compliance surface). Includes the PII network-transit monitoring addendum
    (captive-portal submission = standard web hygiene; export API = reuses ADR 0009's
    Tier 1 engine, explicitly NOT Tier 2; local SQLite reads/writes out of scope,
    already covered by the Data Manager). Business-model paid-integration note
    (unconfirmed liability-shift framing, flagged as needing a lawyer) written into
    `venue-guest-network.md`'s Business model section, not the ADR itself, per
    instruction.
  - **Supporting doc updates:** `ROADMAP.md` Windows Support Status section (scope
    narrowing + reversal stated explicitly); `docs/architecture/0009-security-inspection-proxy.md`
    Connections section (one-line pointer to ADR 0016); `docs/roadmap/venue-guest-network.md`
    (flagged-tension callout + business-model note).
  - **Rule-8 scan:** clean — no home paths, IPs, or emails across all six changed/new
    files (`grep -inE` for each pattern, zero hits).
  - **Held for operator review before committing**, per instruction — not yet
    committed.
  - **Committed** `ffe370a` (operator confirmed "commit all 6"), **pushed**
    (`2805af4..ffe370a`).

- **Window 1 handoff — `l3_tls_validation/` committed** (Tier 2 R&D harness: local
  CA/leaf cert generator, stand-in TLS origin, two-mode cache/inspect interception
  proxy, Layer-1 presentation-identity probe — per `tls-interception-sterilization-scope.md`).
  Reviewed, not rewritten (Window 2 doesn't edit code content). Module's own
  `.gitignore` already correctly excludes `certs/` (private keys) and
  `__pycache__/` — confirmed only the 4 `.py` files + that `.gitignore` staged.
  Rule-8 scan clean (no home paths, no non-loopback IPs, no emails). Sanity
  `py_compile` clean on all 4 files. Committed `b9ec952`, pushed (`ffe370a..b9ec952`).

- **fd-leak status check + troubleshooting doc + PUNCHLIST (docs only, operator-directed).**
  - **Confirmed the `anomaly_detection` fd-leak fix has NOT landed.** Live evidence:
    `journalctl -u dashboard` shows repeated `OSError: [Errno 24] Too many open files:
    '/var/log/suricata/eve.json'` recurring every poll cycle through 11:33 today; `git log`/
    `git diff` on `modules/anomaly_detection/module.py` show no change since `37a02d0`
    (2026-07-25, unrelated Data Manager migration) and no uncommitted local edits. Added as a
    new **`[FIX-NOW]`** PUNCHLIST section (not marked done, per instruction), with a forward
    note (not built now) that the dashboard should fail more gracefully on resource exhaustion.
  - **New troubleshooting section** in `docs/reference/operational-notes.md` ("Troubleshooting:
    dashboard won't load / hangs") — the real diagnostic chain (`systemctl status` → check log
    for the fd-exhaustion error → `restart` as immediate, non-permanent fix →
    `reset-failed` if systemd's rate limiter trips → `journalctl -xeu` if still failing) plus
    today's two debugging gotchas (root-owned files from a manual `sudo` run causing a later
    `PermissionError`; confirming the service's actual user via `grep "^User"
    /etc/systemd/system/dashboard.service` before trusting a manual run's "module not found").
    One-line pointer added to `docs/OPERATION.md`'s Common Scenarios so the user-facing guide
    routes here.
  - **Rule-8 scan:** clean on my diff (`git diff <file> | grep '^+'` against home-path/IP/email
    patterns, zero hits). Pre-existing `127.0.0.1` and `support@nemesis-sw.com` lines in
    `PUNCHLIST.md` are unrelated pre-existing content, confirmed not part of this diff.
  - **Held for operator review before committing**, per instruction — not yet committed.
  - **Left untouched (not mine):** `l3_tls_validation/.gitignore` + `proxy.py` modified and new
    `layer2.py`/`stats.py` appeared mid-task — Window 1 continuing the Tier-2 harness build.
    Not staged, not reviewed as part of this task.
  - **Committed** `3f65d8b` (operator: "commit these 3 only. Hold commits for the win 1
    work until testing completes"), **pushed** (`b9ec952..3f65d8b`).

- **Window 1 handoff — `l3_tls_validation/` Tier-2 harness, second batch (testing complete,
  operator: "commit all of it now — the 9-file set plus the promoted VM-runner once it's
  ready").** Reviewed, not rewritten. `py_compile` clean on all new/changed `.py` files;
  Rule-8 scan clean (no home paths, no non-loopback/non-placeholder IPs, no emails, no
  inline creds); no TODO/FIXME/`NotImplementedError` markers found.
  - **Commit `01fbcfc` (9-file set):** `proxy.py` Layer-2 timing padding (empirical
    inspect-latency profile, precise sleep-then-spin wait); `layer2.py`/`stats.py` drive +
    statistically compare cache-vs-inspect batches; `layer3.py`/`pcap_fields.py`/
    `pcap_synth.py` packet-level (TTL/window/MSS/SACK/options-order) comparison incl. a
    synthetic positive control; `origin_stub.py` renamed `_self_signed`→`self_signed` for
    reuse; `requirements.txt` + `.gitignore` (dpkt/paramiko in a local never-committed venv,
    new `captures/` ignore).
  - **Commit `6d40e7d` (VM-runner pair):** `vm_origin.py` (stdlib-only TLS origin for the
    VM itself — no `cryptography` there) + `vm_runner.py` (SSH orchestration of a real
    Windows test-VM run: deploy via SFTP, run as a Scheduled Task since Windows OpenSSH
    reaps bare child processes on exec-channel close, drive Layers 1/2 from the dev box,
    pktmon capture for Layer 3). **Rule-8 confirmed:** VM IP + SSH credentials load at
    runtime from a gitignored env file outside the repo
    (`~/work/nemesis-private/testvm-creds.env` by convention, override via
    `L3_TESTVM_ENV`) — nothing VM-specific hardcoded in the committed file.
  - **Pushed** (`3f65d8b..6d40e7d`), confirmed local == origin after fetch (0 ahead / 0
    behind, no tracked-file diff).

- **Fork B mechanism resolution + Tier 2 hybrid design (docs only, operator-directed, no code).**
  - **Resolved, not left open:** the mirror-vs-inline documentation gap ADR 0009 addendum's
    Open Item 1 had flagged (`adr-0009-l3-fork-b-scope.md` never stated whether Piece 2's
    forward is gated on Piece 3's Suricata verdict). Confirmed **MIRROR**: Piece 2 forwards
    unconditionally, Piece 3 is a passive `af-packet` interface — neither ever describes a
    verdict gate. Framed explicitly as "never a real contradiction, just an undecided default"
    per instruction, not as newly-discovered ambiguity. New "Mechanism: MIRROR" section added to
    `adr-0009-l3-fork-b-scope.md`; Open Item 1's sub-item in the ADR 0009 addendum rewritten from
    "unresolved" to "RESOLVED (2026-07-26)".
  - **New hybrid inline/mirror design** — added as Piece J in
    `tls-interception-sterilization-scope.md` (full detail) with a summary + cross-ref in the
    ADR 0009 addendum §6 (not duplicated). Per-connection gate: first meaningful chunk of
    *decrypted application data* (explicitly not the literal first packet — TLS handshake
    precedes payload) held inline before delivery; transitions to mirror for the rest of the
    connection's life if clean. Four transition-hardening requirements captured (randomized
    gated-portion cutoff per Piece F's sampling logic; Layer-2 latency padding continued through
    the transition; occasional mid-life re-promotion back to inline; explicit "delivery-gate-
    only" downgrade clarification cross-referencing the existing ADR 0009 addendum §4
    dynamic-cache limitation — same tradeoff, connection-granularity instead of
    destination-granularity).
  - **Tier 2 language corrected** in both the addendum §6 and the TLS doc's intro: Tier 2
    (including the new hybrid gate) does NOT guarantee first-contact prevention of a clean-
    looking payload; it provides fast detection + rapid future-blocking via the cache. Tier 3's
    local late-triggers stated explicitly as the real backstop — intentional defense-in-depth,
    not a Tier-2-specific gap.
  - **One self-inflicted duplication bug caught and fixed during editing:** an `Edit` on the TLS
    doc's header briefly duplicated the "ADR 0009 addendum §6" sentence — caught on re-read
    before Rule-8/commit, corrected.
  - **Rule-8 scan:** clean — `git diff <3 files> | grep '^+'` against home-path/IP/email
    patterns, zero hits.
  - **Held for operator review before committing**, per instruction. **Committed** `bf38988`,
    pushed (`6d40e7d..bf38988`). Post-push drift audit: local == origin, 0 ahead/0 behind,
    confirmed clean.

- **Private-module carve-out: Tier 2 implementation detail + honest-limitation writeups
  (docs/repo-structure only, operator-directed, no functional code change).**
  - **Researched the "private ticket-tracker" precedent** (Explore agent, read-only) before
    building anything: **no built precedent exists** — `support@nemesis-sw.com` → a
    first-party queue is documented only as a named, undesigned prerequisite
    (`docs/roadmap/support-bundle.md`, `PUNCHLIST.md`). The real structural precedent is
    `~/work/nemesis-private/` — a plain directory OUTSIDE the git working tree, not itself
    version-controlled, referenced from the repo by path/name only (never by value). Modeled
    the new private location on that same pattern rather than inventing a new one.
  - **Created `~/work/nemesis-internal/`** (sibling to `nemesis-private`, different scope —
    proprietary technical detail + honest self-assessment, not secrets/creds; explicit README
    stating this is a source-visibility decision, not a feature/tier-gating one — the
    capability ships at every tier per "security is never the upsell").
    - `l3-tier2-tls-interception/code/` — the 13 tracked files formerly at
      `l3_tls_validation/`, copied over before removal.
    - `l3-tier2-tls-interception/IMPLEMENTATION.md` — Pieces E/F/G/H/J moved in full
      (side-channel normalization, cache design, dual randomization, evasion-probing
      handling, the hybrid inline/mirror gate + all 4 transition-hardening requirements),
      plus a placeholder section for parameter values (none exist as concrete numbers yet).
    - `known-limitations/l3-tier2-and-forkb-limitations.md` — Fork B Piece 5's full
      fail-safe/black-hole risk narrative (the clearest match to "the weakest point is Z"),
      plus an explicit **review log** of every other caveat considered and the judgment call
      on each (kept public: ADR 0009 §4 cache limit, the Tier-2 "no first-contact guarantee"
      correction, hard unknowns a/b/c, Piece I pinned-app handling, the Fork-B mirror
      resolution — each with stated reasoning, flagged for operator override).
    - Checked the two pre-existing research docs directly in `~/work/` (already outside the
      repo, untouched) for the more granular caveat phrasing the operator's task description
      used as examples ("steady-state window..,", "rotation doesn't help against a bug...") —
      **not found verbatim anywhere accessible**; noted as possibly living only in the
      external `/areas/nemesis-firewall.md` tracker, out of this session's reach.
  - **Public repo changes:**
    - `l3_tls_validation/` untracked (`git rm -r --cached`) + working tree cleared, replaced
      with a tracked placeholder `README.md` pointing to the private location and stating the
      source-visibility (not tier) framing.
    - `docs/roadmap/tls-interception-sterilization-scope.md` — Pieces E/F/G/H/J bodies
      replaced with short public summaries + pointers; Piece I and hard unknowns (a)/(b)/(c)
      left public (judgment call, reasoned in the private review log); header updated with a
      repo-structure note.
    - `docs/architecture/0009-security-inspection-proxy.md` §6 — hybrid-gate mechanism +
      hardening detail replaced with a summary + pointer; the "Tier 2 does not guarantee
      first-contact prevention, Tier 3 is the backstop" correction **kept public
      deliberately** (user-facing honesty/expectation-setting, not mechanism detail).
    - `docs/roadmap/adr-0009-l3-fork-b-scope.md` Piece 5 — risk narrative replaced with a
      short summary + pointer, estimate kept; the "Mechanism: MIRROR" resolution **left
      public** (redundant with the already-public "Tier 1 = mirror" framing, no new
      attacker-relevant info).
  - **Item 3 audit (confirm nothing already-public needs retroactive scrubbing):**
    - Grepped `docs/handoff/` supplements/HANDOFF for restated Tier-2 mechanism detail — found
      only brief name-level mentions (e.g. "cache design keyed on cert fingerprint... dual
      randomization"), matching what's supposed to stay public; no scrubbing needed there.
    - **Found a real, PRE-EXISTING (2026-06-21/22/25, unrelated to today) Rule-8 leak**
      during the broader re-scan: 6 previously-unflagged tracked files
      (`alert_manager/{alert-watcher,dashboard,device-scanner,hw-monitor,watchdog}.service`,
      `alert_manager/install_pihole_pwd.sh`, `scripts/vpn_dns_livetest.sh`) hardcode the real
      dev-box username in a literal path — only `core/vpn-dns-guard.service` was previously
      flagged (PUNCHLIST). **Verified, not assumed:** `install.sh`'s `deploy_services()`
      already `sed`-rewrites the 5 core `.service` files at install time, so those aren't a
      functional bug for other users — `vpn-dns-guard.service` and the two standalone scripts
      are NOT templated, a genuine functional issue if anyone but the original operator runs
      them as-is. **Not fixed** (real code/config change, out of scope for a docs-only pass)
      — added as a new PUNCHLIST entry with exact file:line locations.
    - **Self-caught leak:** my own first draft of that new PUNCHLIST entry repeated the
      literal `/home/<user>/dashboard` path in its own heading — caught on the immediate
      re-scan, fixed to `/home/<user>/dashboard` before anything was committed.
  - **The `01fbcfc`/`6d40e7d` git-history exposure** (the `l3_tls_validation/` code was
    already pushed to the public GitHub repo before this task) **cannot be undone by
    untracking the directory** — flagged prominently as a new PUNCHLIST
    `[OPERATOR DECISION NEEDED]` entry: leave history as-is (permanent GitHub visibility in
    those two commits) vs. rewrite + force-push (destructive, doesn't undo existing
    clones/forks, rewrites SHAs this session's own docs reference). **Not acted on** — Rule 1
    + the git safety protocol (never force-push without explicit request).
  - **Rule-8 scan:** clean on the final diff across all touched public files (home
    paths/IPs/emails), including the self-correction above.
  - **Committed** `c2d9da5` (operator: "commit these as well as the files win 1 has added" —
    no new Window 1 files existed at that moment, confirmed via `git status`/`find -newer`
    before committing). **Not yet pushed.**

- **Full-project disclosure audit (report only, no changes) — operator-directed, consolidating
  the Tier 2 + WiFi-routing carve-outs into one project-wide pass.** Read all 16 ADRs in full
  plus ~15 roadmap docs flagged by a caveat/novelty grep sweep across all 79
  `docs/architecture/`+`docs/roadmap/`+`docs/business/` files; remainder triaged by title/grep
  (simple UI/business stubs, no mechanism content). Reported a full classified inventory
  (public/parameters-private/fully-private/caveat-relocate) — nothing moved. Four items
  approved by the operator for the next pass (see below); two left explicitly public
  (`sandbox-to-system-migration.md`'s promotion mechanics — business/competitive-IP question,
  deliberately deferred, not a security-disclosure one; ADR 0005's general "local checks are
  defeatable" line — standard honest framing, names no technique).

- **CLAUDE.md disclosure-check standing rule — queued, NOT yet written.** Operator asked for a
  new standing TIER 1 rule (apply the disclosure policy going forward on all new work, not
  just retroactively) explicitly conditioned on "once the current audit's approved changes
  have landed (not before)." Drafted the rule text in chat for early review; **held, no file
  touched** — correctly sequenced behind the audit's approved moves (this session and the
  next one below).

- **Disclosure audit — four approved moves, second pass (docs/repo-structure only, no code).**
  Same structure as the Tier 2 carve-out: private location, public placeholder + summary,
  PUNCHLIST cross-reference. Two new private directories created:
  - `~/work/nemesis-internal/l3-tier1-behavioral-trigger/IMPLEMENTATION.md` — the exact
    lateral-movement risk weights + thresholds, moved from all three places they appeared
    (ADR 0009's "Enrollment enriches detection" table, the addendum §2 cross-ref, and
    `adr-0009-l3-behavioral-trigger-scope.md` Piece 2) — found via grep before editing, so
    none were missed.
  - `~/work/nemesis-internal/device-auth-and-identity/` — two files:
    `tamper-response-ladder.md` (ADR 0005 §6's exact 4-step escalation sequence; the general
    proportional/reversible/human-in-the-loop principle stays public) and
    `device-id-confidence-weights.md` (`device-identification.md`'s exact per-signal
    confidence values + 0.85 merge threshold, including the "85-95%/70%/60s" restatement
    found in a second section of the same file — also grepped for before editing).
  - The both-enrolled WiFi edge case (ADR 0009 addendum Open Item 1) extended the **existing**
    `known-limitations/l3-tier2-and-forkb-limitations.md` rather than a new file, since it's
    the same ADR-0009-addendum-Open-Items family already captured there.
  - **Same git-history caveat as `l3_tls_validation/`:** all four pieces of content were
    already committed/pushed well before today (`6580706`, `1893ae8`, `1285a33`, `07699c2`,
    `3fa8b1a`) — confirmed via `git log -S` per item before writing the PUNCHLIST note, rather
    than assuming. New consolidated PUNCHLIST entry added — explicitly the SAME pending
    history-rewrite decision as the Tier 2 one, not a second separate decision.
  - **Rule-8 scan:** clean — diff-only scan for home paths/IPs/emails, plus a residual-value
    grep (`risk \+[0-9]`, `≥70`, `keypair=1\.0`, etc.) confirming none of the exact numbers
    leaked back into the new public-doc lines.
  - **Left unchanged, as instructed:** the two borderline items (sandbox-to-system-migration.md,
    ADR 0005's general defeatable-local-checks line).
  - **Noticed, not touched:** two new untracked files appeared in `l3_tls_validation/`
    (`gate_hardening_validate.py`, `gate_stream.py`) — Window 1 continuing concurrent work on
    the hybrid gate. Not staged, not reviewed as part of this task.
  - **Held for operator review before committing**, per instruction. **Committed** `ae1689e`.
    **Pushed** (`bf38988..ae1689e`).

- **Post-push verification pass (operator: "Window 2: hold for review before pushing... report
  the diff and these confirmations").** Corrected the operator's premise gently — the four
  disclosure-audit items were already committed+pushed by the time this was asked, not
  outstanding. Re-verified against the live/pushed state instead of a pending diff:
  - Grepped all four redacted locations for residual exact values (risk weights, tamper
    ladder text, confidence weights/threshold) — zero hits, all placeholders present.
  - `git show --stat` on both `c2d9da5`/`ae1689e` — no `gate_stream.py`/
    `gate_hardening_validate.py`/other `~/work/nemesis-internal/` content in either commit
    (one harmless commit-message mention of the path as prose).
  - `l3_tls_validation/` tree + `git ls-files` — exactly one tracked file, `README.md`.
  - Rule-8 scan on both commits' full diffs — clean.
  - Flagged the untracked live worklog as the only actually-outstanding item, and that it's
    not normally committed mid-session per the handoff cadence — asked whether the operator
    wanted it committed anyway rather than assuming. No response needed yet; not committed.

- **CLAUDE.md — Window 1 git-writer rule for private-module repos (operator: "Window 2: commit
  the CLAUDE.md change, hold the push").** Found via `git pull` + `git status` that Window 1
  had already written a NEW CLAUDE.md section (uncommitted, working-tree only) — NOT my
  earlier-drafted disclosure-check rule (that one remains un-added, still queued). Verified
  before committing rather than assuming: confirmed
  `~/work/nemesis-internal/l3-tier2-tls-interception/` is now genuinely its own git repo
  (`git status`/`git log`/`git remote -v` from inside it — one commit `302bf3b`, a `usb`
  remote at `/run/media/paul/storage/nemesis-internal-git/...`, working tree clean, contents
  match: `IMPLEMENTATION.md`, `code/` incl. the new `gate_proxy.py`/`gate_validate.py`/
  `gate_stream.py`/`gate_hardening_validate.py` Piece-J-hardening files). The new CLAUDE.md
  section establishes Window 1 as git-writer for that private repo (same discipline as Window
  2's public practice — verify, Rule-8 scan, push to configured remotes), explicitly scoped
  separate from Window 2's public-repo sole-git-writer rule (not a change to it). Rule-8
  scan on the CLAUDE.md diff itself: clean. **Committed** `ea00d83`. **Holding the push**,
  per explicit instruction — this is the only unpushed commit right now.
  - **Still outstanding, not part of this commit:** my own drafted disclosure-check standing
    rule (queued behind "once the audit's approved changes land" — they have, but the
    operator hasn't yet asked me to add it; distinct from Window 1's change committed here).

- **Disclosure-check standing rule — added (operator: "yes, add the disclosure-check rule now.
  verify it does not exist first").** Grepped CLAUDE.md for the rule's key terms
  ("disclosure-check", "novel mechanism", "honest-limitation", "source-visibility rule", etc.)
  before writing anything — zero hits, confirmed not already present. Added as TIER 1 Rule 10,
  right after Rule 9 (Handoff discipline), same text drafted earlier in the conversation:
  flag genuinely novel mechanisms or honest-limitation/caveat language for a public/private
  decision before any public-repo commit, applying the audit's established policy (general
  architecture/capability-existence public by default; novel implementation, tuning
  parameters, and unresolved-weakness caveats flagged, not auto-committed); explicit
  source/doc-visibility framing, never a tier gate; standing/ongoing, not retroactive-only.
  Rule-8 scan on the diff: clean. **Committed** `751d340`. **Pushed** (`ae1689e..751d340`).

- **History-rewrite: residual-exposure verification + Group-A rehearsal + prep (operator-
  directed, verification/prep only — no real execution, no force-push).**
  - **Release download counts** (`gh api repos/.../releases/tags/<tag>`): all 6 releases in
    range (`v1.0.2`–`v1.0.7`; `v1.0.1` has a tag but no published release) show 0-2 downloads
    on their attached **binary** assets (compiled Windows agent installers) — not the source
    tree, so this doesn't settle Group B's exposure question by itself (GitHub doesn't expose
    download counts for the auto-generated source zip/tarball). Checked GitHub's traffic API
    too as a more relevant signal: 103 clones/48 uniques over 14 days with a 90-clone spike on
    one day — most plausibly this session's own repeated `git pull`/`git fetch` activity given
    0 stars/forks/watchers/PRs, flagged honestly as not certain rather than asserted.
  - **Group-A rehearsal**, in a throwaway `git clone --mirror` (never touched `origin`):
    installed `git-filter-repo` in an isolated venv (PEP-668). **First attempt caught a real
    bug**: `--path l3_tls_validation --invert-paths` (bare directory) also stripped the
    intentional `README.md` placeholder — exactly what rehearsing in a mirror is for. Fixed by
    listing the 13 tracked files individually instead of the directory, plus a
    `--blob-callback` doing an exact byte-extracted (not hand-transcribed) substring
    replacement for the both-enrolled edge-case paragraph.
  - **Final rehearsal, fully verified:** target content (13 `l3_tls_validation/*` files across
    `b9ec952`/`01fbcfc`/`6d40e7d`, the both-enrolled text in `6580706`) completely gone from
    all history; **Group B's 3 commits and all 10 tags (incl. the disaster-recovery tag
    `pre-l1l2l3-build-known-good`) confirmed BYTE-FOR-BYTE IDENTICAL SHA before/after** — zero
    incidental touch. Blast radius: 3 commits cleanly pruned, 24 get new SHAs (vs. Group B's
    242), final tree matches the live repo exactly (`git ls-tree` diff empty).
  - **Backup taken** before any real execution: full verified `git clone --mirror` of the
    current real repo to
    `/run/media/paul/storage/nemesis-state-backups/2026-07-26-1600-pre-history-rewrite-group-a/`
    (mount verified first per the State Snapshots rule), `git fsck --full` clean, `STATE.txt`
    written.
  - **Decision recorded + evaluation doc updated**: Group A approved for rewrite, Group B
    explicitly accepted as residual risk (ages out via normal parameter rotation, not a
    git-log problem) — appended to
    `~/work/nemesis-internal/known-limitations/history-rewrite-evaluation-2026-07-26.md`
    (sections 8-12: decision, download-count/traffic data, rehearsal detail, backup, status).
  - **PUNCHLIST.md updated**: the two open `[OPERATOR DECISION NEEDED]` entries replaced with
    one `[RESOLVED — execution pending go-ahead]` entry, deliberately vague per Rule 10 (points
    to the private eval doc, states the decision shape without restating exposed content or
    commit hashes). Rule-8 scan on the diff: clean.
  - **Nothing executed against the real repo. No force-push.** Holding for explicit operator
    go-ahead on the rehearsed result specifically, per instruction. `PUNCHLIST.md` itself also
    held, uncommitted, pending report-back.

- **History rewrite EXECUTED (operator: "Go-ahead: execute the Group-A rewrite for real and
  force-push").**
  - Re-verified zero drift since rehearsal (`git fetch`; local HEAD == `origin/main`, both
    `751d340`; only the already-held `PUNCHLIST.md` edit outstanding).
  - Executed against a **fresh `git clone --mirror`** (not the working directory directly, to
    keep a clean rollback path) — identical `git-filter-repo` invocation as the rehearsal (13
    explicit `--path` args + the same `--blob-callback`).
  - **Re-ran the full verification battery against this real mirror before it touched
    origin** — identical to the rehearsal: tree matches live repo exactly, Group B's 3
    commits + all 10 tags byte-for-byte unchanged, 3 commits pruned, 24 get new SHAs, 383
    final commits.
  - Re-added `origin` (filter-repo strips it) and force-pushed:
    `git push origin --force --all` then `--force --tags`. Push output showed `main`
    force-updated (`751d340...b8b3713`) and one tag reported as `[new tag]`
    (`build1-methodb-known-good` — apparently hadn't been pushed to origin before; its
    commit target was already unchanged/verified, so this only added a missing pointer, not
    new exposure).
  - **Specifically re-verified the disaster-recovery tag** via `git cat-file -p` on both the
    pre-rewrite backup mirror and a fresh post-push clone from GitHub: tag message, tagger,
    date, and target commit (`14b066b...`) identical byte-for-byte. Only the annotated tag's
    own wrapper-object SHA changed (filter-repo re-serializes all tag objects as routine
    behavior) — the commit `docs/operations/backupproc.md` actually references did not change.
  - **Final proof, from a genuinely fresh clone directly from `github.com/nemesis981/
    dashboard`** (not local state): 383 commits; `l3_tls_validation/` = only `README.md`;
    zero hits for the both-enrolled text or any `l3_tls_validation/*.py` content anywhere in
    history; Group B's risk weights / tamper ladder / device-ID weights all still present and
    intact at their original (unchanged) commits.
  - **Updated local `~/dashboard`**: stashed the held `PUNCHLIST.md` edit, `git fetch` +
    `git reset --hard origin/main`, restored the stash. New local HEAD `b8b3713`, matches
    origin exactly, working tree clean apart from the still-held `PUNCHLIST.md` edit.
  - **Outcome: exactly as rehearsed, no surprises, no deviation.**
  - Updated `PUNCHLIST.md`'s entry from "execution pending" to "done" (Rule-8 scan: clean) and
    the private eval doc's §12 to record the full execution trail. Both held for report-back
    before the (ordinary, non-force) follow-up commit/push.
  - **Follow-up commit/push (operator: "commit and push it").** Re-verified zero drift
    (`git fetch`; local == origin, both `b8b3713`) before staging. Committed `3ecacc8`,
    pushed normally (`b8b3713..3ecacc8` — ordinary push, not force; only `PUNCHLIST.md`
    changed, no history rewrite involved this time). Post-push drift check: local == origin,
    0 ahead / 0 behind, confirmed clean. This closes out the history-rewrite task end to end.

- **Scratch cleanup (operator: "clean up the scratch files").** Removed
  `scratchpad/history-rewrite/` (36MB — both throwaway mirror clones, the verify-clone, the
  git-filter-repo venv, and all tree-comparison/blob-callback helper files) — all disposable,
  durable backup lives independently on the USB drive. Left three small leftover files
  (~26KB) from the morning's unrelated roadmap-audit task; flagged rather than assumed —
  no response yet on whether to clear those too.

- **Private-module remote sync check + CLAUDE.md wording audit (operator-directed, docs +
  verification only).**
  - **Remote sync:** Window 1 had since configured all three remotes for
    `l3-tier2-tls-interception` (`local` — a new bare repo at
    `~/work/nemesis-internal-git/`, `usb`, and `origin` now pointing to a **private** GitHub
    repo, `nemesis981/nemesis-l3-tier2-tls-interception`). Verified: all three report the
    identical HEAD `9075e4c`, matching the working copy. Confirmed via `gh api` that the
    GitHub repo is genuinely `private: true` (0 forks/stars) before treating it as the
    intended low-visibility remote.
  - **CLAUDE.md wording audit:** re-read the "local + USB + private GitHub" rule literally —
    it's scoped to "Window 1... these private repos" specifically, and the adjacent bullet
    already says Window 2's public-repo scope "does NOT change or extend that rule." So the
    public dashboard repo's single `origin` remote (confirmed via `git remote -v` — no
    local/USB analog exists anywhere for it) is **not actually a violation** of the rule as
    written. But the wording was loose enough that Window 1 read it as possibly universal —
    a real ambiguity, not something to dismiss just because a close reading resolves it.
    Tightened the CLAUDE.md text to state explicitly *why* the asymmetry is intentional (a
    public GitHub repo is inherently its own durable off-site copy; a deliberately
    low-visibility private repo isn't, hence the extra local+USB redundancy specific to
    private modules) rather than leaving a future window to re-derive it. Did **not** build
    out new local/USB remotes for the public dashboard repo — the audit concluded that would
    be solving a problem the rule never actually specified.
  - Rule-8 scan on the CLAUDE.md diff: clean. Held for report-back before committing.
  - **Committed** `5cece58`, **pushed** (`3ecacc8..5cece58`). Post-push drift check: local ==
    origin, 0 ahead / 0 behind, confirmed clean.
