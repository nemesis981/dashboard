# Worklog — 2026-07-29-001 (Window 2, closeout reconstruction)

Raw chronological log. This session (Window 2) started at closeout time and reconstructed
the day from git history rather than a live append-as-you-go feed — no Window 2 worklog was
kept earlier today. Times are commit timestamps, not session times.

- 07:01 `7babe5c` — operator-requested full status audit of all 64 roadmap items + 18 ADRs,
  `docs/audits/proposed-features-status-2026-07-29.md`.
- 08:04 `e76b964` — unblocks alert_watcher Commit B: `test_quarantine.py` sys.path fixed to
  resolve `alert_watcher` from `core_module/alert_watcher/`; its `DB_PATH` was tree-relative
  and pointed at a location `alerts.db` hasn't lived in since the DB move — silently created
  and tested against an empty stray DB. Now resolves via `nemesis_paths.db_path()`. Also fixes
  `deploy_nemesis_fwd.sh` `check_sources()` path and adds a landmine-defusing comment in
  `gen_units.py`.
- 08:04 `bfd7983` — `nemesis_fwd.py` `_db()` never closed its connection despite
  `with _db() as conn:` looking correct (sqlite3's context manager only handles
  commit/rollback, not close) — 3 fds leaked per request on a long-lived socket server.
- 08:06 `a096def` — removes stale `alert_manager/alert_watcher.{py,service}`. **core_module
  layout move now 6/6** (was 5/6 as of last night's closeout).
- 08:06 `eeb5218` — gitignore `docs/testing/` (local security-test artifacts).
- 09:11 `459585b` — `lookup_mac_vendor()` was storing macvendors.com's raw 404/429/500 error
  bodies verbatim as device friendly_names; confirmed live, a device was literally named
  `{"errors":{"detail":"Not Found"}}`.
- 09:13 `5849a3b` — Open Item #6: `sudo nmap` was dead under `NoNewPrivileges=yes` with no
  return-code check, so a scan that couldn't run at all looked identical to an empty LAN.
  Replaced with unprivileged `nmap -sn` + reading `/proc/net/arp`. Also fixes bare `print()`
  block-buffering (~7.5hr logging blackout under systemd's pipe).
- 10:47 `02178fa` — pre-design audit for a structured error-code system, prompted by three
  same-day bugs all being "can't distinguish 'checked, found nothing' from 'the check failed'"
  — this is where the dashboard root-logger bug (below) was surfaced.
- 11:28–12:17 Data Manager rollout (`5bd6259`→`296ca86`→`8f24625`→`a0f679e`→`8598a24`→
  `0a91322`→`ceb7c60`→`41ed3fb`→`ca023b6`→`03f38fd`→`2bfdd2e`→`f00997b`→`5e9e23d`):
  - `5bd6259` registers a "dashboard" Data Manager namespace in **WARN mode**, granting four
    uncontested tables, flagging seven table-ownership conflicts.
  - `296ca86` fixes `dashboard.py` never calling `logging.basicConfig()` — every `log.info()`
    was silently dropped.
  - `a0f679e` resolves five of the seven ownership conflicts (column grants for
    `enrollment_tokens`/`agent_devices`; shared-writer grants for `scan_jobs`/`audit_log`/`users`).
  - `8598a24` resolves the remaining two (`scan_threats`/`scan_schedules`) by moving their DDL
    to `database.py`, dropping `hw_monitor`'s now-unneeded grant. **All seven conflicts
    resolved as of this commit.**
  - `0a91322` fixes `nemesis_fwd`'s own `audit_log` write bypassing its own registered grant.
  - `ceb7c60` fixes `alert-watcher` recording alerts as auto-quarantined before the ufw block
    was confirmed to succeed — now pending on failure, not a false success claim.
  - `ca023b6`, `41ed3fb` — punchlist bookkeeping for the above + a Rule-10 relocation of
    sensitive reauth-gap findings to the private known-limitations doc.
  - `03f38fd`→`5e9e23d` mechanically migrate all remaining `dashboard.py` read/write sites off
    raw `sqlite3.connect(DB_PATH)` onto the guarded `_dm_conn()`. `03f38fd` also fixes a bug
    caught by Window 1 review before landing: `settings_page()`'s renamed connection variable
    left two stale references, silently reverting anomaly_detection/AbuseIPDB/CISA settings to
    default on every page load via a swallowed `NameError`.
  - **End state:** zero raw `sqlite3.connect(DB_PATH)` sites remain in `dashboard.py`
    (confirmed by grep). Dashboard namespace fully registered, all writers resolved, still
    **WARN mode**, flip to ENFORCE deliberately deferred pending a quiet-journal period.
- 13:54–13:55 `0b404f4`, `0e571f2`, `ed2c889` — severity/vocabulary fixes. `0b404f4`
  replaces two byte-identical private severity dicts (watchdog, malware_detection) with one
  shared ladder + `meets_threshold()`; fixes a live bug where lowercase `"critical"` ranked as
  0 and never crossed a HIGH ticket threshold — malware tickets silently never fired.
  `0e571f2` converts malware_detection to canonical-uppercase writes / case-insensitive reads
  (permanent, not one-time — ~36.7k pre-existing lowercase rows in `malware_findings`).
  `ed2c889` makes `get_review_queue`'s risk_level filter case-insensitive to match.
- 14:03 `79f1c06` — CLAUDE.md: formalizes Window 3 (overflow, never a git-writer, per-task
  model self-check).
- 14:20 `941ec51` — CLAUDE.md Rule 11: test data written to live `alerts.db` must carry the
  literal phrase "test data" + date.
- 15:35–16:36 Ransomware canary incident (`7373b10`→`9cc7746`→`c40f53d`→`1705e9c`→`d577e10`):
  `ProtectHome=yes` on `malware-canary.service` masked `/home` in its mount namespace since
  2026-07-25 — every bait file read as deleted, ~33,500 false CRITICAL findings, 14,441
  exhausted outbound-mail sends, zero real detection the whole time. `7373b10` fixes
  `ProtectHome`→read-only. `9cc7746` fixes non-atomic plant+baseline (would have kept
  false-tripping even after visibility restored) and swaps to plausible bait filenames.
  `c40f53d` supplies the second missing precondition — a traverse-only ACL so the canary
  account can reach the install user's home at all — and updates `install.sh`. `1705e9c` adds
  dedup/cooldown so one stuck condition produces one finding, not a flood (verified: 4-file
  deletion → 4 rows, then silent across 6 polls). `d577e10` fixes the ambiguity that made all
  of this possible: `os.path.isfile()`-returns-False meant either "deleted" or "I can't see
  it" — now disambiguated by parent-directory visibility, same fix applied to
  `_module_enabled()`'s DB-read-error-reads-as-disabled bug (101 skipped polls silently
  misreported as self-gated-off). `c40f53d` also added a "Ransomware canary" section to
  HANDOFF.md documenting the two preconditions — written mid-thread, predates the dedup/cooldown
  and disambiguation fixes.
- 16:37–17:54 Fork B / nemesis-fwd / host-defense (`2530977`→`517b1e0`→`8c28525`→`480b1c8`→
  `1d63734`→`19c783f`→`86a4c47`): `2530977`+`517b1e0` fix flask-login missing from
  install.sh/requirements — watchdog, hw_monitor, and malware-canary all silently failed to
  import `modules/tickets/module.py` and couldn't file tickets; only `dashboard.py` (installed
  `--user`) ever worked. `8c28525` syncs a stale doc to the locked Fork B MIRROR decision.
  `480b1c8` records, un-actioned, that PIA is installed but left disconnected (Nemesis errors
  while it's active) — filed for later. `1d63734` adds source-NAT for Fork B tunnel-routed
  inspection with a derived (not pinned) egress interface, refusing to install any rule when no
  non-tunnel default route exists; known gap: PIA-down-only. `19c783f` removes two
  never-implemented `nemesis-fwd` ops (`add_rule`/`remove_rule`). `86a4c47` responds directly
  to last night's pen-test finding of no host self-defense: LAN-internal severity now derives
  from Suricata priority when no external reputation data exists; a new narrow `fail2ban`
  PEER_POLICY peer (block_ip/deny_ip only); `:5001` moves to ThreadingHTTPServer plus new
  token-bucket pacing — nothing deployed yet as of that commit, `NEMESIS_NEVER_BLOCK` unset.
- 18:15 — `nemesis-fwd` service restarted (confirmed via `ActiveEnterTimestamp`), picking up
  the `86a4c47` host-defense changes and, as a side effect, the `bfd7983` fd-leak fix from
  08:04 — **Open Item #10 now deployed, not just landed on disk.**
- 19:24 `800b5e2`, `31aee8f` — Window 2 (this session): ADR 0019 stub (novel nftables
  enforcement-point mechanism, Rule 10 disclosure decision — full writeup kept private in
  `~/work/nemesis-internal/firewall-enforcement-engine/`, public repo gets architecture/decision
  level only); PUNCHLIST capture of the Headscale VPN-swap evaluation (capture-only, nothing
  built, recommendation: do not migrate). Both independently Rule-8/Rule-10 checked, pushed.
- (concurrent) Window 1/3 — private-module remote-redundancy policy changed (operator
  decision): new private modules default to **local + USB only, no GitHub remote**;
  `l3-tier2-tls-interception`'s existing three-remote setup is a flagged, left-open exception.
  ADR 0019 gained an "Explicitly NOT solved by this ADR" section (in-path inspection delivery
  is separate, unscoped work). Both landed as uncommitted WIP in the shared working tree;
  handed to Window 2 with a diff-audit note. Window 2 independently Rule-8/Rule-10 scanned and
  committed: `e9dab6e` (CLAUDE.md), `2b75b27` (ADR 0019).
- Diff audit (this session, before closeout) — reviewed `800b5e2`/`31aee8f` (own, already
  pushed) and the two handed-off files above; confirmed `git fetch` showed no origin drift;
  working tree otherwise clean (only expected gitignored artifacts).
