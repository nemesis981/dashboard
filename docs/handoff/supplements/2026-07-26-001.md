# Session supplement — 2026-07-26-001 (Window 2, docs/audit/git-writer)

Curated from the live worklog (`docs/handoff/worklog/2026-07-26-001.md`) at closeout. Full
blow-by-blow (including corrections/retries) lives there; this is the durable narrative.

---

## Morning status + roadmap audit
Lines of code 34,819 at session start. All six services active, tree clean, local == origin.
Roadmap-vs-state audit found +4 file-set drift since the 07-25 baseline (all four traced to
same-session 07-25 work, not new drift), zero shipping drift. New tally at the time: 4
SHIPPED / 8 PARTIAL / 51 PARKED, 63 total. Refreshed again at closeout — see "Closeout roadmap
audit" below.

## CLAUDE.md rule additions (four separate edits today)
1. **Per-window model pins** — Window 1 = Opus (currently Opus 5), Window 2 = Sonnet
   (currently Sonnet 5), role-first with version as a bumpable detail, plus a first-action
   load-time model self-check convention. `2805af4`.
2. **Disclosure-check standing rule (Rule 10)** — before any public-repo commit, flag
   genuinely novel mechanisms or honest-limitation/caveat language for a public/private
   decision rather than silently defaulting either way; explicitly a source/doc-visibility
   rule, never a tier gate. Verified it didn't already exist before adding. `751d340`.
3. **Window 1 as git-writer for private-module repos** — carved-out private modules
   (starting with `l3-tier2-tls-interception`) live and version outside the public repo;
   Window 1 authors and pushes that code to its own remotes, following the same
   verify/Rule-8/push discipline Window 2 uses publicly. `ea00d83`.
4. **Remote-scope clarification** — tightened the above after Window 1 flagged real ambiguity
   about whether the "local + USB + private GitHub" three-remote pattern was meant to apply to
   the public dashboard repo too. Audited: it doesn't (the rule is scoped to private modules
   only) and the public repo's single-`origin` setup is correct as-is — but the wording was
   loose enough to misread, so it now states explicitly *why* the asymmetry is intentional
   (public GitHub already is the public repo's durable off-site copy; a deliberately
   low-visibility private repo isn't). Verified all three of `l3-tier2-tls-interception`'s
   remotes (local/USB/private-GitHub) report the same HEAD before writing this. `5cece58`.

## Three new ADRs — deployment model + venue market direction
- **ADR 0014 (deployment-appliance-model)** — a dedicated Linux appliance (mini PC) becomes
  the primary SMB/venue deployment target, replacing Windows-hosted-VM-as-primary; a genuine
  reversal, stated as such. Cross-platform requirement narrows to the agent only. Home-user VM
  path retained unchanged (WSL2/Hyper-V/dual-boot re-confirmed rejected this session). Flagged
  explicitly: the referenced "locked June 22 release sequence" doc doesn't exist anywhere in
  this repo — substituted `ROADMAP.md`'s Windows Support Status section as the in-repo item
  being reversed.
- **ADR 0015 (guest-self-service-enrollment)** — QR/captive-portal venue guest enrollment.
  Cross-references ADR 0012's existing VENUE AUTO mode as the actual closer relative to ADR
  0011. **Flagged, not resolved:** real tension with `venue-guest-network.md`'s pre-existing
  "app IS the credential" framing vs. today's captive-portal-without-mandatory-app direction.
- **ADR 0016 (guest-marketing-capture)** — opt-in, export-API-only marketing capture (explicit
  non-goal: never becomes an ESP). Legal-review gate stated in the status line. Includes a PII
  network-transit addendum reusing ADR 0009's Tier 1 engine at the export-API chokepoint.
- Committed together as `ffe370a`.

## fd-leak in `anomaly_detection` — confirmed still unfixed
Live `journalctl` evidence: `anomaly_detection`'s `_detection_cycle` fd leak on
`/var/log/suricata/eve.json` is still recurring every poll cycle, no code change landed.
Tracked as a new `[FIX-NOW]` PUNCHLIST item (not marked done). Added a full diagnostic-chain
troubleshooting section to `docs/reference/operational-notes.md` (check the log before
restarting; systemd's rate limiter vs. a new bug; two debugging gotchas from the session:
root-owned files from a manual `sudo` run, and confirming the service's actual user before
trusting a manual run's "module not found"). **Still open, not fixed this session.**

## ADR 0009 Fork-B mirror resolution + Tier 2 hybrid inline/mirror gate
Resolved the mirror-vs-inline documentation gap Open Item 1 had flagged: Fork B's transport is
confirmed **MIRROR** (Piece 2 forwards unconditionally, Piece 3 is passive `af-packet` — never
a real contradiction, just an undecided default until now). Added the new hybrid inline/mirror
gate design to Tier 2 (Piece J in the TLS scoping doc): the first meaningful chunk of decrypted
application data is held inline before delivery, then the connection transitions to mirror for
the rest of its life — with four transition-hardening requirements against a timing-based
bypass. Corrected surrounding language: Tier 2 does not guarantee first-contact prevention;
Tier 3 is the actual backstop. `bf38988`.

## Full-project disclosure audit → two-pass private-module carve-out
Consolidated the Tier 2 + WiFi-routing carve-outs into one project-wide pass: read all 16 ADRs
+ ~15 flagged roadmap docs, classified every candidate novel mechanism/caveat as public,
parameters-private, fully-private, or caveat-relocate. Report-only first (nothing moved), then
two approved carve-out passes:

1. **Tier 2 implementation detail** (`c2d9da5`) — `l3_tls_validation/`'s code (13 files) and
   the TLS scoping doc's Pieces E/F/G/H/J moved to `~/work/nemesis-internal/` (outside the
   public repo, modeled on the existing `~/work/nemesis-private/` pattern: a plain directory,
   not version-controlled at the time, referenced by path only). Public docs now carry short
   summaries + pointers instead of full mechanism detail.
2. **Four more items** (`ae1689e`) — the both-enrolled WiFi edge case, the exact
   lateral-movement risk weights/thresholds, ADR 0005's exact tamper-response escalation
   sequence, and `device-identification.md`'s exact confidence weights/merge threshold. Same
   structure: private location, public placeholder + summary, PUNCHLIST cross-reference.
   Two items deliberately left public on judgment calls (reasoned in the private review log):
   `sandbox-to-system-migration.md`'s promotion mechanics (business/competitive-IP question,
   not a security-disclosure one) and ADR 0005's general "local checks are defeatable" line
   (standard honest framing, names no technique).

Both passes found the moved content was **already exposed in public git history** (pre-dating
this session) — flagged rather than silently accepted, leading to the history-rewrite work
below. Also found and flagged (not fixed) a real, pre-existing, unrelated Rule-8 leak: six
systemd-unit/script files hardcoding the dev box's real username in a literal path — verified
`install.sh` already templates most of them at install time (not a functional bug for other
users); `vpn-dns-guard.service` and two standalone scripts are not templated (a genuine issue).

## Group-A git-history rewrite — executed and verified
Evaluated the git-history exposure question in full (release download counts, GitHub traffic,
tag/release reachability, blast radius). Decision: rewrite the recent, non-tagged/non-released
content (`l3_tls_validation/`'s code + the both-enrolled edge-case text); accept the older,
tag/release-entangled content as residual risk rather than rewrite it (242-commit blast radius,
risk to the `pre-l1l2l3-build-known-good` disaster-recovery tag, and the rewrite wouldn't even
fully close the exposure since GitHub Release archives don't retroactively regenerate).

Rehearsed in a throwaway mirror first (caught and fixed a real bug: an over-broad path filter
would have also stripped the intentional `l3_tls_validation/README.md` placeholder). Took a
full verified backup mirror on independent storage. Executed for real, force-pushed, then
proved the result from a genuinely fresh clone directly from GitHub:
- Target content completely gone from all history.
- **Group B's 3 commits and all 10 tags — including the disaster-recovery tag
  `pre-l1l2l3-build-known-good` — confirmed byte-for-byte identical SHA before and after.**
  `docs/operations/backupproc.md`'s revert reference (`14b066b`) is unaffected.
- Blast radius: 3 commits cleanly pruned, 24 got new SHAs (vs. 242 for the rejected
  alternative), 383 final commits.

Full technical detail + the decision reasoning live in
`~/work/nemesis-internal/known-limitations/history-rewrite-evaluation-2026-07-26.md`
(deliberately not in this repo — the evaluation itself maps where sensitive content sat in
history). **Minor non-blocking follow-up:** a handful of docs (this supplement included, by
necessity) mention the pre-rewrite commit hashes (`01fbcfc`, `6d40e7d`, `6580706`, `b9ec952`)
— those are now stale references (describe commit IDs no longer on `main`), not broken links.

## Window 1's private-module repo
`~/work/nemesis-internal/l3-tier2-tls-interception/` is now its own git repository (Window 1 is
its git-writer, per the new CLAUDE.md rule), with three remotes — `local` (a bare repo on this
machine), `usb`, and `origin` (a **private** GitHub repo, confirmed `private: true`). All three
verified reporting the same HEAD (`9075e4c`) after Window 1's Step 3 push (sandboxing, kill
triggers, forensics, taxonomy work continuing on the hybrid gate).

## Closeout roadmap audit
Refreshed `docs/audits/roadmap-state-audit-2026-07-26.md` — tally unchanged from this morning
(4 SHIPPED / 8 PARTIAL / 51 PARKED, 63 total; no `docs/roadmap/` files added/removed since,
none of the 12 non-parked items touched). Supersedes 2026-07-25 as the Morning Status baseline.

## Scratch cleanup
Removed the `~/scratchpad/history-rewrite/` working directory (throwaway mirror clones, the
git-filter-repo venv, verification helper files) after the rewrite was confirmed done — the
durable backup lives independently on the USB drive.
