# Session Supplement — 2026-08-07-001 (Window 2)

> Append-only durable log. Curated from the live worklog
> (`docs/handoff/worklog/2026-08-07-001.md`), which has the raw blow-by-blow.
> **Checkpoint closeout — the operator plans to return in a couple hours, so a second
> closeout is expected later today.** This entry covers the session through this point;
> if a second closeout happens today it will be a new dated supplement
> (`2026-08-07-002.md`), not an edit to this one.

## What shipped, in order

1. **Morning status + roadmap-vs-state audit.** Baseline `roadmap-state-audit-2026-08-06.md`
   (8/10/58, 76 total) confirmed current. Traced an apparent 76=76 file-count match and found
   it coincidental rather than a clean zero-drift signal — worth remembering as a process
   note, not a live issue.
2. **CLAUDE.md Rule 13 added** (`5a781cc`) — host-level network changes need a proven revert,
   not a claimed one; prefer the isolated VM fleet over the operator's real machine. Drafted
   from the 2026-08-04→08-07 Tailscale exit-node incident (full postmortem in
   `known-limitations/tailscale-exit-node-persistence-2026-08-07.md`) where a shell `trap`
   was handed to the operator with a false auto-revert claim and left his workstation routed
   through a test VM for ~2 days 20 hours.
3. **Read-only audit of vpn-dns-guard + firewall/DNS code for the same unverified-revert
   pattern** — three findings, written to the private mirror only per Rule 10
   (`audits/vpn-dns-firewall-revert-verification-audit-2026-08-07.md`). Operator's call: keep
   private, no public PUNCHLIST entry.
4. **Untangled Window 1 and Window 3's concurrent edits in shared files**, hunk-level, six
   commits (`5328ef5`, `8e1729d`, `ddf3158`, `7ac0e56`, `506f049`, `62032db`) — Window 3's new
   connectivity notifier + its `database.py`/`vpn_dns_guard.py` hunks, Window 1's
   `enrollment_tokens` schema hunk and two `vpn_dns_guard.py` fixes (the audit's Finding 1 fix,
   and an unrelated `--status` bugfix found during today's Pi-hole-auth false-alarm chase).
5. **Window 1's mint-at-download build** (`71ea2fd`) — closes the tskey-exposure finding from
   today's Tailscale-key audit (22 plaintext keys retained indefinitely). Re-ran the suite
   myself before staging (41/41 — the handoff's stated 30/30 was stale, not a real
   discrepancy).
6. **Window 3's DHCP mode-switch fail-over** (`e2c55b8`, 78/78 re-run live) and the
   **diagnostics_watcher connectivity-notify wiring** that never actually landed earlier in
   the day (`7f5f958` — completes the pairing with `506f049`'s `vpn_dns_guard.py` half).
7. **Track C steps 1 and 2**, committed and correctly separated (`ccf02aa` consent gate
   43/43, `e14e5a4` connection-event schema 52/52) after catching that the operator's
   "step 2, 52/52" request actually spanned two distinct private-handoff commits with
   different test counts — flagged before committing rather than bundling.
8. **Scoping ADR for a fully AI-managed autonomous operations mode** — private repo only
   (Rule 10), status PROPOSED/not decided, no recommendation either way. Written to the new
   `~/work/nemesis-internal/adr/` directory, `ADR-priv-0001-ai-managed-operations-mode.md`,
   committed `cc6bbc3`, pushed to both `local` and `usb`. Key finding worth carrying forward:
   the L0-L4 graduated-authority scaffold this direction would need
   (`ai_authority`/`ai_proposals`/`ai_standing_rules` in `modules/ai_engine/module.py`)
   already exists, fully designed and self-tested, and is **completely unwired** — zero
   writes anywhere outside its own table creation.
9. **Window 1's DHCP live-run fixes** (`c064e2b`, 83/83 re-run live) — the module's first-ever
   run against a real dnsmasq daemon under the real sandbox surfaced four defects invisible
   to 159 passing unit tests: a lease-file permission denial, an unwritable pidfile path, a
   privilege-drop failure, and — the significant one — segmentation being mechanically inert
   (`set:` tags a client, it does not restrict a range; needed `tag:`). Caught a scoping
   mismatch in the operator's request ("7 held files") against ground truth (Window 1's
   handoff shows DHCP is 2 files; the other 13 were two unrelated feature groups) and scoped
   the commit to just the described DHCP pair.

Public repo: `72b7568` (yesterday's close) → `c064e2b`, 12 commits today, all Window 2,
all pushed, `local == origin` confirmed after every push.

## Verification discipline actually applied (Rule 3)

Every commit above was compiled and its test suite re-run live by this session before
staging — not trusted from a handoff claim. Two live discrepancies caught this way: the
mint-at-download suite's stated 30/30 vs. actual 41/41 (stale count, not a defect), and the
Track C "step 2" request actually covering two separately-tested private commits. Every
shared-file commit was hunk-verified in isolation (`git diff` hunk headers checked before AND
after staging) to confirm no cross-window contamination, per the CLAUDE.md shared-index
hazard from 2026-08-02.

## Open / held for next session — do not assume done

**Still sitting dirty in the public tree, NOT committed, per Window 1's 08-07 "MINE — ready
to commit" list (their own verification was interrupted mid-pass — re-run tests live before
trusting any of it, same as everything above):**

- **Track C schema v2 — one feature, 10 files, not yet touched this session:**
  `nemesis_agent/conn_events.py`, `nemesis_agent/test_conn_events.py`,
  `nemesis_agent/conn_collector.py` (new), `nemesis_agent/test_conn_collector.py` (new),
  `nemesis_agent/tools/etw_probe.py` (new), `core_module/hw_monitor/test_conn_ingest.py`
  (new), `core_module/hw_monitor/hw_monitor.py` (ingest + consent gate + reaper),
  `alert_manager/database.py` (Track C tables), `alert_manager/data_manager.py` (namespace
  grant), `dashboard.py` (init call).
- **Rule 13 continuation — 2 files:** `alert_manager/nemesis_fw_watch.py`,
  `nemesis_agent/dns_enforce.py`. These are Window 1's own fixes for audit Findings 2/3
  (`2026-08-07-window1-to-window2-revert-verification-fixes.md`) — distinct from the
  `vpn_dns_guard.py` half already committed as `7ac0e56` today.
- **Tooling — 1 new file:** `scripts/nemesis-pihole-password.sh`.
- **Confirmed NOT mine, do not stage:** `alert_manager/install_pihole_pwd.sh`,
  `docs/roadmap/venue-guest-network.md` (pre-existing, every session this week+, still nobody
  has claimed it).

**DHCP module status:** code + tests now fully committed and pushed (`e2c55b8`, `c064e2b`),
but **not deployed anywhere**. The gateway test zone it was live-tested against is a separate,
uncommitted state (not this repo) — deploying the committed code there is a distinct decision
not made today.

**A fifth DHCP defect found, explicitly not fixed:** `status()` reports `state: running` while
the daemon is crash-looping — an observability gap, not derived from real daemon health.

**From earlier in the day, still open, unchanged:** the enrollment-token vestigial-tables
removal audit and the QUIC/nftables ADR 0022 — both were Window 2's carried-forward items
from yesterday's HANDOFF and were not touched today; today's session was entirely consumed by
the exit-node incident response, the revert-verification audit, and landing the day's build
backlog across three windows.

## Cross-references

- `docs/handoff/worklog/2026-08-07-001.md` — raw chronological log, written live.
- `~/work/nemesis-internal/audits/vpn-dns-firewall-revert-verification-audit-2026-08-07.md`
- `~/work/nemesis-internal/adr/ADR-priv-0001-ai-managed-operations-mode.md`
- `~/work/nemesis-internal/known-limitations/tailscale-exit-node-persistence-2026-08-07.md`
- `~/work/nemesis-internal/handoff/2026-08-07-window1-handoff.md`,
  `2026-08-07-window3-handoff.md` — full per-window detail.
- Prior day: `docs/handoff/supplements/2026-08-06-001.md`.
