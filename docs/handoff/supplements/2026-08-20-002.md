# Supplement — 2026-08-20-002 (Window 2, post-restart session)

Curated narrative. Raw detail: `docs/handoff/worklog/2026-08-20-002.md`. Continues from
`2026-08-20-001.md` (the operator restarted this window mid-day to pick up a tool update;
no work happened in the gap).

## What happened this session

1. **Resumed and confirmed** the three commits held at the prior pause (`f91db98`,
   `f812583`, `8948e09`) were still the only unpushed work — nothing landed upstream while
   the window was down.
2. **Committed Window 3's held batch** — `nemesis_agent/install_linux.sh` (rewritten for
   unattended/fleet provisioning: PEP-668-safe venv, enrollment-aware with `device_id`
   persisted, the `cp -r` keypair copy replaced with a filtered `tar` + post-copy
   assertion) and `nemesis_agent/REQUIREMENTS.md` (documents the `cryptography` dependency
   `enrollment.py`/`keyprotect/` need at import time, across all 3 platform sections).
   Confirmed via `~/work/nemesis-internal/handoff/2026-08-20-window3-handoff.md` (§1) that
   these two files were explicitly held for Window 2 to commit, staged by exact path per
   Window 3's own warning that the tree also holds Window 1's unrelated in-flight GUI/tray
   changes. Deliberately left `nemesis_agent/requirements.txt` untouched — Window 3 flagged
   it as still owed the same `cryptography` fix but didn't touch it themselves since
   another window has uncommitted changes there; not this session's call to make.
   - Rule-8 clean (one placeholder example IP, no real leaks).
   - Full standalone regression suite run: 35/36 files fully green; the one non-clean
     result (`test_agent_tray.py`, rc=2) is an explicit "no pystray on this machine"
     unverified-state, not a failure, and belongs to an unrelated untracked file. The
     previously-flagged `test_loopback_retirement.py` failure (from HANDOFF.md's do-not-
     touch note) did not reproduce this run.
   - Committed as `a4dfd40`.
3. **Pushed all four unpushed commits to `origin/main`** after listing them and getting
   explicit operator confirmation: `f91db98`, `f812583`, `8948e09`, `a4dfd40`. Verified
   `HEAD == origin/main` (`a4dfd405caa3bf73ab4be9d3bb8d7d0538e65c62`) post-push.

## Judgment call worth flagging

HANDOFF.md's "do NOT touch" list (written at the prior pause) named
`nemesis_agent/install_linux.sh` and `REQUIREMENTS.md` alongside Window 1's genuinely
in-flight files, attributing the whole group to "Window 1's GUI/tray build." That
attribution was imprecise — those two files were actually Window 3's separate, finished,
held work, caught in the same working-tree snapshot at pause time. Resolved by reading
Window 3's own handoff before acting, not by assuming either the operator's new instruction
or the old HANDOFF note was automatically right. The rest of that do-not-touch list (
`agent.py`, `config.py`, `installer_gui.py`, `uninstaller_gui.py`, `build_installer.py`,
`requirements.txt`, the workflow file, plus untracked `agent_gui*.py`/`agent_tray.py`/tests)
remains genuinely off-limits — still Window 1's, still growing.

## State after this session

- `origin/main` HEAD: `a4dfd40`.
- Working tree: Window 1's in-flight GUI/tray files still uncommitted (untouched, as
  intended); untracked `docs/audits/error-code-classification-batch{1,2,3}-2026-08-08.md`
  and `docs/audits/roadmap-state-audit-2026-08-19.md` still open (HANDOFF §3, unchanged by
  this session); `nemesis_agent/tools/win_priv_probe.py` still unclaimed.
- No deploy/restart triggered by this session — `a4dfd40` (installer script) is not live on
  any host until an operator-driven install/update, same as any other agent-side change.

## Continued this session — three more installer fixes + a private-repo commit

After the above, pushed `4e2d4bf` (the handoff-doc commit itself). The operator then handed
off two more items from Window 3:

1. **Three more `install_linux.sh` fixes**, uncommitted on top of `a4dfd40` (+88 lines,
   purely additive), needing three separate commits per Rule 2 rather than one. Split the
   diff at its 3 natural hunk boundaries — confirmed each applies cleanly and independently
   against the committed base before treating them as truly separable — and committed each
   in turn, checking `bash -n` (and `py_compile` for the one hunk inside the embedded Python
   heredoc) against the actual git blob that would land in each commit, then running the
   full regression suite after each:
   - `133449c` — stop a running agent before reinstalling (prevents a concurrent
     enrollment race; inferred from a shared timestamp, not directly reproduced).
   - `bebd346` — enrollment idempotence gate (don't re-enroll an already-known device;
     directly reproduced: 1 install = 1 row, 2nd install = 2 rows before the fix).
     Distinguishes a real status (skip), `"unknown"` (enroll fresh), and `None`/failed-read
     (fail closed, never treat as `"unknown"`) — the last distinction is what a naive fix
     would get wrong.
   - `f0bdbeb` — pre-create the agent log file owned by the service user, so systemd (which
     opens it as root first) doesn't win the ownership race and crash-loop the agent with
     `EACCES` before it ever reaches enrollment.
   All three: regression suite clean throughout, final state byte-identical to what Window 3
   verified end-to-end on throwaway VMs. Pushed; `HEAD == origin/main` at `f0bdbeb`.
2. **`~/work/nemesis-internal/vm-fleet/VM-FLEET-LOG.md`** — a +128-line entry from Window 3
   documenting the Linux-base SSH/ufw provisioning fix and the duplicate-enrollment
   reproduction session, outside Window 3's narrow git-write grant (their own handoff file
   only). Committed as backup git-writer for this private repo (`7f606a9`), staged by exact
   path only — that repo currently holds a large amount of unrelated uncommitted content
   from other windows/sessions, none of it touched. Pushed to both `local` and `usb`
   remotes; verified all three report the identical HEAD.

## Continued this session — agent_last_seen fix, then the full GUI/DMZ/sentinel/QUIC batch

Pushed `eb5b3fe` (the prior handoff-doc commit). Operator: "Push dc92784" — a hash that
turned out to belong to the private `nemesis-internal` repo, not `/opt/nemesis`: Window 3's
own handoff-file commit, within their standing grant, made but not pushed. Pushed to `local`
+ `usb`, verified all three report `dc92784`.

**`hw_monitor.py`'s `agent_last_seen` fix** (Window 3, built/tested/live-verified on the
Gateway per their handoff): `_update_agent_device` now stamps the SERVER's receipt time
instead of trusting the agent's own payload timestamp — the prior behavior meant two naive
datetimes in different timezones got compared with no error, no warning, just a wrong
answer (measured live: a healthy America/Chicago agent read as 18000s stale against an
1800s threshold on an Etc/UTC gateway, tripping the "revoke it" advice for a healthy
device). `_create_enrollment` already used the server clock and gets an invariant comment
instead of a code change. Committed fix + its dedicated test
(`test_agent_last_seen_frame.py`, 12/12 — writer against the real shipped function, readers
extracted verbatim from `dashboard.py` rather than copied) together as one commit
(`5bb3f73`) per explicit instruction: splitting them would land a commit whose own test
fails. Full repo-wide regression sweep clean (only the same pre-existing environment gaps
as every other run this session). Pushed; `HEAD == origin/main` at `5bb3f73`.

**The large remaining batch from Window 1's tree** — agent GUI (window + core + tray +
tests), a per-device DMZ-mode kill switch, the connection-type sentinel fix, the passive
QUIC SNI decoder, and installer/CI integration — landed as three commits, not one, after
reading Window 1's full closeout handoff and every hunk of the diffs involved:

1. `8101568` — the connection-type sentinel fix alone (`CONN_LOCAL`/`CONN_REMOTE`/
   `CONN_UNKNOWN`, `is_confirmed_remote()`, `_connection_type_for_wire()` for the
   deliberately-still-two-valued heartbeat contract). Genuinely separable in the code and
   explicitly called "standalone" in Window 1's own account — split cleanly except for one
   hunk (`_collect_payload`) that mixed the sentinel's wire-wrapping with an unrelated
   DMZ-mode field on adjacent lines, hand-split at the line level into two hunk fragments
   with the dependency direction verified (`git apply --check` fails for the DMZ fragment
   before the sentinel commit lands, succeeds after).
2. `e7f9b27` — GUI core + tray + DMZ mode + installer/CI kept as ONE commit. Judgment call,
   not a default: Window 1's own handoff groups these as a single held batch, the tray's
   `--settings` action launches the GUI window as a subprocess (non-functional without it),
   DMZ's toggle lives inside that same window, and three of the largest files
   (`agent_gui.py`, `agent_gui_core.py`, `agent_tray.py`) are 100%-new — there is no prior
   commit to split them against, so separating GUI-core from DMZ-mode within them would
   mean reconstructing history that never existed rather than dividing an existing diff.
   Caught and fixed one real Rule-8 leak before committing: a test used the operator's real
   first name in an example device name (same class of leak Window 3 already fixed once
   today elsewhere) — replaced with a generic placeholder, re-verified 107/107.
3. `79e2cab` — the passive QUIC SNI decoder (`quic_sni.py`), fully independent, own commit.

Verified pass counts matched Window 1's claims exactly (107/107 gui_core, 74/74 gui_render,
39/39 tray). Two new files appeared mid-task (`steering_lease.py`, `steering_nft.py`,
`test_steering_lease.py`) — Window 1 visibly still building; confirmed never staged, left
untouched. Pushed all four commits (`5bb3f73`, `8101568`, `e7f9b27`, `79e2cab`) after
listing them and getting explicit confirmation; verified `HEAD == origin/main` at
`79e2cab`.

## Cross-references

- `docs/handoff/worklog/2026-08-20-002.md` — raw chronological detail.
- `~/work/nemesis-internal/handoff/2026-08-20-window3-handoff.md` — source of truth for the
  held-batch attribution and the 3-fix root-cause narrative (§4.2).
- `~/work/nemesis-internal/handoff/2026-08-20-window1-handoff.md` — source of truth for
  every piece of the GUI/DMZ/sentinel/QUIC batch.
- `~/work/nemesis-internal/vm-fleet/VM-FLEET-LOG.md` — the fleet-log entry committed this
  session (`7f606a9`).
- Prior: `docs/handoff/supplements/2026-08-20-001.md`.
