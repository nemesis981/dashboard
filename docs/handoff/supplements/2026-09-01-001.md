# Session supplement — 2026-09-01 (Window 2)

> Curated account. Raw chronology: `worklog/2026-09-01-001.md`. This session ran under an
> operator-issued tropical-storm/power-loss warning (from mid-morning), which tightened
> working discipline for the rest of the day — see "Storm-mode discipline" below. No power
> event occurred; nothing was lost as a result.

## Headline

The day's largest thread was Window 1's work: a Tailscale snap→apt migration that exposed
a real DNS outage risk when a killswitch VPN (PIA) reconnects, followed by building and
live-testing a fix (the MagicDNS/killswitch guard, an ADR 0002 amendment) directly on the
daily driver — 6 PIA live tests plus 1 Proton VPN generalization test, 5 distinct root
causes found and fixed, ending with `accept-dns=True` and MagicDNS genuinely working as
the current live state. Full account, including the mistakes made along the way (a
self-corrected wrong diagnosis, two verification-tool bugs, an anomaly that should have
gated a step and didn't): `~/work/nemesis-internal/known-limitations/
tailscale-magicdns-pia-saga-FULL-2026-09-01.md` (private mirror; consolidated by this
window, then corrected same day when the "not yet built" framing turned stale within
hours of being written).

## Morning Status

197,556 LOC, all 6 core services active, working tree clean at session start. Roadmap
audit: the 08-31 baseline (`roadmap-state-audit-2026-08-31.md`, finalized 08:28 that
morning) was found to already predate roughly 70 commits of that same day's later
shipping (Gateway Mode completion, Track C closeout, admin-approval, port-broker,
email-security, data-retention going live) — flagged as the baseline being "stamped
today" while still hours behind by the time anyone reads it, structurally the same shape
as the 2026-07-27 false-drift incident CLAUDE.md's Rule 6 already documents, just
inverted. Not corrected in place (read-only per Rule 1); flagged for a deliberate refresh
rather than a rushed one. File-set count matched exactly (86 on disk, 85 tracked + 1
documented exclusion) — no file-set drift. Full detail: `docs/briefing/2026-09-01.md`.

Also found and corrected: HANDOFF.md's §1 claimed `a8e0221` was awaiting push
confirmation; it was already on `origin/main`. Noted for this closeout rather than fixed
as an isolated edit.

## Elevated grants

Re-checked live twice today: production box `sudo -n -l` unchanged and clean both times,
`pihole`/`nemesis-db`/`nemesis-fw` group memberships unchanged, `tcpdump` capabilities
unchanged (still active Tier 2 use). Polkit rules unreadable from this session for the
5th consecutive session. One closed add/revoke cycle recorded: a temporary Tailscale-audit
sudo grant, added earlier for Window 1's investigation and confirmed revoked two ways
(operator's own interactive-auth re-test, and this window's independent `sudo -n -l`
check finding no matching entry). `nemesis-vpndns`'s addition to group `nemesis-fw`
(operator-approved, for the MagicDNS guard's socket access) is now also tracked. Full
detail: `docs/handoff/elevated-grants-tracking.md`.

## The Tailscale/PIA/snap DNS saga

Consolidated from ~14 documents (spanning back to 2026-08-01) into one private-mirror
account at operator request, "while it was fresh." Two corrections worth recording about
the process itself:

1. **The consolidated doc was itself wrong within hours of being written** — it described
   the PIA-reconnect fix (Option B) as "scoped, not yet built" at the exact moment Window 1
   was actively shipping it. Caught on the next `git fetch` before pushing unrelated work,
   and corrected with an explicit UPDATE section (not a silent edit) in both the private
   saga doc and a PUNCHLIST follow-up entry that had inherited the same stale framing.
2. **A user-facing doc was deliberately scoped narrower than first asked.** The operator's
   request named "the MagicDNS + VPN killswitch interaction" as what should self-correct
   in ~15-35s; the mechanism that actually self-corrects in that window is
   `vpn-dns-guard`'s general Pi-hole-upstream repoint (already shipped, unrelated to
   MagicDNS specifically). The MagicDNS/PIA case does not yet self-correct without today's
   new guard, so the doc was written about the general mechanism only —
   `docs/operation/VPN_CONNECT_DNS_DELAY.md` — to avoid describing not-yet-default-enabled
   behavior as already-working, user-facing fact.

## Public-repo commits landed today (14, all pushed, `local == origin` verified)

```
109191d..faf7666 range starts with yesterday's tail; today's own set:
ff3d6c4 feat(vpn-dns-guard): detect the MagicDNS/killswitch DNS conflict (detection only)
8e26c6d feat(magicdns): privileged actuator -- fifth peer, one op, helper re-validates
f1924b8 fix(magicdns): wire evaluate_magicdns into reconcile + assert the call site by AST
38bc7d6 test(peers): assert every peer can OPEN the socket, not merely that it is allowlisted
d3086b2 fix(magicdns): OSCILLATION -- restore must hinge on the CAUSE, not the symptom
7c699d5 feat(magicdns): verify the OUTCOME, not just the preference (step 2)
9f4e430 fix(magicdns): refuse to ENABLE while Tailscale's release path is broken
b05ec54 feat(resolvconf): self-repair the disable-direction stuck state (ADR 0002 amendment 2)
faf7666 perf(vpn-dns-guard): 1 try per zone (18s->9s worst case) + log dig durations
eb938b3 test(tailscale): packaging-independence suite -- proves apt/snap agnostic behaviour
e86e366 docs(elevated-grants): re-check live -- production box clean, unchanged
d1ebcf9 docs(elevated-grants): record closed Tailscale-audit sudo grant add/revoke cycle
750c806 docs(punchlist): log + immediately correct the MagicDNS VPN-agnosticism follow-up
ab73831 docs(operation): explain the brief DNS pause after connecting a killswitch VPN
```

Two of these (`ab73831`, `750c806`) were explicitly flagged by Window 1 as "not mine" per
the shared-tree attribution discipline before this window pushed anything — confirmed as
this window's own before publishing, per that same discipline.

## Private-repo (`~/work/nemesis-internal`) — reviewed, push blocked

Requested to push 2 specific commits; found they sit deep in a single linear,
entirely-unpushed 76-to-82-commit history (grew across the session as Window 1 kept
working) — a normal push cannot publish 2 commits without publishing everything beneath
them, and this window will not rewrite history to isolate them without being asked.

Ran the requested categorized review of the full backlog before pushing anything:
- **Still relevant (all of it):** the bulk 08-01→08-31 doc-backlog catch-up (109 files,
  grepped for credentials/secrets — none found; the real infra values it does carry are
  already correctly scoped to this private-mirror tier), Window 3's Piece F/G TLS work
  (coherent progression, matches current file state), the full Tailscale/PIA/MagicDNS
  thread, the LICENSE draft and VPS/ gitignore (both already operator-decided 08-31).
- **Superseded/stale:** none found warranting exclusion — old audit baselines sitting
  alongside newer ones are correctly-kept history, not dead weight.
- **Stay local-only:** none found that exceed what this repo's local+usb tier already
  treats as appropriate.

Caveat stated in the review itself: proportionate, not an exhaustive line-by-line read of
~55K+ lines — commit messages read in full, thread-coherence spot-checked against current
file state, targeted secret grep across the largest bundle.

**End of day: push attempted for the full reviewed set (82 commits) to both `local` and
`usb`, blocked by the harness's permission classifier** (likely the scale of the action —
82 commits in one push). Held for explicit operator action rather than retried under a
different form. `local` and `usb` remain in sync with each other (confirmed same SHA)
but both now 82 commits behind this window's `HEAD`.

## Storm-mode discipline

Applied for the whole afternoon/evening per the operator's own instruction: commit
locally immediately on every completed unit (not batched), handoff updated after every
commit rather than only at session boundaries, no long atomic state-changing operations.
No power event occurred.

## Open items carried forward

1. **Private-repo push (82 commits) still not landed** on `local`/`usb` — blocked by the
   permission classifier this session; needs either explicit re-approval or the operator
   running it directly.
2. **Roadmap audit baseline drift** (flagged this morning, not yet refreshed) — the
   08-31 baseline predates a full day of further shipping.
3. **`l3-tier2-tls-interception`** (a separate private repo, Window 3's Piece F/G work,
   16 commits local and unpushed there) was NOT reviewed or pushed today — flagged by
   Window 3 as "Window 1/Window 2 decide," out of scope for tonight given the private
   `nemesis-internal` review already in progress; deserves its own dedicated review pass
   given that repo's wider remote set (includes private GitHub) and higher disclosure
   sensitivity (Tier 2 is the carved-out novel-mechanism module).
4. **`accept-dns=True` / MagicDNS-on is now the live default** on the daily driver — a
   real state change from this morning's "keep it false" position, made safe only because
   the guard is live and proven. If `vpn-dns-guard` is ever stopped or reverted without
   also reverting this preference, DNS breaks on the next VPN connect with nothing
   watching. Worth surfacing prominently for whoever picks this up next.
5. **Proton VPN now installed on the daily driver** (Rule-13-compliant install/test,
   revert script written and verified offline-capable before use) — cleanup/removal
   decision not yet made; revert at `~/work/nemesis-internal/migration/
   proton-revert-daily-driver.txt` if it should come out.
6. **Cleanup owed, not this window's to perform:** orphaned tailnet node
   `nuKHjHphBz11CNTRL` from the rehearsal VM (non-ephemeral, does not self-remove); the VM
   `Nemesis TS-MIGRATION-REHEARSAL 09-01` (powered off, keep only if the logged Option-D
   experiment is still wanted).
7. **PUNCHLIST follow-up, correctly not-urgent:** verify the MagicDNS guard's
   vendor-neutral design against a simulated (non-PIA) killswitch, now partially
   pre-answered by today's real Proton test (detection generalized; the disable/restore
   path specifically was not exercised under Proton, so that half is still open).

## Cross-references

- `docs/briefing/2026-09-01.md` — this morning's Morning Status briefing.
- `docs/handoff/worklog/2026-09-01-001.md` — raw chronology.
- `docs/handoff/elevated-grants-tracking.md` — standing elevated-grants record.
- `~/work/nemesis-internal/known-limitations/tailscale-magicdns-pia-saga-FULL-2026-09-01.md`
  — full saga account, private mirror.
- `~/work/nemesis-internal/handoff/2026-09-01-window1-handoff.md` — Window 1's FINAL
  closeout (build detail, broken-instrument notes, cleanup owed).
- `~/work/nemesis-internal/handoff/2026-09-01-window3-handoff.md` — Window 3's NIGHT
  closeout (Piece F/G TLS work, invariants, VM fleet review — 35 VMs, 0 expired).
