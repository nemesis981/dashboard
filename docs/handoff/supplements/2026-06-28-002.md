# Session supplement — 2026-06-28-002

> Append-only durable record. Focused session: project-identity update (support email +
> Tailscale migration to a project-owned account ahead of the remote field deployment).
> Docs + the hardcoded support-email string only — no behavioral code changes.

---

## 1. Support email → `support@nemesis-sw.com`

The support/diagnostics-submit address moved off the personal ProtonVPN mailbox onto the
**project domain**:

- **New:** `support@nemesis-sw.com` (domain `nemesis-sw.com`); replaces the prior
  personal ProtonMail support inbox.
- **Filtering:** inbound support mail is sorted by **subject-line tag** — messages carry a
  `[Nemesis Firewall]` subject tag for routing/filtering on the `nemesis-sw.com` side.

**Where it lived:** the address was not in any doc — it was hardcoded in `dashboard.py`
(the `/api/diagnostics/submit` `send_email(...)` `to=` target plus the diagnostics-panel
tooltip/help text, ~5 occurrences). All were updated in place. This is the only "code"
touched this session and it is a literal-string swap, not a logic change.

**Why:** a project-owned domain is demo-ready and decouples support from a personal
account; subject-tag filtering keeps Nemesis mail separable without per-sender rules.

## 2. Tailscale migrated to a project-owned account

The tailnet moved off the **personal** Tailscale identity onto a dedicated **project**
account so the demo/field environment carries **no personal information**.

- **Account:** `<project-account>` (was a personal Tailscale login)
- **New tailnet IP (this box):** `<tailnet-ip>` (the prior personal-tailnet IP is
  retired)
- **Tailnet:** the personal tailnet is **replaced by the project tailnet** — demo-ready,
  no personal info exposed in the mesh.

**Doc updates:** the old personal-tailnet IP was swapped to `<tailnet-ip>` everywhere it
appeared in committed docs — `docs/handoff/HANDOFF.md` (current-state, ×2) and the
`docs/handoff/worklog/2026-06-27-001.md` enrollment entry (×1). The worklog entry was
updated by explicit decision this session (the old personal-tailnet IP should appear
nowhere in committed docs), even though it is normally an append-only flight recorder.

**Why:** the upcoming remote deployment (Wisconsin trip) runs over this tailnet; a
project-owned account keeps the demo clean and the access attributable to the project,
not a personal login.

## 3. Open follow-up — son's laptop re-enrollment

The son's laptop is still enrolled under the **old personal** Tailscale account. It needs
**re-enrollment under the project account** (`<project-account>`) when
convenient — not blocking, but required before the laptop is part of the demo-ready
project tailnet. Captured here so it is not lost.

## 4. Out-of-repo bookkeeping

`~/work/nemesis-private/local-config.md` (outside the repo, never committed) was updated
with the new Tailscale IP, the project Tailscale account, the support email, the
`nemesis-sw.com` domain, and the Wisconsin-trip / laptop-re-enrollment note. Referenced
here by location only per Rule 8 — no values copied into the repo.
