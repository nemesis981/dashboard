# Session supplement — 2026-08-30 (Window 2)

Curated account. Raw chronology: `docs/handoff/worklog/2026-08-30-001.md`.

## Landed this session

`docs/handoff/elevated-grants-tracking.md` created (standalone, edited-in-place tracking
file) · `CLAUDE.md` item 7 amended to point at it instead of embedding detail in
`HANDOFF.md` · `HANDOFF.md` refreshed (§1 closed, elevated-grants section replaced with a
pointer) · `docs/briefing/2026-08-30.md` (Morning Status) written and mirrored.

## §1 — `ed6af88` incident: CLOSED (was reported open in the 08-29 HANDOFF; found already resolved)

`HANDOFF.md` at `5086b51` (the 08-29 closeout) stated the incident was still open and
`origin/main` unsafe to deploy fresh. It wasn't, by the time this session read it: four
commits had landed after `5086b51`, all pushed —

- `ecd6e35` — restores the full companion set (`alert_manager/database.py`,
  `alert_manager/roles.py`, `modules/email_security/{enrollment,views,writes}.py` + 2 new
  test files).
- `c0e3b57` — the `attestation.py` `CHALLENGE_TTL_SECONDS` fix (the other file wiped by the
  same bad command).
- `88d8776`, `fc40e31` — unrelated doc/CLAUDE.md work, same landing sequence.

Their commit messages self-describe as "Operator-directed landing order (2026-08-29), step
2a/2b/3 of 3" — a deliberate sequence that completed and pushed same-day, but nobody
refreshed `HANDOFF.md` afterward to reflect it. **Verified live this session, not taken on
the commit messages' word:** ran `alert_manager/test_roles.py` directly against the current
working tree (clean, matches `HEAD` for all four files) — **158 passed, 0 failed**,
including the full registry-completeness and mutation-canary suite. `HANDOFF.md` §1 is
closed on that basis.

## Elevated grants — thinning root-caused, structural fix applied

Traced the "Standing elevated grants — REVIEW FOR REVOCATION" section's disappearance via
`git log -p -- docs/handoff/HANDOFF.md`: full content at `f79f5ad` (08-26) → pointer-only at
`670ab6b` (08-27) → absent at `f20d696` (08-28) and `5086b51` (08-29). Four closeouts,
monotonic decay, despite CLAUDE.md item 7 explicitly requiring it "surfaced live every
session, not noted once and forgotten."

**Root cause:** `HANDOFF.md` is overwritten wholesale each closeout (Rule 9's "latest state
wins"). Content embedded there survives only if every closeout author remembers to manually
retype or carry it forward in full — no diff shows what was dropped, no reflog records it.
Same failure shape as the uncommitted-tracked-file hazard the 2026-08-29 "commit locally,
immediately" rule was written to close (structural gap, not vigilance gap — and vigilance
had already failed four times running here).

**Fix, matching that rule's shape (make the correct outcome the only reachable one, not a
thing to remember):**
- New file, `docs/handoff/elevated-grants-tracking.md`, **edited in place** (never
  regenerated from scratch — same discipline as `PUNCHLIST.md`). A file that's edited
  rather than overwritten cannot lose content nobody touched, because there's no rewrite
  step where a section can go silently missing from "the new version."
- Populated with this session's live findings: production box confirmed clean (no broad
  grants, no `nmap`), `pihole` still open (unchanged, tracked for the cardinality tool),
  gateway-VM `nmap` grant carried forward **explicitly marked not re-verified today** (last
  confirmed 2026-08-26 — Morning Status checks the build host, not fleet VMs; the open
  scope question about extending it to fleet VMs is restated, not silently dropped a second
  time), polkit rules unchecked (permission denied, needs root — noted per the existing
  instruction to flag rather than skip).
- `CLAUDE.md` item 7 amended: now instructs updating the tracking file directly, and
  explicitly forbids embedding the detail in `HANDOFF.md` again, with the four-closeout
  decay documented as the reason.
- `HANDOFF.md`'s own section is now a one-line pointer + current-state summary — cheap
  enough to keep accurate every closeout, and if it does go stale, the detail behind it
  hasn't (it isn't re-derived from the pointer).

## Working tree / commit status

Morning briefing (written before this closeout) flagged 8 modified/untracked files not
matching HANDOFF's list. Confirmed this session: both landed as local commits (per the
2026-08-29 "commit locally, immediately" rule) —
- `30efe9c` — `feat(integrity): fact-file -> ticket path via diagnostics-watcher` (5 files:
  `alert_manager/integrity_watch.py` + its test, `diagnostics_watcher.py`,
  `deploy_integrity_check.sh`, `nemesis-integrity-check`).
- `e302768` — `feat(email-security): D4 personal sender baseline, salted recurrence
  tokens` (3 files: `baseline.py`, `sender_id.py`, `test_d4_baseline.py`).

`git show --stat` on both confirms all 8 previously-flagged files are accounted for.
`git status --short` is clean. **Neither commit is pushed** — `HEAD == e302768`,
`origin/main == fc40e31` (confirmed via `git fetch` + `git rev-parse`). Not pushed this
session; no push-confirmation request was made of the operator. Tree-clean confirmed as
requested; push is a separate, later decision.

## Cross-references
- `docs/handoff/worklog/2026-08-30-001.md` — raw chronology, this session.
- `docs/handoff/elevated-grants-tracking.md` — new standing tracking file.
- `docs/briefing/2026-08-30.md` — this morning's Morning Status briefing.
- `docs/handoff/supplements/2026-08-29-001.md` — prior day, full `ed6af88` incident account.
