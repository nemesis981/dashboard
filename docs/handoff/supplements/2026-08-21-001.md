# Supplement — 2026-08-21-001 (Window 2)

Curated narrative. Raw detail: `docs/handoff/worklog/2026-08-21-001.md`. Continues from
`2026-08-20-002.md` (same continuous session, date rolled over past midnight).

## What landed on the public repo today

Eight commits, all independently reviewed/tested by this window (not a trusted pass-
through of either build window's self-report), all pushed and verified `HEAD ==
origin/main`:

1. `847041a` — `cryptography` added to `nemesis_agent/requirements.txt` (the actual
   installable manifest catching up to what `REQUIREMENTS.md` already documented).
2. `9620be6`/`e81cb41`/`9f0b769`/`ac53b0c` — the zero-day M1-M4 cores (engine inventory,
   behavioral monitoring, detonation sandbox, Layer D ML pipeline). Excluded
   `agent_errors.py` from the "4 modified files" Window 1's own handoff described — its
   only change was 100% steering-related on inspection, not zero-day.
3. `0588ac0` — 4 PUNCHLIST entries from Window 3's AI-surfacing audit, filed independent
   of whether a future automation mode ships. Two stated in full (functional/docs bugs);
   two summarized publicly with specifics kept in the private audit per Rule 10.
4. `c091ed5`/`7f499a7`/`719d93f` — the three zero-day operational-gap closures (Falco
   deployment mechanism, detonation base image + guest observation, model training path).
5. `7945d26`/`89a875d`/`289e0dc`/`c7ac0cc` — four AI-mode fixes from Window 3 (render the
   never-shown malware verdict, pseudonymize every billed call at one choke point, degrade
   to a cheaper model at the rate ceiling instead of refusing, replace the fictional
   Teaching/Automated-Mode doc with the real L0-L4 ladder). Items 2 and 3 shared
   `ai_engine/module.py` — split by hand into two disjoint hunk sets, verified the
   reconstruction matches the original working tree exactly before committing either, in
   the operator-specified order.

Also today, private mirror (`nemesis-internal`, `local`+`usb`, both verified same-HEAD):
Window 3's `agent-last-seen-timezone-audit-2026-08-20.md` (`c4f6ed3`) and the AI-surfacing
audit + automation-mode scoping doc (`009111f`).

## The blocked batch — what's held and why

Late in the session, asked to pull a further batch (merged `sysmon_collector.py` module +
tests, `sandbox.py`/`build_detonation_base_linux.sh`/`behavioral_agent.py`, new Windows
detonation artifacts, `install.sh` + the ISO-dependency doc section). Everything checked
out — Rule-8 clean, pass counts verified, `run-sample.cmd` correctly identified as a
required companion file despite not being named explicitly — **except one real cross-file
coupling hazard**: `build_detonation_base_linux.sh` now calls
`deploy_behavioral_linux.sh --extra-rules`, but that flag only exists in the currently-
excluded, still-pending `deploy_behavioral_linux.sh`. The already-committed version
(`c091ed5`) has strict argument parsing that hard-exits on any unrecognized flag.
Committing `build_detonation_base_linux.sh` as instructed would have broken the base-image
build script against currently-shipped code. Stopped, reported the specific finding, and
did not stage anything for this batch — the operator then asked for a nightly closeout
instead of resolving it in-session.

**This is now resolved for tomorrow, found while reading Window 1's end-of-day handoff**:
their own commit-ordering plan groups `deploy_behavioral_linux.sh` and
`build_detonation_base_linux.sh` together as one unit ("Layer B behavioral fixes") —
confirming the `--extra-rules` flag and its caller are meant to land in the same commit,
which resolves the blocker without needing an operator decision on which half to hold back.

## The much larger picture (not committed, not this session's job to fully digest)

Both build windows produced substantial additional work today, none of it committed to the
public repo, all `HELD` per their own accounts. Full detail lives in their own handoffs —
not reconstructed here, both are large (25KB / 42KB):

- `~/work/nemesis-internal/handoff/2026-08-21-window1-handoff.md` — memory-injection
  detection arc (Tier 2 attestation integration, RAM-budget working-set consumer, Linux
  memory-inspection capability, Windows authenticated-IPC foundation), the Windows
  behavioral/Sysmon arm, synthetic sample suite, agent GUI findings tab. **Contains an
  explicit commit-ordering plan (groups A-H) for the entire held set** — the single most
  useful artifact for whoever picks this up next; read it before attempting to stage
  anything from the current working tree.
- `~/work/nemesis-internal/handoff/2026-08-21-window3-handoff.md` — full AI automation
  mode (paused at a scoping gate, three blockers built+tested), Windows
  detonation/observer Phase 3 + ancestry filter, a master-password-authority rebuild, IP
  reversal handlers, raise-time automation readiness.

The working tree's `git status` at this point is a snapshot only — both windows were still
actively writing when this closeout was done. Do not treat any file enumeration in this
session's docs as exhaustive; re-check `git status` fresh next session.

## Cross-references

- `docs/handoff/worklog/2026-08-21-001.md` — raw chronological detail.
- `~/work/nemesis-internal/handoff/2026-08-21-window1-handoff.md` — the commit-ordering
  plan (§ "HELD FILES for Window 2"), KEEP VMs, gotchas.
- `~/work/nemesis-internal/handoff/2026-08-21-window3-handoff.md` — AI automation mode
  status, Windows detonation Phase 3, master-password rebuild, IP reversal handlers.
- Prior: `docs/handoff/supplements/2026-08-20-002.md` / `2026-08-20-001.md`.