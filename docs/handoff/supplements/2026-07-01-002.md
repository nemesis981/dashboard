# Session supplement — 2026-07-01-002 (build window, closeout)

> Append-only session log (durable history). Build-window session; complements
> `2026-07-01-001.md` (docs window, mid-session). Distilled from the day's commits + audit docs
> (no live worklog was kept this session — gap noted). Rule 8: placeholders only.

**Session theme:** designed + BUILT the clean-install/uninstall overhaul (provenance manifest,
Add/Remove Programs, shipped uninstaller, signed de-enroll endpoint), deployed the endpoint live,
proved the full self-onboard/enroll chain on a fresh VM (test-2), then two installer UX
improvements — one of which (TS_NOLAUNCH) regressed the Tailscale join, caught on VM-3 and banked
with a fix plan for next session.

## Arc

1. **Clean-uninstall build spec** → 3-phase build, one commit each (one-variable-at-a-time):
   - Phase 1 `9321cfe` — `install-manifest.json` provenance (probe-BEFORE-install for Tailscale/
     PawnIO), ARP HKCU key, Start Menu, ship `NemesisUninstall.exe` (bundled in the setup exe).
   - Phase 2 `5b03260` — `:5001 POST /api/agent/uninstall`, signed (device keypair, ADR 0011),
     soft-mark `uninstalled` + `uninstalled_at`/`uninstalled_by` (actor seam), idempotent, guarded
     migration. Verified vs temp DB incl. proof-of-possession (attacker-key rejected).
   - Phase 3 `14ce142` — manifest-driven uninstaller + consent UX. Cross-contract verified: the
     uninstaller's signature passes the Phase-2 endpoint's verifier.
2. **Deploy Phase 2** — Rule-6 backup → **hw-monitor** restart (NOT dashboard; separate service,
   and it needed a real-terminal sudo — no NOPASSWD rule for it). Migration applied, endpoint live
   (400/401 not 404), no data touched.
3. **Test-2 (VM, 66d190b build)** — CI rebuild (setup exe now embeds `NemesisUninstall.exe`,
   +15.9 MB) → re-stage → OAuth-mint regenerate → SSH-deliver → operator install. **PASS:** self-
   onboard, Tailscale join, enroll (one pending row — fix #3 holds), device_id persisted, conf
   consumed, + Phase-1 manifest/ARP/Start-Menu all confirmed. Screenshot revealed the HW-monitor
   prompt = **PawnIO** (PL-11).
4. **Two UX improvements** (each its own commit):
   - `1f495ad` silent PawnIO pre-install (`PawnIO_setup.exe -install -silent`) + provenance;
     removal=**never** always (shared kernel driver — Fan Control/OpenRGB depend on it).
   - `739e435` `TS_NOLAUNCH=1` on the Tailscale MSI (verified property via docs, not guessed);
     reordered to MSI-primary so the property actually applies.
5. **VM-3 test → REGRESSION.** `TS_NOLAUNCH` suppressed the GUI, which initializes the IPN backend;
   backend stuck `NoState` → `tailscale up --authkey` couldn't join → install blocked at Tailscale
   connect; a watched retry never left `NoState`. PawnIO never reached (runs after the join).
   Banked the finding + fix plan (launch-minimized first) in the regression audit doc.
6. **Closeout hygiene** — re-staged the known-good 66d190b exe as `NEMESIS_AGENT_EXE`; preserved the
   regressed 739e435 exe under a labeled name. Improvement commits stay on `main` (source intact
   for tomorrow's fix).

## Notes / gaps
- **No live worklog** kept this session (Rule 9 gap) — this supplement is distilled from commits +
  audit docs, not a flight recorder.
- **Uninstall lifecycle never run end-to-end** (test-2 uninstall deferred; VM-3 blocked before it) —
  it's the deferred proof after the TS_NOLAUNCH fix.
- Second test-2 screenshot held uncommitted (visible "Test-User" account name — Rule-8 decision
  pending).
- Secret exposure earlier in-session: bash-sourcing `/etc/nemesis.env` echoed `PIHOLE_PASSWORD` in
  an error message (transcript only, no file/commit). Rotate if the transcript is shared. Switched
  to Python env-parsing thereafter.

## Carry → next session
Fix TS_NOLAUNCH (regression doc's plan) → CI rebuild → regenerate → re-test on fresh VM (also
validates PawnIO) → then the full uninstall lifecycle test. Details in HANDOFF.md.
