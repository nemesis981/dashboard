# Supplement — 2026-08-23-001 (Window 2)

Curated narrative. Raw detail: `docs/handoff/worklog/2026-08-23-001.md`. An exceptionally
large session — 51 commits, four distinct handoffs from Window 3 plus Window 1's Set 2
plan, all executed and pushed. Full HEAD: `a0d971c`.

## What landed, in the order it landed

**Morning status + roadmap audit refresh.** Ran the standing Morning Status checks,
refreshed the roadmap-vs-state baseline (`5a04d0d`, `docs/audits/roadmap-state-audit-2026-08-22.md`
supersedes the 08-19 baseline), and established a first itemized elevated-grants baseline
in HANDOFF §9 (`6448f5b`) since none had existed before.

**Window 3's detector-coverage-corrections batch** (`2618b02`→`7e6cb8b`, `bounded.py`
first per dependency): a shared bounded-settings module (refuses values that silently mean
"never" — a floor with no ceiling is a coverage-disable with a number on it), applied to
malware and diagnostics settings, an IPv6-absence false-fault fix, the throttle-exclusion
fix for required detectors (mem_appliance.py, hunk-split cleanly from Window 1's
concurrent memory-injection work in the same file), and wiring `integrity_watch`'s
evaluation loop, which had never had a caller.

**Memory-injection Set 1** (`0509b67`→`52d3ab6`): Tier 2 attestation integration,
RAM-budget reservations (transient headroom distinct from steady budgets), the
E-AGENT-100/110-115 error-code declarations, Linux memcap (opt-in, default off, real
capability probing via `process_vm_readv`), the Windows privileged IPC channel + memory
acquisition (SYSTEM service, authenticated pipe, two independent adversarial SID checks),
and a `.ps1` encoding/banner fix. **The banner fix itself had a bug**, caught in review
before committing: the ENCODING disclaimer landed outside the `<# #>` block comment,
parsing as valid-but-wrong PowerShell syntax that would fail only at runtime. Sent back to
Window 1, fixed with per-line `#`-prefixing plus a new Windows-side token-level checker,
re-verified, then committed.

**Diagnostics-tools batch** (`f023bfd`→`062df42`, harness first): five new
self-verifying diagnostics (schema drift, clock/timestamp sanity, agent enrollment
integrity, dependency preflight, config drift) plus a real bug fix — `anomaly_state` had
been reporting the module disabled unconditionally, on this box, while it was enabled and
running, because it queried a key that table never held. Found 11 real defects between
the five new checks; specifics (a stale model name in dashboard.py's Settings prose vs.
the real active model, exact enrollment-collision counts) trimmed to the private mirror
per an explicit Rule 10 decision, general pattern published.

**The 2026-08-21 AI-automation batch, held two days, finally landed**
(`1225bd0`→`6f93588`): master-password authority/spend-metering/undo path, the dashboard
authority gate closing a real incoherence (the alert-verdict path auto-ignored low-risk
alerts with NO authority check while chat on the same alert was fully gated), the
ARCHITECTURE.md ladder documentation, a real Windows sandbox bug fix (guest-side error
reason wasn't reaching the host), and the consent five-valued coverage state. `dashboard.py`
needed a genuine hunk-split between the authority-gate/undo-handler work and the
unrelated consent-badge work sharing the same file.

**Lookup + TLS tools, with a real production-affecting bug** (`00c3223`→`93b0e41`, then
`542f56a`): domain/IP lookup (dig/whois, tiered output, argument-injection defended in
three layers) and TLS certificate inspection (deliberately unverified read + a separate
verifying connection, never conflated), folded into one module. `31478f0`'s `module.py`
used relative imports that work when tested via direct `spec_from_file_location` but
**fail under the real `modules_loader` mechanism** — reproduced independently before
trusting the report, fixed via an explicit sibling-file loader, re-verified via the real
loader. No live install was affected (`enabled_by_default: false`), but this is exactly
the kind of defect a verification instrument that can't reproduce the real load path will
keep missing — worth remembering for any future module split.

**Digest scheduling foundation + F1 fixes** (`7a7d703`, `a809835`, `5478f81`): scheduling
and routing only (content/delivery/scheduler tick are H1-H3, still outstanding — see
open items). F1 closed a genuinely broken HEAD test: a spend-metering rename left two
sibling test files with stale mocks; one (`test_rate_degradation.py`) was actually broken
(crashed with `database is locked` after the stub silently stopped intercepting calls),
the other merely stale but still passing. Landed the real fix first, the preventative one
later as part of Set 2's G-remainder.

**Pre-filter ladder** (`8cf521d`): heuristic triage ahead of AI model calls, framed
explicitly as a cost prerequisite (125:1 noise ratio measured live; an unfiltered week
would blow the $10 trial budget). Shadow-mode-first, sampling instead of blackout under
budget pressure, and structurally incapable of suppressing coverage (only ever
forward/defer, never a "don't record" instruction).

**Set 2 — the oldest outstanding debt, fully executed** (`71dda37`→`328e25e`, groups
A-H): roaming traffic steering, Windows detonation base image, **Windows behavioral
monitoring (Sysmon)** — closing the specific gap the operator flagged, Windows endpoints
were behaviorally blind until this landed — the synthetic sample suite (harness public,
AV-triggering fixtures correctly gitignored per the 2026-08-21 decision), Layer-B
Falco/Sysmon corrections including a real bug (`status_reader()` reported every Windows
endpoint's behavioral engine as permanently absent, even healthy, because it was never
actually run on Windows to notice), the agent GUI findings tab, the previously-undeclared
ISO-builder `install.sh` dependency, and the four outstanding audit docs.

**G1 — a priority fix landed alone**: `modules/lookup/module.py`'s relative-import defect
(above) was committed and pushed by itself, ahead of everything queued, per explicit
operator priority.

**Netprobe** (`2249930`→`81ca877`): ping/traceroute, restricted to the LAN inventory /
enrolled-agent list rather than gating *who* may probe (that layer doesn't exist yet —
see RBAC below). Port scan and packet capture remain deliberately unbuilt, enforced by a
test asserting their absence.

**RBAC foundation** (`c84dcce`→`a0d971c`): three roles (admin/user/viewonly) enforced at
a `before_request` gate — not a decorator, because 45 of 149 live endpoints are
module-registered and undecoratable from `dashboard.py`. `role` existed on the users
table and was enforced nowhere before this (one stray read, never checked). Six existing
GETs that perform actions (report_abuse, diagnostics execution x2, analyze_alert,
test_enrichment, filesystem browse) got elevated minimums rather than a method change,
which is tracked separately (PUNCHLIST) rather than done here. **`dashboard.py` needed a
20-hunk split** between this batch and Window 1's concurrent, unrelated
protection-schema work sharing the same file (a detector-coverage monitor/banner and
master-password-authority routes, both labeled with Window 1's own R-numbering,
coincidentally colliding with this batch's own R2/R3/R5 labels). Independently
re-verified beyond Window 3's own report: re-ran `test_roles.py` directly, separately
replicated the live app + module-loading setup and re-ran `assert_registry_complete()`
outside the test harness, manually sampled all 45 module endpoints against the registry,
and read `dashboard.py` source directly to confirm the six GET-that-act routes are
genuinely bare GETs before trusting their elevated minimums.

One real, documented (not silently absorbed) side effect: `roles.py` names two endpoints
(`api_ai_authority`, `api_ai_authority_raise`) that only exist in Window 1's held work,
so `test_roles.py` will read 143/144 instead of 144/144 until that work lands. Confirmed
the affected check (`assert_registry_complete`) is test/audit-only, never called from the
live request path — zero live effect, self-resolves once Window 1's routes land.

## Two things flagged for tomorrow, not fixed tonight

1. Two overlapping lookup CUSTOM guides (`docs/modules/lookup/CUSTOM_LOOKUP_BACKEND.md`,
   shipped; `docs/CUSTOM_LOOKUP_RESOLVER.md`, held uncommitted) need Window 3 to reconcile
   into one. Full detail in HANDOFF §3.
2. CLAUDE.md's ADR 0006 actor-mechanism note was stale (claimed unwired; wired since
   2026-08-04) — corrected tonight after independently verifying `dashboard.py:_set_dm_actor`
   directly, not just accepting Window 3's flag.

## Closeout discipline notes

- Held, per explicit operator instruction, without acting speculatively: Window 1's R1-R7
  protection-schema work (no commit plan yet), the R6 "active pipeline alert" pending
  clarification on assignment, batch 3's H1-H3 digest-delivery remainder (scope was
  netprobe only tonight), and all of Set 4's memory-injection work.
- Every mixed-file hunk-split tonight (dashboard.py twice, database.py, and continuing
  the pattern from earlier in the week with agent_errors.py) was done by reading every
  individual hunk before deciding, then verifying the staged-only content compiles/passes
  in isolation before committing — not assumed clean from a file-level diff stat.
