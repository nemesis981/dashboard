# Session supplement — 2026-07-29 (Window 2)

> Curated narrative distilled from `docs/handoff/worklog/2026-07-29-001.md` (raw log).
> Current state lives in `docs/handoff/HANDOFF.md` (overwritten). This file is
> append-only history.
>
> **Reconstruction note:** unlike prior supplements, this one was not distilled from a
> live append-as-you-go worklog kept by this session — Window 2 opened at closeout time
> and reconstructed the day's arc from `git log` (42 commits, `f00a1fe..HEAD`) plus the
> morning briefing. Bug descriptions below are therefore drawn from commit messages, not
> witnessed directly by this session; treat the "found by review" framing in individual
> commit messages as authoritative, not this file's paraphrase of it.

## Arc of the day

A very full day across seven threads, roughly in landing order:

1. **core_module layout move finished (6/6).** Yesterday's holdout, `alert_watcher`,
   was unblocked (`test_quarantine.py`'s import path and its tree-relative `DB_PATH` —
   which had been silently testing against an empty stray DB, not the live one — plus
   `deploy_nemesis_fwd.sh`'s source check) and its stale `alert_manager/` copy removed.
   Same window fixed `nemesis_fwd.py`'s per-request fd leak. (`e76b964`, `bfd7983`,
   `a096def`, `eeb5218`)
2. **device-scanner fixes** — a garbage-vendor-name bug (macvendors.com error bodies
   stored verbatim as device names) and the `sudo nmap`-dead-under-`NoNewPrivileges`
   bug flagged as Open Item #6 last night, replaced with an unprivileged scan.
   (`459585b`, `5849a3b`)
3. **Data Manager rollout — the big one.** The "dashboard" namespace went from
   registered-in-WARN-mode with seven ownership conflicts to all seven resolved and
   every `dashboard.py` DB site (read and write) routed through the guard. Caught a
   silent root-logger bug (`log.info()` going nowhere) and a `settings_page()`
   `NameError` that was reverting security settings to default on every page load.
   Namespace stays WARN, not ENFORCE, by deliberate choice — pending a quiet-journal
   period, matching the discipline used elsewhere. (`5bd6259`→`5e9e23d`, thirteen
   commits)
4. **Severity/vocabulary consolidation** — one shared severity ladder replacing two
   duplicated dicts (fixing a live bug where lowercase `"critical"` never crossed a
   ticket threshold), plus canonical-uppercase malware vocabulary with
   case-insensitive reads (permanent, given ~36.7k legacy lowercase rows).
   (`0b404f4`, `0e571f2`, `ed2c889`)
5. **Process rules** — Window 3 formalized in CLAUDE.md (overflow, never a
   git-writer, per-task model check), and Rule 11 (test data written to live
   `alerts.db` must be labeled with "test data" + date). (`79f1c06`, `941ec51`)
6. **Ransomware canary incident, root-caused and closed.** `ProtectHome=yes` had
   blinded the canary to `/home` since 2026-07-25 — ~33,500 false CRITICALs, mail
   rate limit exhausted, zero real detection. Fixed in five passes: the
   `ProtectHome` masking itself, a non-atomic plant+baseline that would have kept
   false-tripping even after visibility returned, the missing traverse ACL
   (the second, non-code precondition), a dedup/cooldown so one stuck condition
   reads as one finding not a flood, and — the fix underneath all of it —
   disambiguating "file not found" from "I can't see this directory" everywhere
   the canary and its module-enabled gate relied on `os.path.isfile()`.
   (`7373b10`, `9cc7746`, `c40f53d`, `1705e9c`, `d577e10`)
7. **Fork B / host-defense, evening thread.** Fixed a packaging gap that had
   silently broken ticket-filing for three of four services since flask-login
   was never installed system-wide. Added Fork B source-NAT with a derived (not
   pinned) egress interface. Removed two never-implemented `nemesis-fwd` ops from
   its allowlist surface. Then, directly answering last night's pen-test finding
   of zero host self-defense: LAN-internal severity now derives from Suricata
   priority when no reputation data exists, a narrow `fail2ban` peer was added to
   `nemesis-fwd`'s policy (block/deny only, cannot release), and `:5001` gained
   pacing — landed but explicitly described as not yet deployed at commit time
   (it was, later — see below). (`2530977`, `517b1e0`, `8c28525`, `480b1c8`,
   `1d63734`, `19c783f`, `86a4c47`)

At 18:15 `nemesis-fwd` restarted (picking up the host-defense changes), which as a
side effect deployed the 08:04 fd-leak fix — closing Open Item #10 for real, not just
on disk.

Late in the evening, Window 2 opened for closeout, ran a diff audit (`800b5e2`,
`31aee8f` — ADR 0019 stub + Headscale VPN-swap evaluation, both Rule-8/Rule-10 clean,
pushed), then found two more files with uncommitted changes from a concurrent
Window 1/3 session: a private-module remote-redundancy policy change (new private
modules default to local+USB only, no GitHub, per an explicit operator decision) and
an ADR 0019 scope-boundary addition (owning the enforcement point doesn't solve
in-path traffic delivery — that's separate, unscoped work). Operator confirmed both
ready; Window 2 independently leak-scanned and committed them (`e9dab6e`, `2b75b27`).

## Bugs fixed today (from commit messages, nine total)

- `test_quarantine.py` testing against a phantom empty DB instead of the live one
  (`e76b964`)
- `nemesis_fwd.py` `_db()` leaking 3 fds per request (`bfd7983`)
- macvendors.com error bodies stored verbatim as device names (`459585b`)
- `sudo nmap` dead-on-arrival under `NoNewPrivileges`, silently indistinguishable
  from an empty LAN, plus a ~7.5hr `print()` buffering blackout (`5849a3b`)
- `dashboard.py` root logger never configured — `log.info()` silently discarded
  (`296ca86`)
- `nemesis_fwd`'s own `audit_log` write bypassing its own Data Manager grant
  (`0a91322`)
- `alert-watcher` claiming auto-quarantine success before the ufw block was
  confirmed (`ceb7c60`)
- `settings_page()` `NameError` silently reverting security settings to default on
  every page load (`03f38fd`)
- lowercase `"critical"` severity never crossing the HIGH ticket threshold
  (`0b404f4`)

## Ransomware canary — five-bug incident, closed

See thread 6 above. Root cause plus the four bugs it took to actually close the gap
(not just silence the symptom): `ProtectHome` masking, non-atomic plant+baseline,
missing traverse ACL, no dedup/cooldown, and the `isfile()`-ambiguity that made the
false positives possible in the first place. HANDOFF.md's canary section (added
mid-thread by `c40f53d`) is extended in tonight's HANDOFF refresh to cover the
dedup/cooldown and disambiguation work that landed after it was written.

## Open items resolved or advanced today

- **#1** (alert_watcher Commit B blocked) — **RESOLVED**, core_module layout now 6/6.
- **#6** (device_scanner unconditional `sudo nmap`) — **RESOLVED**.
- **#9** (six files with raw tree-relative `alerts.db` paths) — **PARTIALLY
  RESOLVED**: `test_quarantine.py` fixed; three `stage2_migrate_*` scripts,
  `wal_concurrent_smoketest.py`, and `test_anomaly_cleanup.py` untouched.
- **#10** (`nemesis_fwd.py` connection leak) — **RESOLVED and deployed** (fixed
  08:04, `nemesis-fwd` restarted 18:15).
- **#2** (Rule-8 real-username-in-commit-message finding) — **not touched**, still
  needs an explicit operator decision. Carried forward unchanged.

## Private work alongside (kept out of the public repo per Rule 10)

`~/work/nemesis-internal/firewall-enforcement-engine/` — new directory, holding the
full ADR 0019 writeup (measured findings, exact nftables hook priorities and why each
is safe, rejected alternatives, lockout-failsafe mechanics). Pushed to `local` + `usb`
remotes only, per today's new private-module remote default — no GitHub remote for
this one. Confirmed both remotes report the same HEAD.

## Pointers

- Raw log: `docs/handoff/worklog/2026-07-29-001.md`.
- Prior narrative: `docs/handoff/supplements/2026-07-28-001.md`.
- Adversarial test report (gitignored, local-only):
  `docs/testing/adversarial-test-2026-07-29.md` — the finding `86a4c47` responds to.
- New audit: `docs/audits/proposed-features-status-2026-07-29.md` (does not reset the
  roadmap baseline — different filename pattern than `roadmap-state-audit-*.md`).
