# Session Supplement — 2026-08-07-002 (Window 2)

> Append-only durable log. Curated from the live worklog
> (`docs/handoff/worklog/2026-08-07-002.md`), which has the raw blow-by-blow. Second
> session today — picks up after the `2026-08-07-001` checkpoint closeout (`8c99a85`).
> This is the day's nightly closeout.

## What shipped, in order

1. **Second Morning Status run + roadmap-vs-state audit refresh.** Baseline
   `roadmap-state-audit-2026-08-06.md` (8/10/58, 76 total) re-checked against this
   session's commit history. File-set drift: none. Shipping drift: one real upgrade —
   `track-c-metadata-tier-build-plan.md` PARKED→PARTIAL, its header still unrefreshed
   ("no code written yet") but Requirement 0 and step 2 both shipped with commits
   explicitly naming Track C. New baseline written and committed (`85e4e1e`):
   `docs/audits/roadmap-state-audit-2026-08-07.md`, tally 8/11/57 (76 total).
2. **Landed all of Window 1's 08-07 "MINE — ready to commit" work**, held from session
   001, in dependency-ordered commits after independently re-verifying every test suite
   live rather than trusting the prior claim (Rule 3):
   - **Track C schema v2** (`180514a`, 10 files) — server-side ingest with a
     fail-closed consent gate (`hw_monitor.py::ingest_connection_events()` checks
     server-recorded consent BEFORE parsing or storing anything; no row, revoked, or
     any lookup error all mean reject), a real retention reaper (`reap_conn_events()`,
     clamped 1–365 days, returns -1 rather than 0 on failure), and schema v2's
     `resolved_name`/`resolved_name_source` fields (nullable, provenance-gated — a name
     with no matching source is rejected as untraceable evidence). Includes the new
     `conn_collector.py` (platform-independent collection core) and `etw_probe.py`
     (Windows-only measurement tool, not part of the agent). Tests: 64/64 + 54/54 +
     28/28, all re-run live.
   - **Rule 13 continuation** (`b43e487`) — `nemesis_fw_watch.py`'s auto-restore and
     `nemesis_agent/dns_enforce.py`'s set/restore/kill-switch paths now read the live
     state back and prove it matches the claimed target before logging success, same
     shape as the already-landed `vpn_dns_guard.restore()` fix. Not independently
     verified live in this session (no real nft table or Windows adapter available
     here) — compile-verified and reasoning-checked against the proven precedent only.
   - **New tooling** (`7400e9c`) — `scripts/nemesis-pihole-password.sh` replaces the
     dead `install_pihole_pwd.sh` (a completed one-shot migration whose `UNIT_SRC` no
     longer resolves). `--check`/`--sync` modes; password never touches argv/logs;
     `--sync` verifies the credential against Pi-hole's API before persisting and
     proves the post-restart result rather than trusting exit codes.
   - Caught and held back a stray `chmod +x` on `install_pihole_pwd.sh` (644→755, no
     content change) rather than bundling it — Window 1 confirmed it was accidental and
     reverted it mid-session.
3. **DHCP steady-state health observability** (`f5deda0`, 4 files landed together since
   the module's writes and the Data Manager grant must ship as one unit). Closes the
   observability gap `c064e2b` (session 001) explicitly left unfixed: `status()` no
   longer reports "running" during a crash-loop. Three new operating-state error codes
   (E-DHCP-014/015/016), each health-derivation layer self-tested at import time
   (`selftest_proc_port_probe`, `selftest_port67_classifier`,
   `selftest_health_derivation`), UDP:67 binding read from `/proc` rather than inferred
   from systemd's opinion. New `dhcp_health_samples`/`dhcp_lease_events` tables, granted
   to the `dhcp` Data Manager namespace by exact name and proven behaviourally (not just
   source-text) in the module's own test suite. 159/159 + 78/78, re-run live.
4. **Four DHCP-deployment follow-ups filed to `PUNCHLIST.md`**, surfaced via a new
   `HANDOFF.md` open item explicitly flagged for tomorrow's (08-08) Morning Status
   briefing, per direct operator instruction, for Window 3 to pick up after Paul's usage
   resets: the polkit rule used tonight for dashboard→daemon control is a stopgap (the
   architecturally consistent fix is a `nemesis-fwd` peer/op, matching the codebase's one
   existing privileged-operation chokepoint); the Pi-hole group-membership grant needed
   for dashboard DHCP-status reads also grants read access to Pi-hole's config file
   (including its web-password hash) — an unassessed privilege increase; the `dhcp`
   namespace has no grant for `error_codes`/`error_occurrences`, so no `E-DHCP-*`
   occurrence has ever actually persisted (framed as an ADR-0006 question — how does any
   module reach the core-owned error system — not a DHCP-specific bug); and three of
   tonight's six deployment fixes (polkit rule, systemd drop-in, group membership) are
   host-level changes present nowhere in the repo or installer, so a fresh install would
   hit the identical wall tonight worked through by hand. `HANDOFF.md` item 5 marked
   RESOLVED, superseded by this list (committed `d2b4df8` together with the PUNCHLIST
   entries).
5. **Audited and landed two long-orphaned "not mine, don't touch" files**, each flagged
   across multiple sessions without ever being reviewed:
   - `docs/audits/roadmap-state-audit-2026-08-07.md` — mine, from this session, simply
     not yet committed. Committed at the same location/format as every prior baseline.
   - `docs/roadmap/venue-guest-network.md` — traced to an operator decision dated
     2026-08-03 (mobile-agent platform sequencing: Android work deferred until near
     v2.0-final, Apple/Mac/iOS strictly gated behind Android's QR-enrollment flow
     validating end-to-end, not run in parallel) that had sat uncommitted for at least
     four days despite six separate session mentions. Verified the file's pre-existing
     "FLAGGED TENSION" caveat (app-as-credential vs. ADR 0015) was unchanged by this
     diff and already public since 07-26 — no new Rule 10 exposure from landing it; the
     new content itself is a sequencing decision, not a novel mechanism or new
     honest-limitation caveat. Committed (`75b8b14`) alongside the audit doc.
6. **Track C step 4 — `conn_collector.py`'s ETW classification corrected against
   measured provider behaviour** (`9a94244`). Window 1 iterated on this live through the
   session; landed once settled. Two ground-truth-verified corrections, both documented
   in-file with the retracted earlier reasoning left visible rather than deleted:
   `connid` is not a connection identifier (measured: the literal string `"0"` on all
   211 probe events — identity is now derived from the 5-tuple + open timestamp), and
   the prior field-shape close-detection heuristic was actively wrong (`TcpIpSend`
   carries the same `startime`/`endtime` fields the heuristic tested for, so every send
   was misclassified as a close — this also produced a false secondary conclusion, now
   retracted in-file, that ETW delivers events out of order). Classification is now by
   measured event ID, confirmed against exact known byte counts on a LAN rig in both
   directions. `pywintrace` import confirmed lazy (inside `EtwSource.start()`), so the
   module stays importable and its non-ETW logic testable on Linux. 88/88, re-run live
   (was 54/54).

## Working tree state

Fully clean as of `9a94244` — the first point all session either commit reached that
state. Every file Window 1 had left as "ready to commit" from session 001, plus
everything surfaced mid-session (the DHCP grant, the two orphaned docs files, the step-4
collector rewrite), landed and pushed. No held or excluded files remain.

## Push discipline this session

Every push preceded by a fresh `git fetch` and an explicit unpushed-commit list shown to
the operator before pushing (Rule: push coordination). Every push followed by a fresh
fetch confirming `local == origin/main` by hash, not assumed. Six pushes total this
session:
`8c99a85→7400e9c`, `7400e9c→f5deda0`, `f5deda0→d2b4df8`, `d2b4df8→75b8b14`,
`75b8b14→9a94244` — final state confirmed at `9a94244` before this closeout began.

## Concurrent-write handling

Two live concurrent edits from Window 1 were caught mid-session, correctly excluded from
staging (path-based `git add`, never `-A`), and flagged to the operator rather than
silently worked around: `modules/dhcp/module.py` appearing modified mid-audit (later
landed deliberately as its own commit once the accompanying Data Manager grant was
ready), and repeated fresh diffs on `conn_collector.py`/`test_conn_collector.py` through
the session (landed once Window 1's iteration settled, per this session's item 6).

## Verification discipline

Every commit this session was preceded by: a fresh `git fetch` confirming no stale base,
`py_compile` on every touched Python file, a live re-run of the relevant test suite(s)
(never trusted from a prior claim), a full diff read rather than a stat-only skim, and a
Rule 8 leak scan. Two files were held back from a commit specifically because of this
discipline rather than bundled on trust: `install_pihole_pwd.sh` (stray chmod, confirmed
and reverted by Window 1) and, earlier in the day, nothing else — every other file that
reached staging passed review clean.

## Cross-references

- `docs/handoff/worklog/2026-08-07-002.md` — raw log, written live through the session.
- `docs/handoff/supplements/2026-08-07-001.md` — session 001 (the morning checkpoint
  closeout this session picks up after).
- `docs/audits/roadmap-state-audit-2026-08-07.md` — refreshed roadmap baseline.
- `PUNCHLIST.md` — new section, "Four follow-ups from tonight's live DHCP deployment
  (2026-08-07)".
- `docs/handoff/HANDOFF.md` — refreshed this closeout (see below), current state.
