# Session supplement — 2026-07-01-001

> Append-only session log (durable history). Current state lives in `HANDOFF.md` (overwritten).
> Rule 8: placeholders here (`<clone-ip>`, `<box-lan-ip>`, `<tailnet-ip>`, `<project-account>`,
> `<owner>`); real values in `~/work/nemesis-private/local-config.md` (outside the repo).

**Session theme:** trip 1.0.8 installer built + clean-VM lifecycle tested (self-onboard PASS,
uninstall gaps found); clean-uninstall build in progress (phases 1–2 committed, phase 3 WIP); a
batch of design/roadmap docs captured. Docs window ran read-only audits + all doc writes; build
window landed the code.

## Trip 1.0.8 installer — BUILT, TESTED, WORKS

- Build commits on origin: **ab00674** (text fixes + tier-picker), **c933291** (OAuth Tailscale
  key minting + hybrid fallback), plus **c0a5bd0** (description sanitization fix).
- **Clean-VM lifecycle test PASSED the core criterion:** clean self-onboard + Tailscale handoff +
  enrollment (**PENDING**, `auto_approve=0`, **fix #3 single-row confirmed**). Tier-picker, new
  text, and OAuth mint all worked on a bare Win 11 clone.
- **Two bugs found + fixed during regenerate testing** (both "would have bitten on the trip"):
  1. Tailscale key **description rejected dots** → sanitized (**c0a5bd0**).
  2. **Staged agent exe was stale v1.0.7** → rebuilt from CI (run **28545056460**, **c0a5bd0**) and
     re-staged.
- **Server has OAuth live:** `/etc/nemesis.env` has `TAILSCALE_OAUTH_CLIENT_ID`/`SECRET`;
  `is_configured()` = True; description-sanitization deployed via the 15:17 restart.

## Uninstall audit findings

Source: `docs/audits/trip-1.0.8-vm-lifecycle-test-2026-07-01.md`.

- **Local teardown clean.** GAPS:
  - **no uninstaller ships** in the pack;
  - **not registered in Add/Remove Programs** (Tailscale IS — reference implementation);
  - **server GHOST** — de-enroll POSTs to a **nonexistent `:5001` route → 404**;
  - **orphaned tailnet node**;
  - **NO provenance manifest**.

## Clean-uninstall build — IN PROGRESS

Spec: `docs/roadmap/clean-uninstall-build-spec.md` (**0b2aa02**).

- **Phase 1 committed (9321cfe):** provenance manifest w/ probe-before-install, HKCU ARP
  registration, Start Menu, ship-uninstaller wiring.
- **Phase 2 committed (5b03260):** signed `:5001` **de-enroll endpoint** (proof-of-possession,
  soft-mark uninstalled, idempotent, guarded migration). **NOT deployed** — needs restart + Rule-6
  backup; held for after phase 3.
- **Phase 3 IN PROGRESS:** manifest-driven **NemesisUninstall.exe** + consent UX. Must sign
  de-enroll as **`uninstall|<device_id>|<signed_at>`**, PKCS1v15/SHA-256, to match phase 2.
- **After phase 3:** CI exe rebuild → deploy phase 2 (backup + restart + migration) → whole-chain
  VM test on VM `<clone-ip>`. (`<owner>` will add a standard-USER account to VM3 to test non-admin
  install; admin SSH observes.)

## Docs captured today (all on origin)

- Tester install-guide rewrites (Beginner/Intermediate/Pro + WINDOWS_AGENT_SETUP deprecation +
  SETUP_WINDOWS banner).
- Server-on-Windows roadmap (v2 script-gen VM / v3 native; deprecated-unsupported 5GB OVA).
- De-enroll-on-uninstall design item.
- Interactive-AI-clarification roadmap item.
- `docs/CUSTOM_TAILSCALE_OAUTH.md` (+ linked in CLAUDE.md key paths).

## TOMORROW tasks

1. **INTEREST/SHOWCASE DOCUMENT for the Wisconsin trip:** audit Nemesis's current features +
   benefits → proposal-style showcase doc (real deliverable) + "coming soon" section. **Framing =
   low-pressure "are you interested?", NOT a funding pitch.** Honest about stage (in-development,
   AI-assisted, solo). **Benefits-not-features** for a non-technical reader; lead with the **"no IT
   department"** market. Mention a commercial model exists (free BYO-key core + paid tier) but **NO
   pricing numbers** — pricing is a **separate thinking thread**, not in the doc.
2. **Finish + test the clean-uninstall build** (phase 3 → CI rebuild → deploy → VM `<clone-ip>`
   test incl. non-admin user account).
3. **Punchlist:** HW-monitor download-prompt needs tester guidance (screenshot on next install run
   for the exact filename).

## Open / untested

- **§3 approval-flow path** — the device was uninstalled while pending, so the **approval path was
  never tested**.
- **Non-admin install behavior** — untested (the VM3 standard-user test covers this tomorrow).
- **Test residue may linger** — ghost row **d8806d8e** and an orphaned tailnet node — unless
  cleaned.
