# Session log — 2026-06-26-001

Append-only per-session log. Newest entries at the bottom.

## Done this session
- **ai_engine**: migrated (Stage 2 copy) + cut over (Stage 3) to the shared `alerts.db`
  `ai_*` tables — committed.
- **tickets**: migrated (Stage 2 copy into shared `tickets` / `tickets_seq` /
  `tickets_settings`, with the unprefixed `settings`/`ticket_seq` renames) + cut over
  (Stage 3) + full reader/writer sweep across all processes — **committed `6f6b6c3`**,
  pushed to `main`.
  - Cutover gotcha caught & fixed: `watchdog` is a separate process that calls
    `open_ticket()` but never runs `modules_loader.init()`; registered the shared path
    (`set_shared_db_path(HW_DB_PATH)`) before the import so HW auto-tickets keep working.
  - Old `modules/tickets/tickets.db` retained as a live fallback (not deleted).
- **Backups**: took a fresh Stage-0 rollback snapshot before the tickets cutover —
  `nemesis-stage0-refresh-20260626-090254/` on independent external storage, all
  `integrity_check=ok`, with SHA256SUMS.
- **Doc/handoff system**: standing up `CLAUDE.md`, `docs/handoff/`, `docs/roadmap/`,
  `PUNCHLIST.md` (this work).

## Next
- **community_queue** — Stage 2 (copy → `community_*`) then Stage 3 (cutover).
- **Cleanup Stages 4–6** — de-dup table creators, retire ghost DBs, rework backup to a
  single SQLite-safe shared-DB snapshot + auto-discovered services, retire old module DBs.
- **single-user-assumptions audit** (read-only) — after migration, before the
  responsive-dashboard build.
