# Session supplement 2026-06-29-001

> Curated, append-only session log (distilled from worklog `2026-06-29-001`). Placeholders
> per Rule 8 (public repo); real IPs/hosts/names in `~/work/nemesis-private/local-config.md`.

## Scope
A long session spanning: a large morning design-capture arc, the agent-enrollment smoke test,
a public-repo Rule-8 leak scrub, Windows-agent UX code, and closeout. Ran alongside a
concurrent session building the Windows self-installer (pull-before-commit adopted as standing
practice after a benign git race).

## What happened

### Design captures (record, don't build)
Captured a connected product arc in `docs/roadmap/` + PUNCHLIST: malware detection pipeline
(certification scan, hash cache, 4-tier validation, clone sandbox + canary trap, sandbox-first
install, software lifecycle, stale-app report); sandbox→system migration (closing the audit gap
cluster); support bundle; three-snapshot vendor package; Verified Partner Program; AI tutorial
walkthrough (+ searchable index, connected dashboard); pre-escalation support search; device
identification (passive mDNS + active fingerprint); MAC randomization + hardware stable IDs;
installer email delivery. A 39-item capture audit (`docs/audits/roadmap-capture-audit-2026-06-29.md`)
found 31✅/5⚠️/3❌ and drove the sandbox→migration capture.

### Agent enrollment smoke test — 8/8 (decision: trip-ready)
Verified scan-before-trust → enroll → owner-approve → authenticated heartbeat on a Linux test
VM over the LAN. Caught + fixed two **deployment** gaps: port 5001 only opened under
`INSTALL_MODE=windows_vm` (added an always-on subnet-scoped ufw rule, `c37a177`); live
`hw-monitor` running pre-enrollment code (restarted → endpoints live + `agent_devices`
migration). Audit-first also caught (and stopped) a naive port-redirect that would have
blacked out the dashboard — resolved as an nginx-layer concern.

### Rule-8 public-repo leak scrub (trip-critical)
Repo confirmed PUBLIC; concurrent installer work surfaced leaks. Audited handoff + repo-wide,
then scrubbed in 4 separate commits (PII device names/IPs; handoff box/tailnet IP + project
account; hardcoded box IP/subnet in shipped code → env-driven; explicit CLAUDE.md handoff
leak-scan rule + OPERATION.md nginx entrypoint + PUNCHLIST port-canon note). HEAD verified
clean; git history intentionally **not** rewritten (others may have cloned).

### Windows agent UX (code, 3 commits)
Uninstall script (clean removal, never touches Tailscale); Tailscale 5-state handling with
retry loop + State-5 enroll-failure hint; `/api/health` endpoint (env-driven version) +
nginx auth-bypass + installer reachability check completing the connection chain.

### Verified on real hardware (owner)
First **non-developer** enrollment via the token installer (a family member's laptop) — proved
the non-technical-user install flow end-to-end. `/api/health` confirmed 200 live. Agent version
bumped to **v1.0.6**. `link_type` detection in progress.

## Decisions
- Pull-before-commit is mandatory on this repo (concurrent sessions). Recorded to memory.
- Port canonicalization lives at nginx, never a Flask redirect (would bounce users to the
  ufw-blocked internal :5000).
- Rule-8 applies to handoff docs explicitly; sanitize real names/IPs/accounts, real values stay
  in local-config (outside repo). No git-history rewrite.
- `/api/agent/uninstall` server endpoint deferred — uninstall script notifies best-effort.

## State at closeout
All 3 deploy gates resolved (env vars set; nginx bypass applied + 200; tailnet ufw rules already
existed). Windows self-installer v1.0.6 built + proven on real hardware (one device); a clean
9-phase Windows VM test is the next validation. Trip-ready. See HANDOFF for the live list.
