# Session supplement — 2026-08-31-001 (Window 2)

Full raw chronology: `docs/handoff/worklog/2026-08-31-001.md` (reconstructed at closeout
rather than kept live this session — noted there, not repeated here).

## Shape of the day

Morning Status found the roadmap-state audit badly stale (78 `feat` commits since the
08-24 baseline) and flagged it rather than attempting a quick incremental diff. The
operator then commissioned a full re-derivation, which became the day's throughline:
audit → fix what the audit found stale → verify what got fixed is actually live → audit
again at a different layer (completeness) → fix what that found → repeat, twice more,
against increasingly deep layers (roadmap docs → individual subsystems → two full ADRs).
Genuinely the largest single-day documentation-correction effort on record for this repo.

## What shipped, by theme

**Roadmap audit (full reconciliation).** First full per-file re-derivation since
2026-08-06 — every baseline since had been an incremental diff against an unverified
base. Six parallel investigations, cross-checked against the actual 08-06 source, caught
two sub-agent false negatives before they could ship as fact. Final tally: 13 SHIPPED /
13 PARTIAL / 59 PARKED (85 tracked, 1 excluded reference doc, 86 files on disk) —
`docs/audits/roadmap-state-audit-2026-08-31.md`. `malware-detection-pipeline.md` retired
(superseded by its own tracked sub-features); `product-thesis-built-in-it-expertise.md`
pulled out of the tally entirely (category error, not a build item); 5 roadmap files had
stale headers fixed where the audit-doc tally had been right for weeks but the source
file itself was never corrected.

**Two ADR rewrites**, both replacing a diagnosis that measurement had refuted:
- `docs/architecture/0005-dns-firewall-device-auth-architecture.md` §1 — the
  client-refusal-by-source PIA diagnosis was itself wrong (the original "decisive
  experiment" used the same source address in both its arms, so it never tested what it
  claimed to). Replaced with what was actually measured, retracted the "vpn-dns-guard
  solves the wrong problem" claim, recorded the real defect (a connectivity watcher that
  read `vpn_connected=1` into its own DB row and then ignored it) and its already-live
  fix. §8 also added: the Fork B install-time NAT chokepoint exception, correctly
  attributed to this ADR rather than ADR 0009.
- `ADR-0019-AMENDMENT-01-steering-authority-2026-08-08.md` (private repo) — retired an
  expired comparison-based argument for a mechanically-enforced one, resolved a real
  internal contradiction between two of its own sections, added a risk the amendment had
  never listed (a *healthy* renderer destroying steering, found and fixed same day), and
  tracked the mechanism from "does not exist" through "built, render-proven only" to
  "proven end-to-end on a real nftables table" as the actual build happened underneath
  the doc across the session — three separate status-line updates, each one correct
  when written, none of them left stale.

**Completeness audit.** Eight parallel forks verified Gateway Mode, Fork B,
admin-approval, DNS-exfil/rogue-DHCP, Track C, email-security, PII redaction, and the
error-code work against live/deployed state rather than commit messages. Real, useful
findings: Fork B's rebuilt classifier has zero production callers; the A1/A2
admin-approval WebAuthn ceremony has never run against a real browser or physical key
(now filed); a recurring "service predates the commit" gap surfaced independently in
three different subsystems (Gateway Mode, email-security, error-code work) — each one
caught by comparing `ExecMainStartTimestamp` against the commit timestamp rather than
trusting a curl 200 or a green test suite.

**PUNCHLIST corrections.** Two self-corrections applied to the same finding this
session, both against the operator's/Window 3's own prior claims, both verified
independently before being written down rather than transcribed on trust: the
many-large-prefixes tunnel-coverage gap (owed since 08-30, never filed until today), and
two rounds of "this piece isn't actually built" corrections on the TLS-interception
undercount finding (Piece E, then Piece F — the second one caught by Window 3 while
scoping a third piece, Piece G, and independently re-verified by this window before
landing).

## Push volume and cross-window coordination

Every push this session followed the same shape: list the full range, verify each
commit independently (real test runs, live route curls, DB row counts — not trusting
commit messages), Rule-8 scan, gate-check immediately before pushing (caught real drift
twice — new commits landing in the shared tree between confirmation and push, both times
correctly excluded via SHA-pinned pushes), then verify `local == origin` after.
Approximate volume: ~90 commits pushed across the public repo and three private repos
today, none of them mis-attributed, none pushed without independent verification.

Two live cross-session exchanges with Window 3 (`nemesis-07`): a file-clearance check
before a build (answered honestly — not Window 1, shared what could be verified
read-only, declined to speak for Window 1's intentions) and two PUNCHLIST correction
requests, both independently re-verified against the actual code before being applied
rather than transcribed on trust.

## Open items, not forgotten

- **~100+ untracked files in `~/work/nemesis-internal`** — audits, briefings, decisions,
  scoping docs dating back to 2026-08-01, never committed. Flagged twice this session,
  not resolved. Real risk (no git history, no reflog protection) distinct from and worse
  than the routine push-backlog pattern. Needs a decision on whose job it is to sort
  through and commit.
- **A1/A2 WebAuthn ceremony** has never run against a real browser + physical key —
  filed as PUNCHLIST `[HIGH]` this session (self-disclosed in a commit message,
  previously untracked anywhere that gets routinely scanned).
- **Fork B's `reconcile()`** has zero production callers — built, tested, completely
  dormant. Not urgent (`ip_forward=0`), but worth naming plainly: nothing in production
  currently exercises this code path.
- **WRITE_OPS audit-trail gap** (`allow_port_on_interface`/`deny_port_on_interface`
  execute with no audit record) — filed, not fixed, deliberately (wants its own commit +
  test per Rule 2).
- **Original PIA-vs-Nemesis compatibility question** — the connectivity-watcher
  misclassification is fixed, but nobody has re-tested whether PIA's *original* reported
  "Nemesis throws errors" symptom (never recorded in detail) still reproduces now that
  PIA is reconnected. Left open in PUNCHLIST, not closed.
- **Fork B's production collector** — still not built; the topology classifier it would
  call is tested and correct but has nowhere to run from yet.

## Process note, for the record

This session ran long enough, and dense enough, that the live worklog (Rule 9's
"append the moment each step completes") was not actually kept live — it was
reconstructed retrospectively at closeout instead, from full conversation context. The
reconstruction is accurate (verified against the actual commit history while writing
it), but the discipline gap is real and worth naming rather than quietly closing over:
a session this size is exactly the case the live-worklog rule exists for, since it is
also the case most likely to lose fidelity if reconstructed from memory instead of
appended in real time.
