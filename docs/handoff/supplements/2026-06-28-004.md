# Session supplement — 2026-06-28-004

> Append-only durable record. End-of-day closeout for a MAJOR day: minimal auth went live,
> the Windows agent v0 was built, and a large architecture body (Data Manager, device-user
> model, impossible-travel, inspection proxy, ZTNA/NAC/venue, MSP plane) was locked — ahead
> of Friday's Wisconsin deployment. (Morning diagnostics/identity work is in supplement
> `2026-06-28-003.md`.)

---

## Auth system live
- **Flask-Login minimal auth deployed and verified** (`21c8931`).
- **First-run setup complete** — the owner (**`<owner>`**) account was created.
- **Tiered lockout:** 3 attempts → 5 min / MEDIUM; 5 → 15 min / HIGH + email; 10 → 60 min /
  CRITICAL + email. `login_events` collecting from day one.
- **Concurrent session seam:** HIGH ticket if the same username logs in from a different IP
  within 24h.
- **2FA by default:** Flask-Login (something you know) + RSA keypair (something you have).
  Marketing one-liner captured (product thesis).
- **SSH recovery:** `python3 core/manage.py reset-password / unlock / list-users / create-user`.

## Windows agent
- **Owner-gated RSA enrollment built** (`enrollment.py`) — **additive** to the existing
  cross-platform agent. (Code **found and preserved the mature existing agent** rather than
  overwriting it.)
- **`platform/` → `platforms/` rename** fixes a **stdlib shadow bug** — the agent is now
  actually launchable on Windows.
- **`agent_devices`: 11 new columns** in one migration (all together): `enrollment_status`,
  `public_key`, `enrolled_by`, `enrolled_at`, `os`, `os_version`, `hardware_summary`,
  `interface_name`, `connection_speed`, `lhm_available`, `last_heartbeat_data`.
- **Dashboard enrollment UI:** pending list + approve/reject + `enrolled_by=current_user.username`.
- **`install_windows.ps1` extended:** Tailscale check/install offer, LHM web server auto-start
  on 8085, enrollment step.
- **NOT YET SMOKE TESTED on a real Windows box** (the resume item).

## Major architecture locked today
- **Data Manager (ADR 0006):** single authoritative DB layer, mandatory for all modules,
  enforced by the loader.
- **Device-user model (ADR 0007):** many-to-many device↔user; handles shared workstations +
  the traveling IT person.
- **Impossible-travel detection (ADR 0008):** `login_events` collecting now; detection logic
  v2; cross-site detection v3+.
- **Security Inspection Proxy (ADR 0009 — ⚠ decision captured, ADR FILE NOT YET WRITTEN):**
  the tunnel carries **decisions, not data** (self-hosted SSE). Solves the WiFi blind spot
  completely — **WiFi + Mode 2 = full protection** (Pi-hole + Suricata via the tunnel);
  bandwidth negligible after cache warm-up. **TODO: write the ADR 0009 file** (this is only a
  captured summary so far).
- **ZTNA + NAC:** no agent = no WiFi (captive-portal enforcement). **Venue guest network:**
  agent as credential, TOS disclosure, auto-approve after a clean scan.
- **MSP management plane:** v3+ roadmap; clean API seam to leave now.
- **AI hacking inflection point:** behavioral detection is the answer to AI-generated novel
  malware. Market timing: building now, before the wave. Thesis — *"AI makes attacking easy.
  Nemesis makes defending automatic. No IT department required."*
- **Enrollment enriches lateral-movement detection:** behavioral baseline per device,
  connection graph, post-detection timing, compounding accuracy over time. Repeat venue guests
  restore their historical baseline on reconnection.

## Operational notes
- **Starlink confirmed working** (SSH test passed over Tailscale).
- **Starlink Gen 3** (2 ethernet ports) — bypass mode + Netgear for the full stack in the
  camper, or direct ethernet to the laptop for a simple setup.
- **Ethernet cable needed** (find before Friday).
- Son's laptop needs Tailscale re-enrollment (`<project-account>`) when convenient.
- KDE Connect broken — not pursuing (prefer Tailscale).
- Header light green (all test artifacts cleaned up).
- **Total code lines: ~30,500+** (was 28,672 this morning).

## Tomorrow (Monday)
1. Morning status fires automatically (new Code session).
2. **Pre-enrollment scan build** (scan device before trust — small, targeted addition to the
   enrollment flow).
3. **Windows smoke test** (`install.ps1` on a real Windows box — 12-step verification sequence,
   see the Wisconsin test plan in HANDOFF).
4. Fix any gaps found.
5. Session closeout.
6. **Trip ready** (Friday departure).
